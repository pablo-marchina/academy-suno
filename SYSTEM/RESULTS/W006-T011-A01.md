# RESULT — W006-T011-A01

`TASK_ID: W006-T011`  
`ATTEMPT_ID: A01`  
`BASE_STATE_VERSION: 0047`  
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`  
`OBSERVED_MAIN_SHA_AT_START: 598e633e43399138c06813ef38de2fe90b7686da`  
`OBSERVED_STATE_VERSION_AT_START: 0058`  
`WORKER_BRANCH: worker/W006-T011-A01`  
`STATUS: RESULT_READY_FOR_PR_CI`  
`CONTINUITY_CHECK: PASS`

## Outcome

T011 refreshed current provider/model/pricing/lifecycle/rate-limit facts from primary sources and froze a DRG-compatible comparison package under `W005-BENCHMARK-METHODOLOGY-V002`.

No provider/model/default, deterministic cascade, or learned route is promoted. The material decision remains:

- `PARETO_NOT_COMPUTABLE`
- `NO_PREFERENCE`
- `BUSINESS_UTILITY: PENDING_EVIDENCE`
- `PRODUCTION_READY: NOT_AUTHORIZED`

This is deliberate evidence discipline, not a synthetic benchmark result.

## Current candidate facts

The bounded current catalog is:

1. OpenAI `gpt-5.6-terra` via Responses API. Official model documentation observed on 2026-09-23 states structured outputs support, 1.05M context / 128k max output, standard short-context token pricing of $2.00 input / $0.20 cached input / $12.00 output per 1M tokens, and usage-tier rate-limit tables.
2. Anthropic `claude-sonnet-5` via Messages API. Official documentation observed on 2026-09-23 marks it `Active`, with tentative retirement not sooner than 2027-06-30, structured outputs support, and list pricing of $2 input / $10 output per MTok plus documented cache rates. Rate limits are organization/tier scoped; current tenant limits were not observed.
3. Google `gemini-3.8-flash` via `generateContent`. Official documentation observed on 2026-09-23 marks the stable model GA, updated September 2026, with structured outputs, 1,048,576 input / 65,536 output token limits, and introductory $0.75 input / $3.75 output per 1M tokens through 2026-12-31 (then $1.50 / $7.50 from 2027-01-01). Interactive rate limits are project/model/tier dependent and active values are exposed in AI Studio.

Exact URLs, checked timestamps, pricing IDs, lifecycle context and rate-limit context are persisted in `provider_current_facts_t011_v001.json`.

## Execution / exposure evidence

A fresh current-model benchmark was **not run** in this attempt.

Two independent blockers are persisted rather than hidden:

1. the available GitHub connection exposes workflow read/rerun operations but no operation to create a new `workflow_dispatch`; re-running an old dispatch would reuse stale inputs and is not an exact current-model benchmark;
2. the integrated W006 partition manifest explicitly says its six-source DEV/CALIBRATION bootstrap is not representative of production traffic, so it cannot support the dispatch's material provider-default promotion claim.

The repository does contain a protected manual provider workflow and harness for `openai_responses` and `anthropic_messages`; those are therefore the bounded next execution targets. Gemini remains a protocol gap in the existing provider harness.

## Historical observed control

The accepted W004 A08 artifact is preserved only as historical mechanics evidence:

- provider/model: Groq / `openai/gpt-oss-120b`;
- captured: `2026-09-21T22:54:07.119863+00:00`;
- run: `provider-run-f48d0b831ac6d34e`;
- observed latency: `349.694 ms`;
- observed usage: `84` input / `61` output / `145` total tokens;
- derived cost from official snapshot: `$0.0000492`;
- response fingerprint: `5a3466faf9d179f5da92cde5ad5de9e2227e9821c7260976040db466e7b8d3fc`;
- content-quality evidence: absent;
- full provider comparison eligibility: false.

It is not reused as current quality evidence and does not select a T011 winner.

## Frozen comparison design

The manifest pins:

- methodology: `W005-BENCHMARK-METHODOLOGY-V002`;
- paired contract: `w006-paired-experiment-contract-v001`;
- pair key: `source_group_id × audience × format`;
- source-group clustering and no 3×3 sample inflation;
- hard gates before cost/latency;
- raw named quality axes, latency distributions, observed token usage + effective pricing, and reliability/failure counts;
- explicit missingness;
- no scalar utility;
- no HELD_OUT tuning;
- no production audience threshold from model/self-consistency evidence.

The bounded next packet contains the four non-HELD_OUT bootstrap source groups × 3 audiences × 3 formats = 36 planned jobs per candidate. It remains labeled `BOUNDED_DIAGNOSTIC_SAMPLE`, not production-representative evidence.

## Pareto / routing / rollback

All fresh candidate measured objectives are missing because the runs were not executed. Therefore the required-objective Pareto comparison is `PARETO_NOT_COMPUTABLE`; selecting a winner would violate the accepted W005 methodology.

Deterministic cascade and learned shadow routing are not evaluated because there is not yet a two-arm measured eligibility envelope. Learned routing remains non-default.

Rollback evidence is `NOT_APPLICABLE_NO_PROMOTION`. This is not converted into a fake PASS. A future adaptive/default promotion must first supply an actual critical-failure canary rollback PASS.

## Human-evidence boundary

T010's boundary is unchanged:

- independent human primary streams: `0`;
- adjudicated human-gold items: `0`;
- audience thresholds: `DIAGNOSTIC_ONLY`;
- HELD_OUT release replication: `NOT_RUN`.

No model/self-consistency evidence is upgraded into human calibration.

## Hard-acceptance mapping

- hard-gate violating output accepted for cost/latency: `0`;
- capability-ineligible fallback: `0`;
- invalid auth/config/tenant/policy blindly retried: `0` in the existing one-shot harness;
- accepted fresh run missing policy/catalog/pricing identity: `0` (fresh accepted runs: `0`);
- provider/model/pricing evidence without checked/effective context used for promotion: `0`;
- forced winner on unresolved Pareto: `0`;
- scalar/business utility without representative evidence: `0`;
- missing human gold converted into production audience thresholds: `0`;
- production-ready claim: `NOT_AUTHORIZED`.

## Reproducibility artifacts

- `data/evals/production/w006/provider_current_facts_t011_v001.json`
- `data/evals/production/w006/provider_benchmark_manifest_t011_v001.json`
- `data/evals/production/w006/provider_benchmark_observations_t011_v001.json`
- `data/evals/production/w006/provider_pareto_t011_v001.json`
- `experiments/provider_benchmark_w006/validate_t011_evidence.py`
- `tests/evals/test_provider_benchmark_w006_t011.py`

Generation/config identity is frozen as `sha256:0daf6b598f4988aedfc119c860d9a7156d261fb88575d1c83ac87bea4ec98d9e`.

## Verification

Worker-side isolated validation:

```text
python experiments/provider_benchmark_w006/validate_t011_evidence.py
PASS — W006-T011 evidence invariants validated

python -m unittest tests.evals.test_provider_benchmark_w006_t011 -v
Ran 5 tests in 0.001s
OK
```

The validator fails closed on fabricated measurements, missing current-fact provenance, a forced preference, unauthorized production claims, sample-inflation drift, or human-evidence drift.

## Decision Research Gate

No new DRG material-promotion record is created because this attempt makes no material provider/model/routing promotion. The accepted W005 benchmark methodology remains the governing decision contract. A future promotion must persist representative measured evidence, sensitivity/utility evidence if utility is introduced, and rollback proof before changing defaults.

## Next bounded evidence

At most two fresh provider runs should be executed first, using the protected `provider-observed-evidence` environment and exact current inputs:

1. OpenAI `gpt-5.6-terra`;
2. Anthropic `claude-sonnet-5`.

Both must consume the same frozen corpus packet, retain pair/source-group identity, persist effective pricing and raw failures, and remain diagnostic until representativeness and human-evidence requirements are satisfied. Gemini should only enter the comparable execution set after a first-party `generateContent` protocol adapter is added to the evidence harness.
