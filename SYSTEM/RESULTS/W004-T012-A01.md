# RESULT W004-T012-A01

`TASK_ID: W004-T012`  
`ATTEMPT_ID: A01`  
`ROLE: Auditor/Builder`  
`STATUS: COMPLETE — READY_FOR_ORCHESTRATOR_REVIEW`  
`CONFIDENCE: HIGH for task-scoped clean-CI release-smoke evidence; NO CLAIM for production readiness, calibrated human thresholds, real provider quality/cost, semantic-backend preference, or parser implementation winner`  
`BASE_STATE_VERSION: 0023`  
`BASE_COMMIT_SHA: 2fcf016ade631e9307ec0d222d633c066adf4c88`  
`OBSERVED_MAIN_SHA_AT_START: 2fcf016ade631e9307ec0d222d633c066adf4c88`  
`OBSERVED_MAIN_SHA_AT_FINISH: 2fcf016ade631e9307ec0d222d633c066adf4c88`  
`ISSUE: #102`  
`WORKER_BRANCH: worker/W004-T012-A01`

## 1. CONTINUITY_CHECK

`PASS`.

- Protocol `1.6.0` and canonical state `0023` matched the dispatch/Issue bind.
- Issue #102 bound this attempt to exact base `2fcf016ade631e9307ec0d222d633c066adf4c88` after W004-T011 integration.
- `main` matched that SHA at start and remained there at finish.
- The worker branch was created from the exact bound SHA before substantive work.
- Lifecycle `TASK_STARTED` and material `TASK_PROGRESS` signals were persisted on Issue #102.
- No canonical coordination file was modified.

## 2. Objective executed

W004-T012 converted the T011 limitation “workflow compiles but does not explicitly execute `tests/release_smoke`” into observed clean-checkout CI evidence.

A task-specific workflow was added at `.github/workflows/release-smoke-w004-t012.yml`. On push to the isolated worker branch it:

1. performs a clean `actions/checkout@v4` checkout;
2. provisions Python 3.13 and the pinned `pydantic==2.13.4` / `pytest==9.0.2` dependencies;
3. explicitly runs `scripts/release_smoke/run_release_smoke.py` and persists its machine-readable manifest;
4. explicitly runs `python -m pytest -q tests/release_smoke/test_release_smoke.py`;
5. asserts the task acceptance boundaries from the generated manifest;
6. writes CI provenance;
7. uploads the complete task-specific evidence packet as a GitHub Actions artifact.

## 3. Clean CI provenance

Observed GitHub Actions execution:

- workflow: `W004 T012 Release Smoke`;
- run: `35605139400`;
- run attempt: `1`;
- job: `106350276042` (`clean-checkout-release-smoke`);
- execution head: `bd6e90021aa007be23de555d3751612372192a4b`;
- runner: Ubuntu 24.04.5 LTS / `ubuntu-24.04` image;
- Python: `3.13.15`;
- conclusion: `success`.

All task-critical steps completed successfully:

- `Execute release smoke and persist manifest` — success;
- `Execute focused release-smoke test` — success;
- `Verify task acceptance boundaries` — success;
- `Capture CI provenance` — success;
- `Upload task-specific evidence` — success.

The focused pytest result was observed as:

```text
1 passed in 0.45s
```

Persisted provenance: `docs/release/release_smoke/W004-T012-A01-ci-provenance.json`.

## 4. Exact mechanics and repair evidence

The clean-CI proof report observed:

- `final_phase = complete`;
- `job_count = 9`;
- `joined_output_count = 9`;
- exact 3×3 mechanics therefore completed `9/9`;
- evidence scope remains `MECHANICS_ONLY`;
- provider mode remains `deterministic_stub`;
- provider quality claim remains `NOT_MADE`;
- real provider cost evidence remains `N/A`.

Persisted repair lineage observed for `beginner:carousel`:

- before status: `FAIL`;
- after status: `PASS`;
- before output hash: `2daec349f65ac35140f7c35cf78cfe22ef9b8c53516ea63f7884ba80a9691ee8`;
- after output hash: `c031b71292c25cdb4c28605945f7df8f19c399d5fb9eef4b58b6dbbfc1b0a64e`;
- fresh hard-gate runs: `true`;
- accepted siblings immutable: `true`.

This is execution evidence, not compile-only evidence.

## 5. Cockpit and parser gates

The smoke runner completed the evidence-cockpit generation and verified that the generated HTML retained:

- `PROVEN`;
- `DIAGNOSTIC_ONLY`;
- `PRODUCTION_UNKNOWN`;
- `MECHANICS_ONLY`;
- both before/after repair hashes;
- the explicit statement that no aggregate readiness score is computed.

The parser/source-trust bakeoff completed and observed:

| Gate | Observed status |
|---|---|
| provenance-preserving reference | `PASS` |
| flat-value-only candidate | `REVIEW_REQUIRED` |
| wrong-role probe | `FAIL` |
| wrong-unit probe | `FAIL` |
| implementation decision | `UNLOCKED` |
| preferred implementation | `null` |

No parser implementation winner is inferred from these observations.

## 6. External/semantic gaps remain unpromoted

The clean run preserved the required evidence boundaries:

| Claim area | Posture after T012 |
|---|---|
| controlled mechanics | `PROVEN` within `MECHANICS_ONLY` |
| audience thresholds | `DIAGNOSTIC_ONLY` |
| human confusion matrices | `BLOCKED/PENDING` |
| real provider quality/latency/cost | `PRODUCTION_UNKNOWN/BLOCKED` |
| parser implementation | `PENDING/UNLOCKED` |
| semantic ablation | `NOT_RUN/PENDING` |
| production/release readiness | `PENDING_W004_T008` |

Explicit non-claims remain:

1. no calibrated audience-threshold claim;
2. no provider/model quality or preference claim;
3. no real provider-cost claim;
4. no production-readiness claim.

## 7. Machine-readable evidence and artifact provenance

Persisted task-specific manifest:

- `docs/release/release_smoke/W004-T012-A01-manifest.json`

Persisted CI provenance:

- `docs/release/release_smoke/W004-T012-A01-ci-provenance.json`

GitHub Actions artifact:

- artifact ID: `10641632565`;
- name: `w004-t012-release-smoke-bd6e90021aa007be23de555d3751612372192a4b`;
- size: `39086` bytes;
- digest: `sha256:18e17939b212c6cb43a36d5a7d3298378c81792b9b0047b954324d760c5787f4`;
- expiry reported by GitHub: `2026-12-20T13:21:57Z`.

The uploaded artifact contains seven files: raw smoke manifest, CI provenance, cockpit HTML, parser bakeoff JSON, proof report, RunStore history, and telemetry JSONL.

The raw T011 runner manifest intentionally retains `task_attempt_id = W004-T011-A01`, because it is the runner authored by T011. The persisted T012 wrapper manifest binds this observed clean-CI execution to `W004-T012-A01`; no source runner identity was silently rewritten.

## 8. Acceptance mapping

| Acceptance condition | Evidence | Result |
|---|---|---|
| release smoke executes, not just compiles | CI run step executes `run_release_smoke.py` | `PASS` |
| focused test executes | pytest reports `1 passed` | `PASS` |
| exact 9/9 mechanics | proof report `job_count=9`, `joined_output_count=9` | `PASS` |
| persisted FAIL→repair→PASS | distinct persisted before/after states and hashes | `PASS` |
| fresh hard gates + sibling immutability | both observed `true` | `PASS` |
| cockpit gates | required markers/hashes/non-aggregate posture asserted | `PASS` |
| parser gates | reference PASS; unsafe probes remain non-PASS; impl UNLOCKED | `PASS` |
| external gaps stay blocked/pending | human/provider/semantic postures not promoted | `PASS` |
| machine-readable manifest | task-specific JSON persisted | `PASS` |
| CI provenance | repo JSON + Actions artifact provenance | `PASS` |
| no production-readiness claim | explicit `false` + `PENDING_W004_T008` | `PASS` |
| lifecycle signals | STARTED + PROGRESS; terminal after RESULT persistence | `PASS` |

## 9. SUCCESS_IMPACT

T012 closes the internal evidence gap identified by T011: release-smoke behavior is now observed in a clean CI checkout instead of inferred from syntax/compile checks. It increases confidence in the controlled mechanics, repair lineage, evidence cockpit, and parser/source-trust release path without laundering missing human/provider evidence into readiness claims.

It does **not** close the project’s external validity bottlenecks and does not substitute for W004-T005, W004-T006, W004-T007, or final W004-T008.

## 10. TRACEABILITY_UPDATES

- T011 clean-release limitation → task-specific workflow run `35605139400` → executed smoke + pytest + acceptance assertions.
- 9/9 mechanics → uploaded `proof_report_v001.json` → task manifest `mechanics` section.
- FAIL→repair→PASS → proof repair lineage → task manifest `repair_lineage` section.
- cockpit/parser gates → generated cockpit/parser artifacts → CI smoke assertions + task manifest gate sections.
- “no production readiness” requirement → `production_readiness_claim: false`, `PENDING_W004_T008`, and four retained non-claims.

No canonical traceability matrix was modified by this worker.

## 11. Assumptions and risks

- `ASSUMPTION`: GitHub-hosted clean checkout is an appropriate clean-CI environment for this task. This is directly evidenced by the checkout logs and isolated runner workspace.
- `RISK`: the GitHub Actions raw artifact is retention-bound; the stable task manifest/provenance therefore records artifact ID, digest and acceptance-relevant observations in-repo.
- `RISK`: the source release-smoke manifest names T011 because that is its authoring task. This is explicitly surfaced rather than rewritten; T012 identity lives in the task-specific wrapper evidence and CI provenance.
- Existing external evidence blockers are intentionally unchanged.

## 12. State / decision proposals

For Orchestrator review only:

- accept W004-T012-A01 as task-complete evidence for the clean release-smoke gate;
- integrate the task-specific workflow/evidence if desired;
- keep human/provider/semantic/final-release gates in their existing blocked/pending states;
- do not infer production readiness from this result.

## 13. Artifacts

- `.github/workflows/release-smoke-w004-t012.yml`
- `docs/release/release_smoke/W004-T012-A01-manifest.json`
- `docs/release/release_smoke/W004-T012-A01-ci-provenance.json`
- `SYSTEM/RESULTS/W004-T012-A01.md`
- GitHub Actions run `35605139400`
- GitHub Actions artifact `10641632565`

## 14. Next actions

1. Orchestrator reviews this result and the worker PR, then integrates if accepted.
2. W004-T005 remains dependent on two genuine independent human primary streams.
3. W004-T006 remains dependent on valid development human gold.
4. W004-T007 remains dependent on valid human evidence plus observed credentialed provider evidence.
5. W004-T008 remains the final clean-E2E release proof and must keep release readiness pending until its prerequisites are genuinely satisfied.
