# RESULT W004-T004-A01

`TASK_ID: W004-T004`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0020`
`BASE_COMMIT_SHA: 2555308805c2b3eac4ef494605c35e01697f8884`
`WORKER_BRANCH: worker/W004-T004-A01`
`STATUS: BLOCKED_EXTERNAL_PROVIDER_ACCESS`
`CONFIDENCE: HIGH`

## Continuity

- `PROTOCOL_VERSION: 1.6.0`
- observed canonical `STATE_VERSION: 0020`
- observed main SHA at start: `2555308805c2b3eac4ef494605c35e01697f8884`
- Issue #84 base SHA: `2555308805c2b3eac4ef494605c35e01697f8884`
- `CONTINUITY_CHECK: PASS`
- required `TASK_STARTED` signal was emitted after branch creation and before substantive work.

## Outcome

Prepared a credential-safe provider/model mechanics harness and evidence protocol that can execute real HTTP provider calls when a credentialed runtime is available. The worker runtime did not expose provider credentials or another authorized external-provider execution channel, so **no real provider/model call was fabricated**.

The harness records provider/protocol/model/version/endpoint provenance, observed request latency, provider-reported token usage, pricing-source provenance, and cost derivation only when both usage and an eligible official pricing snapshot are available. Missing observations remain explicit `N/A`.

## Findings / evidence

### Mechanics harness

`experiments/provider_w004/run_provider_probe.py`

- supports `openai_responses` and `anthropic_messages` wire formats without introducing a provider preference;
- reads API credentials only from a caller-specified environment variable;
- records latency only after an actual HTTP attempt;
- extracts usage only from provider response metadata;
- stores response hash/byte count rather than treating response content as quality evidence;
- classifies the output as `MECHANICS_ONLY` and sets `content_quality_evidence: false`.

### Pricing / cost evidence rule

`docs/provider_w004/pricing_snapshot.example.json` is deliberately marked `synthetic` and therefore ineligible as commercial evidence. Cost remains `N/A` unless:

1. provider-reported usage is observed; and
2. the supplied pricing snapshot declares `kind: official_provider_pricing`; and
3. source URL, retrieval/effective timestamps, currency, and numeric input/output prices are present.

This prevents synthetic pricing from becoming a commercial claim.

### Validation

Local isolated validation of the persisted-equivalent files:

```text
python -m unittest discover -s tests/provider_w004 -v
Ran 4 tests in 0.001s
OK
```

Validated invariants:

- unobserved usage stays `N/A`;
- synthetic pricing is not commercial evidence and cannot derive cost;
- official pricing + observed usage can derive cost deterministically;
- missing credential returns `BLOCKED_NO_CREDENTIAL`, with latency/usage/cost still `N/A` and without a network call.

A no-credential probe was persisted at `experiments/provider_w004/runs/credential_blocker_probe.json`. It is blocker/mechanics evidence only, not a provider run.

## Blocker

`BLOCKER: authorized credentialed provider runtime unavailable to this worker attempt.`

`NEEDS: execute the prepared harness in a runtime with an authorized provider API credential; for any commercial cost claim, pair the run with a versioned snapshot of that provider's official pricing source.`

Because no external provider call occurred, observed provider latency, usage, model-version response metadata, and real cost remain `N/A` / `PRODUCTION_UNKNOWN`.

## Acceptance status

- provider/model/version/pricing provenance explicit: **PASS for harness/schema; real-run values pending runtime**
- usage/cost remain `N/A` when unobserved: **PASS**
- synthetic pricing ineligible as commercial evidence: **PASS**
- mechanics separated from quality evidence: **PASS**
- no provider preference without comparable measured data: **PASS**
- explicit blocker if real execution unavailable: **PASS**
- lifecycle signals required: **PASS at start; terminal signal follows this RESULT**

## SUCCESS_IMPACT

Positive but partial. The task reduces execution-robustness risk by converting provider telemetry requirements into an executable, falsification-safe harness and preventing false precision around cost/usage. It does **not** resolve the W004 production unknown for real provider/model quality, latency, usage, or cost because credentialed execution was unavailable.

Relevant targets: `REQ-003`, `REQ-016`, `REQ-020`, Feasibility, Partner Outcome, Execution Robustness. Risk reduction is partial for `RISK-0017`, `RISK-0019`, `RISK-0021`; none should be considered closed from this attempt alone.

## TRACEABILITY_UPDATES

Proposed for Orchestrator integration only (worker does not edit canonical traceability):

- link `REQ-003/016/020` to `experiments/provider_w004/run_provider_probe.py` as executable mechanics evidence;
- retain provider latency/usage/cost and provider-comparison claims as `PRODUCTION_UNKNOWN` until observed comparable runs exist;
- mark the pricing template as guardrail evidence, not commercial evidence.

## Assumptions / risks affected

- assumption that provider telemetry can be captured without contaminating content-quality evidence: supported by executable harness + tests;
- assumption that cost can be safely claimed without official pricing provenance: rejected by guardrail;
- external credential/runtime availability remains unresolved;
- provider/model preference remains unresolved and must not be inferred from this attempt.

## Artifacts

- `experiments/provider_w004/run_provider_probe.py`
- `experiments/provider_w004/runs/credential_blocker_probe.json`
- `tests/provider_w004/test_run_provider_probe.py`
- `docs/provider_w004/README.md`
- `docs/provider_w004/pricing_snapshot.example.json`

## Next actions

1. Run the harness in an authorized credentialed runtime against the exact provider/model/version candidates to be compared.
2. Use the same representative prompt/input set and capture observed latency + provider usage under comparable conditions.
3. Snapshot official pricing with source/effective/retrieval provenance before deriving cost.
4. Feed those observed mechanics into W004-T007 together with independent quality evidence; do not choose a provider from mechanics alone.
