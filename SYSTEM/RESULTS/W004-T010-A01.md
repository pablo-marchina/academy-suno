# RESULT W004-T010-A01

`TASK_ID: W004-T010`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0022`
`BASE_COMMIT_SHA: 7059381fd34aee20f159a477ba61eb4c4048af45`
`WORKER_BRANCH: worker/W004-T010-A01`
`STATUS: COMPLETE`
`CONFIDENCE: HIGH`

## Continuity

- `PROTOCOL_VERSION: 1.6.0`
- observed canonical `STATE_VERSION: 0022`
- Issue #96 base SHA: `7059381fd34aee20f159a477ba61eb4c4048af45`
- observed main SHA at start: `7059381fd34aee20f159a477ba61eb4c4048af45`
- branch created from the exact bound base SHA;
- `CONTINUITY_CHECK: PASS`;
- required `TASK_STARTED` signal was persisted on Issue #96 before substantive work.

## Outcome

Built a minimal manual-only, credential-safe execution/export/import path around the integrated W004-T004 provider harness. This attempt **did not execute a real provider call** and did not require or inspect a provider credential.

The path lets a future authorized operator trigger one GitHub Actions `workflow_dispatch`, pass provider/model/version/endpoint/input provenance, use `PROVIDER_API_KEY` only through the GitHub secret environment, persist the existing T004 probe output, export a versioned downstream contract, and validate/import it for W004-T007 mechanics comparison.

Preparation is not evidence of provider quality and creates no provider/model preference.

## Delivered artifacts

### Manual workflow

`.github/workflows/provider-manual-evidence.yml`

- trigger surface is `workflow_dispatch` only: no push, pull-request, schedule, or automatic provider call;
- secret is read as `secrets.PROVIDER_API_KEY` and passed only as the environment variable consumed by the T004 harness;
- no secret is a CLI argument, artifact field, step-summary value, or committed file;
- workflow persists probe/export/import artifacts even when the run is blocker-only;
- workflow summary distinguishes operational workflow completion from observed provider evidence;
- content quality remains explicitly outside this workflow.

### T004 wrapper

`experiments/provider_manual/run_manual_provider.py`

- dynamically loads the integrated `experiments/provider_w004/run_provider_probe.py`;
- does not reimplement provider transport;
- requires exact model-version provenance;
- writes the raw T004 `provider-probe-v1` artifact plus the new versioned export;
- preserves `BLOCKED_NO_CREDENTIAL` as a valid non-observed outcome instead of converting it to provider success.

### Versioned export / downstream contract

`experiments/provider_manual/provider_evidence.py`

`docs/provider_manual/provider-observed-evidence-v1.schema.json`

Introduced `provider-observed-evidence-v1` with:

- provider / protocol / exact model / model-version / endpoint provenance;
- observed-run flag and original provider status;
- latency, provider-reported usage, cost and pricing provenance;
- response SHA-256/byte fingerprint only;
- `content_quality_evidence.included: false`;
- `mechanics_eligible_for_t007` separated from `full_provider_comparison_eligible`, which is always false here.

A mechanics artifact is eligible for strict T007 import only when the provider run was actually observed, required provenance is complete, latency and provider-reported usage are observed, pricing is eligible official-provider pricing with source/retrieval/effective/currency provenance, cost is derived from that observed usage + pricing, and a response fingerprint exists.

### Importer / rejection semantics

`experiments/provider_manual/import_provider_evidence.py`

Strict mode exits non-zero for blocker-only or incomplete evidence, including:

- missing/non-observed provider run;
- missing model/version/endpoint provenance;
- unobserved latency or token usage;
- synthetic/ineligible pricing;
- ineligible cost derivation;
- missing response fingerprint;
- embedded content-quality claims;
- secret-like fields.

`--audit-only` permits retention of such artifacts while keeping `mechanics_comparable: false`. This makes auditability distinct from comparability.

### Operator documentation

`docs/provider_manual/README.md`

Documents protected-environment/secret setup, one controlled manual action, artifact interpretation, no-secret semantics, strict import behavior, and the requirement that W004-T007 use a predeclared comparable input set plus separate human/source content-quality evidence.

`experiments/provider_manual/inputs/smoke_prompt.txt` is explicitly mechanics-only.

## Safe no-secret demonstration

Focused isolated validation exercised the new wrapper with the credential environment variable absent and an invalid example endpoint. The T004 interface returned before any network transport and the exported evidence remained blocker-only:

```text
status: BLOCKED_NO_CREDENTIAL
observed_run: false
mechanics_eligible_for_t007: false
```

The audit importer retained the artifact with `mechanics_comparable: false` and explicit findings; no fake latency, usage, cost, response fingerprint, or observed provider status was produced.

## Validation

Focused worker validation:

```text
python -m unittest discover -s tests/provider_manual -v
Ran 5 tests in 0.002s
OK
```

Validated invariants:

- observed run + complete provenance + official pricing + observed usage/cost is mechanics-eligible only;
- synthetic pricing is rejected for comparison;
- missing model version is rejected;
- secret-like fields are rejected;
- no-secret execution stays `BLOCKED_NO_CREDENTIAL`, writes audit artifacts, and remains ineligible for T007 mechanics comparison.

The versioned JSON Schema also parsed successfully during local validation.

GitHub source verification confirmed the persisted workflow contains only `workflow_dispatch`, and the branch diff against the exact base contains only task-owned paths plus this RESULT. At result time the branch head had no automatic GitHub combined-status checks/workflow runs, so this RESULT does **not** claim branch CI PASS.

## Ownership / diff check

Before RESULT persistence, compare `7059381fd34aee20f159a477ba61eb4c4048af45...1a4a6f828504771cd1ce5840323500d24ba7b305` reported:

- ahead by 8;
- behind by 0;
- only `.github/workflows/provider-manual-*.yml`, `experiments/provider_manual/**`, `tests/provider_manual/**`, and `docs/provider_manual/**` changed.

No canonical coordination file was modified.

## Acceptance status

- manual invocation only / no automatic real provider call: **PASS**
- secrets never committed/echoed/persisted by the new path: **PASS by design + validation guard**
- missing credential => explicit blocker, never fake provider success: **PASS**
- provider/model/version/endpoint provenance required: **PASS**
- cost only from observed usage + eligible official pricing provenance: **PASS**
- content-quality evidence separated from mechanics/telemetry: **PASS**
- versioned artifact/import contract for downstream T007: **PASS**
- importer rejects incomplete/synthetic evidence as comparable provider mechanics: **PASS**
- focused tests/docs: **PASS**
- lifecycle signals: **TASK_STARTED PASS; TASK_COMPLETE follows this RESULT**

## Evidence posture / non-claims

- no authorized provider credential was used in this attempt;
- no real provider endpoint was contacted by this task execution;
- provider latency, usage, real cost and model quality remain `PRODUCTION_UNKNOWN`;
- the workflow/harness path itself is not provider/model evidence;
- `full_provider_comparison_eligible` remains false until W004-T007 combines valid observed mechanics with independent human/source quality evidence;
- no provider preference is inferred.

## Artifacts

- `.github/workflows/provider-manual-evidence.yml`
- `experiments/provider_manual/run_manual_provider.py`
- `experiments/provider_manual/provider_evidence.py`
- `experiments/provider_manual/import_provider_evidence.py`
- `experiments/provider_manual/inputs/smoke_prompt.txt`
- `tests/provider_manual/test_provider_manual.py`
- `docs/provider_manual/README.md`
- `docs/provider_manual/provider-observed-evidence-v1.schema.json`

## Next actions

1. Orchestrator may review/accept this worker result without treating it as observed provider evidence.
2. When an authorized credential exists, manually run the workflow with the exact provider/model/version candidate, a predeclared comparable input set, and eligible official pricing provenance if cost is required.
3. Strict-import only observed mechanics that pass the validator.
4. W004-T007 must join those mechanics with valid human/source content-quality evidence before any provider comparison or preference.
