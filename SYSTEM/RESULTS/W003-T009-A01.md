# RESULT — W003-T009-A01 — end-to-end mechanics proof + W003 readiness synthesis

`TASK_ID: W003-T009`  
`ATTEMPT_ID: A01`  
`ROLE: Synthesizer/Auditor`  
`STATUS: COMPLETE — READY_FOR_ORCHESTRATOR_REVIEW`  
`CONFIDENCE: HIGH (0.91) for mechanics/invariant synthesis; NO CLAIM for production provider quality, calibrated audience thresholds, semantic-backend superiority, or release readiness`  
`PROTOCOL_VERSION: 1.6.0`  
`BASE_STATE_VERSION: 0019`  
`BASE_COMMIT_SHA: 8e84982a8ecd492a925d25ae1e37bedcfe0f889c`  
`OBSERVED_MAIN_SHA_AT_START: 8e84982a8ecd492a925d25ae1e37bedcfe0f889c`  
`OBSERVED_MAIN_SHA_BEFORE_RESULT: 8e84982a8ecd492a925d25ae1e37bedcfe0f889c`  
`ISSUE: #67`  
`PR: #80`  
`WORKER_BRANCH: worker/W003-T009-A01`

## 1. CONTINUITY_CHECK

`PASS`.

- Issue #67 binds `BASE_STATE_VERSION: 0019` to exact SHA `8e84982a8ecd492a925d25ae1e37bedcfe0f889c`.
- `SYSTEM/STATE.md` at the bound base declares `PROTOCOL_VERSION: 1.6.0`, W003 active and W003-T009 READY after W003-T001..T008 integration.
- observed `main` matched the bound SHA at task start and again immediately before RESULT persistence.
- isolated branch `worker/W003-T009-A01` was created from the exact base before substantive work.
- mandatory `TASK_STARTED` and one material `TASK_PROGRESS` were persisted on Issue #67.
- no canonical `STATE`, ledger, wave manifest, scorecard, traceability matrix or risk register was modified.

## 2. Objective executed

Executed the final W003 synthesis as two distinct evidence products:

1. an executable, deterministic **mechanics-only** proof that composes the integrated orchestration/RunStore/grounding/audience/repair/telemetry boundaries through source → 9 jobs → evaluation → branch-local repair → join → aggregate; and
2. a readiness synthesis that classifies every important W003 claim by evidence strength rather than averaging implementation success, diagnostics and unknown production behavior into a single green status.

The deterministic stub in this task is deliberately labeled `provider_mode=deterministic_stub` and `evidence_scope=MECHANICS_ONLY`. It is not evidence of LLM/provider content quality, provider latency, provider usage/cost, calibrated audience discrimination or production repair convergence.

## 3. End-to-end proof harness

Artifacts:

- `experiments/w003_e2e/run_proof.py`;
- `experiments/w003_e2e/README.md`;
- `tests/integration/w003/test_e2e_proof.py`.

### 3.1 Source → exact 3×3 execution

The proof planner materializes exactly nine jobs:

```text
BEGINNER      × ARTICLE | CAROUSEL | SHORT_VIDEO
INTERMEDIATE  × ARTICLE | CAROUSEL | SHORT_VIDEO
ADVANCED      × ARTICLE | CAROUSEL | SHORT_VIDEO
```

The jobs execute through the integrated `AsyncGraphOrchestrator` and persistent `SQLiteRunStore`; the harness does not introduce a second orchestration engine.

### 3.2 Controlled factual/audience failure and targeted repair

`beginner:carousel` starts with a deliberately source-inconsistent value (`20.0` vs controlled canonical `18.7`) and incomplete audience treatment (`"Receita foi R$ 20 bi. EBITDA."`).

Evaluation composes:

- canonical source/factual/policy hard-gate semantics through `hybrid_decision(...)`;
- the integrated audience complexity vector / anti-gaming diagnostics;
- the integrated T006 `build_repair_request(...)` failure-code mapping.

The branch therefore routes to targeted repair with factual/concept/jargon diagnostics rather than generic self-reflection. The repair corrects only that branch, hashes accepted siblings before/after and requires fresh SOURCE / DETERMINISTIC_FACTUAL / POLICY run IDs on re-evaluation.

The proof asserts:

- exactly one quality repair on `beginner:carousel`;
- all three hard-gate run IDs change after repair;
- accepted sibling hashes remain unchanged;
- before/after output hashes and resolved/introduced failure codes are retained in repair lineage;
- the repaired branch reaches PASS only after hard-gate/audience re-evaluation.

### 3.3 Independent controlled transport retry

`advanced:short_video` raises one controlled `TimeoutError` on its first generate attempt and succeeds on retry.

This produces one transport retry while `beginner:carousel` produces one quality repair. The two mechanisms remain distinct in orchestration state and telemetry.

### 3.4 Persistent pause/reopen/resume

The run pauses after all nine branches settle in `branches_complete`. A new `SQLiteRunStore` and a new `AsyncGraphOrchestrator` instance reopen the same persisted database and resume through lossless join, aggregation and `complete`.

The proof records RunStore snapshot history and asserts accepted branches are not regenerated after reopen/resume.

### 3.5 Hard-gate non-compensation probe

A separate probe feeds audience-clean content with complete required-concept recall but a deliberately wrong factual value.

Expected and asserted result:

```text
DETERMINISTIC_FACTUAL = FAIL
soft/audience signal clean
terminal evaluation != PASS
```

This makes the W003 cross-layer invariant explicit: audience/readability/semantic-soft evidence cannot mask source/factual/policy hard failure.

### 3.6 Telemetry evidence

The proof wraps plan/generate/evaluate/repair/aggregate through the integrated T007 telemetry hooks and persists task evidence files when run with `--output-dir`:

- `proof_report_v001.json`;
- `runstore_history_v001.json`;
- `telemetry_events_v001.jsonl`.

The controlled proof expects:

```text
transport_retries.total = 1
quality_repairs.total = 1
usage = null
observed_cost = null
terminal.run_phase = complete
```

`usage=null` and `observed_cost=null` are intentional. No synthetic `demo-pricing.v1` value is promoted into provider-cost evidence.

## 4. Verification evidence and exact scope

### 4.1 Focused proof-contract execution

A focused worker-side execution of the proof contract completed successfully:

```text
PYTHONPATH=src python experiments/w003_e2e/run_proof.py --output-dir /tmp/w003-local-proof
PASS — final complete; 9/9 joined; one repair; one transport retry; RunStore reopen/resume

PYTHONPATH=src python -m unittest discover -s tests/integration/w003 -v
1 test: PASS
```

The worker shell did not have a network-resolvable GitHub checkout. The focused execution therefore validates the task proof control flow against a local boundary mirror and is **not** claimed as an independent clean-checkout execution of every imported repository file.

### 4.2 Clean-checkout repository CI

PR #80 executed the repository's existing clean GitHub-hosted checks on implementation head `3c3aca37adc4167c8c3787794812859d218469f7`:

- **System Integrity**, run `35227610542`: `completed / success`;
- **Foundation Regression**, run `35227610475`: `completed / success`.

The Foundation gate performs clean checkout, pinned runtime dependency installation, whole-tree `compileall`, critical foundation suites, factual oracle, parser contract assertions and the orchestration benchmark.

Important scope boundary: the current Foundation workflow does **not** yet include `tests/integration/w003` in its explicit pytest target list. It compiles the task files and validates the integrated foundation, but the new W003-specific proof test itself should be added to a later release-proof/CI fan-in before any production-release claim.

This limitation is carried forward rather than hidden.

## 5. W003 evidence reconciliation — T001 through T008

| Task | Durable result | Current evidence classification |
|---|---|---|
| W003-T001 | independent benchmark schema, source-level dev/held-out isolation, blind human-gold workflow | benchmark mechanics **PROVEN**; labels/agreement/thresholds **DIAGNOSTIC_ONLY / PENDING HUMAN EVIDENCE** |
| W003-T002 | claim-level grounding, typed uncertainty, non-compensatory `HybridDecision`, semantic-ablation interface | hard-gate/grounding contract **PROVEN**; production semantic recall/precision **PRODUCTION_UNKNOWN** |
| W003-T003 | explicit graph/state orchestration, exact 9-way join, persistent SQLite RunStore, independent retry/repair | mechanics **PROVEN**; plain async remains provisional executed leader; LangGraph `PENDING_RUNTIME_RECHECK` |
| W003-T004 | PT-BR readability, finance ontology, multidimensional audience vector, anti-gaming diagnostics | deterministic feature/anti-gaming contract **PROVEN**; numeric audience thresholds **DIAGNOSTIC_ONLY** |
| W003-T005 | clean-checkout combined foundation runner and separate application CI | clean integrated foundation **PROVEN** |
| W003-T006 | diagnostic targeted repair, sibling immutability, fresh hard-gate runs, before/after lineage | repair mechanics/invariants **PROVEN**; real-model convergence/quality **PRODUCTION_UNKNOWN** |
| W003-T007 | versioned run/job/attempt telemetry, latency/retry/repair lineage, guarded usage/cost | telemetry mechanics **PROVEN**; real provider usage/cost **PRODUCTION_UNKNOWN** |
| W003-T008 | calibration/release gate with anti-circularity, anti-gaming and evidence guards | overall `DIAGNOSTIC_ONLY`; audience matrices `NOT_COMPUTABLE`; semantic ablation `NOT_RUN`; provider comparison `NOT_COMPARABLE` |

## 6. Final evidence classification

### PROVEN within W003 scope

- canonical/foundation package coherence from clean checkout;
- source → exact 9 jobs → branch evaluation → targeted repair → 9/9 lossless join → aggregate mechanics;
- persistent RunStore checkpoint/history/reopen/resume behavior;
- source/factual/policy hard-gate non-compensation;
- branch-local repair with fresh gate execution identity and sibling immutability;
- separate transport-retry vs quality-repair semantics;
- versioned telemetry schema and failure/review visibility;
- deterministic audience-feature and mandatory anti-gaming contract;
- development/held-out anti-circularity structure.

### DIAGNOSTIC_ONLY

- audience-complexity feature interpretation as a predictor of human audience level;
- any numeric audience floor/threshold;
- the current 3-source benchmark as a generalization claim;
- deterministic stub outputs as an example of content behavior.

### NOT_COMPUTABLE / NOT_RUN / NOT_COMPARABLE

- observed target→human and human→evaluator confusion matrices: `NOT_COMPUTABLE` because independent human gold is absent;
- final numeric threshold freeze: not authorized;
- semantic-on/off development-gold ablation: `NOT_RUN`, therefore `NO_BACKEND_PREFERENCE`;
- provider/model quality-latency-cost comparison: `NOT_COMPARABLE`, therefore `NO_PREFERENCE`.

### PRODUCTION_UNKNOWN

- real provider/model content quality and stability;
- real provider latency, usage and versioned commercial cost;
- semantic backend recall/precision/value-add on representative gold;
- targeted-repair convergence and cost under real generation;
- parser/source-generalization beyond current controlled fixture families;
- end-to-end release operations under representative failures/load;
- final production telemetry storage/retention/redaction design.

## 7. W003 readiness decision proposal

**Proposal to Orchestrator:** treat W003 as having completed its implementation/mechanics objective and advance the critical path to W004 evidence acquisition + cockpit/release proof, while explicitly **not** marking the system production-ready.

A useful canonical posture would be equivalent to:

```text
W003_MECHANICS: PROVEN
W003_CALIBRATION: DIAGNOSTIC_ONLY
PRODUCTION_RELEASE_READINESS: NOT_CLAIMED
NEXT_BOTTLENECK: REPRESENTATIVE_HUMAN_PROVIDER_PARSER_EVIDENCE
```

The major architecture/control-loop questions are now testable and reconstructible. More orchestration abstraction is not the highest-value next investment; representative evidence is.

## 8. Recommended W004 decomposition and safe parallelism

### W004-A — B13 evidence cockpit / evidence graph — start immediately

Build a cockpit that ingests the already-versioned RESULT/RunStore/telemetry/calibration artifacts and displays evidence state per requirement/risk as `PROVEN`, `DIAGNOSTIC_ONLY`, `NOT_COMPUTABLE`, `NOT_RUN`, `NOT_COMPARABLE`, `PRODUCTION_UNKNOWN`, `FAIL` or `REVIEW_REQUIRED`.

The cockpit must preserve provenance and unknowns; it must not collapse them into an overall green score.

### W004-B — representative corpus + independent human calibration — critical path, start immediately

Expand beyond the three-source bootstrap, freeze natural outputs by source/audience/format, execute blinded double annotation + adjudication, measure agreement and create development human gold without touching held-out tuning evidence.

This is the dominant blocker for audience confusion matrices, threshold freeze and evaluator calibration.

### W004-C — real provider/model execution + cost/latency evidence — start in parallel

Run candidate generation providers/models on the frozen development corpus with real observed usage, stage latency and separately governed versioned pricing. Compare content quality only against source/human evidence; keep provider/model choice open until the data are actually comparable.

### W004-D — parser/source generalization expansion — start in parallel

Expand parser/source-trust experiments to more document families and real structural failure modes. Preserve the current hard findings for table-role loss and silent wrong-role corruption instead of weakening them for coverage.

### W004-E — semantic backend ablation — prepare now, execute after development gold exists

Candidate adapters can be wired in parallel, but the actual semantic-off/on decision experiment should run on the same independent development gold. Promote no backend without measurable incremental value while hard-gate codes remain invariant.

### W004-F — B14 release proof / task-specific clean E2E CI — downstream fan-in

After W004-B/C/D and any accepted E evidence, add the W003-specific integration proof to a clean-checkout release gate, execute representative source→9→eval→repair→aggregate runs, audit terminal failures/reviews and publish a release evidence packet.

B14 should consume cockpit evidence rather than inventing a separate readiness score.

### W004-G — LangGraph unchanged-challenger recheck — optional parallel, non-blocking

Re-run the unchanged challenger only if dependency/runtime availability makes the test cheap. Plain async remains the provisional evidence leader. Do not let framework experimentation block human/provider/parser evidence acquisition.

## 9. Kill criteria / anti-overengineering rules for W004

Stop or reject a W004 path if it requires any of the following without new measured evidence:

- replacing the working orchestration runtime merely to introduce another framework;
- freezing audience thresholds without independent human gold + agreement/adjudication evidence;
- using generation target as human truth or using held-out evidence during tuning;
- selecting a semantic backend/provider/model from preference, demos or incomparable runs;
- presenting synthetic pricing as real provider cost;
- allowing audience/semantic/readability improvements to compensate source/factual/policy hard failure;
- treating retrieval/extraction uncertainty as a prose-rewrite problem;
- building a cockpit that hides `N/A`, unknown, review-required or failed evidence behind an aggregate score;
- declaring B14/release readiness while the task-specific E2E proof is absent from clean-checkout release CI;
- adding infrastructure whose measurable value cannot be tied to an active success bottleneck.

## 10. Findings

### F-001 — the architecture bottleneck has shifted from mechanics to evidence acquisition

W003 now has versioned contracts for graph/state, grounding, audience features, repair and telemetry, plus clean foundation CI. The remaining high-value uncertainties require representative external observations rather than another control-loop abstraction.

### F-002 — anti-gaming evidence is stronger than calibration evidence

The mandatory deterministic anti-gaming cases execute and are release-gated, but there are still no independent human audience labels/agreement statistics. Those evidence classes must remain separate.

### F-003 — provider-neutral observability is ready before provider selection is

W003 can capture attempt lineage, latency and guarded usage/cost without selecting a provider. This makes W004 provider experiments possible without architecture lock-in.

### F-004 — deterministic E2E demos are useful only when their evidence class is explicit

The controlled stub can prove fan-out, retry, repair, resume and audit lineage. It cannot prove arbitrary LLM quality, economic efficiency or production convergence. The proof therefore persists mechanics/production-unknown labels in the report itself.

### F-005 — the next release gate should include this W003-specific test explicitly

Current Foundation CI is clean and valuable, but its selected pytest list predates `tests/integration/w003`. A production-release claim should require the W003 proof in a clean-checkout release gate rather than relying only on compile coverage plus local task execution.

## 11. SUCCESS_IMPACT

This task closes the active `W003_END_TO_END_MECHANICS_AND_NEXT_WAVE_DECISION` bottleneck at the **mechanics/synthesis** level:

- integrates source→9-job→evaluation→targeted-repair→aggregate into one auditable proof contract;
- demonstrates the intended RunStore/retry/repair/telemetry lineage and non-compensatory hard-gate behavior;
- reconciles T001..T008 without elevating diagnostic/demo evidence into production evidence;
- identifies independent human gold + real provider/model + parser-generalization evidence as the next total-success bottleneck;
- gives W004 a dependency-aware decomposition that allows cockpit, corpus/human, provider and parser work to run safely in parallel before downstream release fan-in.

## 12. TRACEABILITY_UPDATES proposed to Orchestrator

No canonical traceability/risk/state file was edited by this worker.

Suggested fan-in actions:

- link W003-T009 proof artifacts to the already-established execution/auditability requirements for exact 3×3 orchestration, durable RunStore, targeted repair, telemetry and hard-gate precedence;
- preserve W003-T001/T004/T008 audience requirement states as diagnostic/pending human evidence rather than PASS;
- preserve `REQ-019` observed confusion-matrix evidence as pending / `NOT_COMPUTABLE` even though metric computation capability exists;
- preserve semantic backend and provider/model decisions as OPEN / no preference;
- carry the task-specific clean-E2E-CI gap into B14 release-proof acceptance;
- move the wave bottleneck to representative human/provider/parser evidence plus B13/B14 fan-in.

## 13. Assumptions / risks affected

Reduced at the mechanics layer:

- state loss across restart;
- silent 9-way branch overwrite/loss;
- retry vs repair ambiguity;
- stale hard-gate reuse after repair;
- accepted-sibling mutation;
- silent hard-gate compensation;
- absent/unknown cost being represented as zero.

Remain materially open:

- tiny current gold corpus and no independent human labels/agreement;
- provider/model quality, latency and cost under real executions;
- semantic backend incremental value;
- parser generalization to broader source families;
- real-model targeted-repair convergence;
- task-specific E2E test inclusion in clean release CI;
- operational cockpit/release evidence packaging.

No risk is claimed closed solely because the controlled E2E demo reaches `complete`.

## 14. Artifacts

Worker-owned implementation/proof:

- `experiments/w003_e2e/run_proof.py`;
- `experiments/w003_e2e/README.md`;
- `tests/integration/w003/test_e2e_proof.py`.

Required RESULT:

- `SYSTEM/RESULTS/W003-T009-A01.md`.

Coordination/evidence:

- Issue #67 lifecycle signals;
- PR #80;
- clean CI implementation head `3c3aca37adc4167c8c3787794812859d218469f7`;
- System Integrity run `35227610542` — success;
- Foundation Regression run `35227610475` — success.

## 15. Next actions

1. Orchestrator validates branch/result provenance and integrates W003-T009 if accepted.
2. Orchestrator updates canonical state/traceability/risk artifacts; this worker does not.
3. Dispatch W004-A/B/C/D in parallel; prepare E but gate its decision on development human gold.
4. Fan accepted evidence into W004-F / B14 only after representative human/provider/parser evidence exists and the W003-specific E2E test executes in clean release CI.
5. Keep LangGraph recheck non-blocking and evidence-driven; do not substitute framework work for the next-wave evidence bottleneck.
