# RESULT W006-T006-A01

`TASK_ID: W006-T006`  
`ATTEMPT_ID: A01`  
`STATUS: VALIDATION_PENDING`  
`BASE_STATE_VERSION: 0047`  
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`  
`OBSERVED_MAIN_SHA: 0c6dc39d4b5b0bf4ce469fcad98686c823df3189`  
`CONTINUITY_CHECK: PASS`  
`WORKER_BRANCH: worker/W006-T006-A01`

## Outcome

Implemented a framework-neutral authenticated live-cockpit bakeoff over the accepted W006-T001-A02 production contract. Two rendering candidates (`server_hypermedia_shell`, `client_event_shell`) share one fail-closed durable projector, canonical command builder and snapshot/replay semantics. No frontend framework winner is selected.

## Hard-acceptance mapping

- Same production API/event contract: canonical v1 `CommandEnvelope`, `EventEnvelope` and `AuthorizedStream` fields are consumed/built by `src/suno_content/cockpit/live.py`.
- Durable state only: candidate view state comes only from an authoritative snapshot plus accepted canonical events; reconnect rebuilds snapshot first and replays newer events.
- Static W004 route unavailable: no import/fallback to `app/recipient`; missing authoritative source fails closed.
- Cross-tenant source/event/trace projection success target `0`: adversarial source, trace, outer event and nested payload vectors are covered by `tests/cockpit/test_live_cockpit.py`.
- Failed/review-required cells explicit: `FAIL` and `REVIEW_REQUIRED` remain textual first-class cell states in both candidate renderers.
- Accessibility/dynamic-update evidence: labeled upload/workspace, semantic headings/cell labels, explicit status text and `role="status" aria-live="polite"` are asserted in the candidate test.
- Replay cursor is not authority: cursor is absent from projector authorization and view model; the server-authorized stream supplies tenant/run scope.

## Owned evidence

- `src/suno_content/cockpit/live.py`
- `tests/cockpit/test_live_cockpit.py`
- `docs/production/frontend/W006-T006-A01-bakeoff.md`
- `artifacts/w006_t006_a01/frontend_bakeoff_evidence.json`

## Validation

Repository checkout is unavailable inside the connector worker runtime, so executable validation is delegated to the repository pull-request CI. Required checks:

```text
python -m unittest tests.cockpit.test_live_cockpit
python docs/production/contracts/v1/validate_contracts.py --base-ref main
```

CI result and PR reference will be appended before the terminal task signal.

## Decision posture

`EVIDENCE_GATED_NO_WINNER`. This task proves shared state/security/replay mechanics for both shells and deliberately does not freeze a concrete frontend framework, package manager, runtime or deployment class.
