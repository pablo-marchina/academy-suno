# RESULT — W006-T007-A01

`TASK_ID: W006-T007`  
`ATTEMPT_ID: A01`  
`ROLE: Synthesizer + Production Architect + Evidence & Consistency Auditor`  
`BASE_STATE_VERSION: 0047`  
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`  
`OBSERVED_STATE_VERSION_AT_START: 0052`  
`OBSERVED_MAIN_SHA_AT_START: 52dad30166a79cb3613cb3740a5e78f8af70d7de`  
`OBSERVED_MAIN_SHA_BEFORE_RESULT: 52dad30166a79cb3613cb3740a5e78f8af70d7de`  
`WORKER_BRANCH: worker/W006-T007-A01`  
`ISSUE: #194`  
`STATUS: COMPLETE_PENDING_PR_CI`  
`CONTINUITY_CHECK: PASS`  
`CONFIDENCE: HIGH on evidence-boundary reconciliation and carried contract locks; MEDIUM/LOW on production technology selections intentionally left open`  
`PRODUCTION_READY_CLAIM: NOT_AUTHORIZED`

## 1. Continuity / provenance

The dispatch preserves original provenance at STATE 0047 / `0fa1fd46d02d8fb2ad3410823ba417d83b596eac` while requiring a fresh continuity review against the current canonical state.

At execution start:

- protocol version was `1.8.0`;
- canonical STATE was `0052`;
- observed `main` was `52dad30166a79cb3613cb3740a5e78f8af70d7de`;
- STATE 0052 explicitly accepted/integrated W006-T002 through T006 and declared W006-T007-A01 READY;
- branch `worker/W006-T007-A01` was created from observed main;
- protocol-valid `TASK_STARTED` was persisted on Issue #194 before substantive work;
- main was rechecked immediately before RESULT persistence and remained unchanged.

Therefore `CONTINUITY_CHECK: PASS`.

No canonical coordination file (`SYSTEM/STATE.md`, `TASK_LEDGER`, W006 wave manifest, canonical decision/scorecard state) was modified.

## 2. Objective outcome

Completed the Phase 9 production substrate evidence fan-in and persisted the primary decision artifact:

- `docs/production/W006_T007_PRODUCTION_SUBSTRATE_DECISION_FANIN.md`.

The fan-in applies the accepted Decision Research Gate and W005 benchmark v002 ordering:

1. non-compensatory hard-gate eligibility;
2. raw multidimensional evidence and explicit uncertainty;
3. Pareto only when the comparison is valid and representative;
4. production/default promotion only with exact research evidence, workload evidence, material-risk controls, confidence and reversal conditions;
5. otherwise retain `NO_PREFERENCE`, `PENDING_EVIDENCE`, `DIAGNOSTIC_ONLY`, or equivalent.

No scalar utility, invented business weights, invented practical-effect thresholds, invented SLO/capacity targets, or technology preference by familiarity was used.

## 3. Accepted evidence consumed

### W006-T002-A01 — security/identity/session/SSE substrate

Accepted implementation evidence proves the defined portable application-level security invariants, including zero observed defined cross-tenant unauthorized successes across API/data/object/event/trace surfaces, zero revoked-session/membership resume successes, zero prior-tenant access after org switch, zero cross-tenant cursor replay successes, and zero credential-canary leaks.

T007 carries the fail-closed tenant/authz/replay invariants while preserving IdP, durable session store, data engine, object/scanner, event backend, proxy and cloud IAM choices as evidence-gated.

### W006-T003-A01 — durable state/event consistency

Accepted T003 reference evidence supplies the reusable logical correctness floor and common failure harness:

- `14/14 PASS`;
- silent stale same-run overwrite accepted: `0`;
- permanent logical event gap after reconciliation: `0`;
- uncommitted event accepted/projected: `0`;
- duplicate logical projection: `0`;
- cross-tenant replay success: `0`;
- defined restart/replay/repair and backup/restore: `100% PASS`.

The SQLite implementation remains reference infrastructure only and is not promoted as a production database.

### W006-T004-A01 — secure ingestion/parser bakeoff

Accepted T004 evidence implements the fail-closed controlled PDF quarantine/provenance boundary and reports:

- source-group-aware raw evidence;
- encrypted/malformed preflight quarantine;
- explicit OCR/review routing;
- no ambiguous table/OCR role promoted as trusted evidence;
- `eligible_candidates = []`;
- `pareto_frontier = []`;
- `decision_state = NO_PRODUCTION_PARSER_WINNER`.

T007 preserves that state exactly.

### W006-T005-A02 — runtime/shared-state bakeoff

Accepted A02 executes custom/CAS, LangGraph 1.2.12 and DBOS 2.31.0 on the exact 3×3 workload plus the shared T003 correctness floor. All three pass defined hard gates. The local point Pareto set for the declared task-local objectives is `[custom_cas]`.

T007 explicitly **does not** promote that local singleton because:

- the common acceptance topology is local SQLite-backed reference infrastructure;
- intended multi-replica/shared-state production topology is absent;
- no production database/control-plane comparison exists;
- failover/network/replica coordination, saturation/backpressure, operational burden and production security/isolation are not representative;
- external billed provider cost is not observed.

Therefore:

`PRODUCTION_RUNTIME_LOCK: NONE`  
`PRODUCTION_DATABASE_LOCK: NONE`  
`DECISION_STATE: PENDING_EVIDENCE`

### W006-T006-A01 — live cockpit same-contract bakeoff

Accepted T006 validates framework-neutral authoritative snapshot/event projection, replay/reconnect, cross-tenant projection denial, explicit FAIL/REVIEW_REQUIRED states and accessibility semantics for two shell shapes.

T007 carries the shared contract mechanics while preserving concrete frontend framework, rich editor, package manager, UI runtime and deployment class as evidence-gated.

## 4. Decision Research binding

Exact research authority used in the fan-in:

- `dr://DR-0001` — `docs/decisions/research/DR-0001-evidence-cockpit-frontend-api-live-events.md`; only exact locked subclaims bind:
  - D1 typed/versioned HTTP/OpenAPI boundary;
  - D2 SSE default browser run feed with durable replay/snapshot semantics;
  - D3 server-side authorization/redaction;
  - D4 stable opaque source/citation references;
  - frontend/editor choices remain pending.
- `dr://DR-0014` — `docs/decisions/research/DR-0014-financial-document-parsing.md`; no parser/OCR winner.
- `dr://DR-0701` — observability portable semantic/export lock only; backend/sampling/retention remain open.
- `dr://DR-5008` — deployment/runtime topology has no overall preference.
- `dr://DR-5009` — reliability/capacity/recovery validation method may bind; numeric targets remain open.
- `dr://DR-5401` — identity/authz/tenancy research supports security invariants while vendor selection remains open.
- `dr://DR-5402` — data substrate remains evidence-gated.
- `dr://DR-5403` — object-store/scanner choice remains evidence-gated.
- `dr://DR-5909` — accepted W005 benchmark methodology v002 decision surface.
- `dr://DR-5910` — orchestration/durability research remains `PENDING_EVIDENCE` for production runtime.
- `dr://DR-5914` — developer-platform control locks are narrow; package managers/repository topology remain evidence-gated.
- `docs/decisions/research/DR-6005-runtime-shared-state-bakeoff-a02.md` — exact post-registry-snapshot T005-A02 research record; `PENDING_EVIDENCE`, runtime lock NONE, database lock NONE, with explicit production-topology recheck conditions.

The v1 Decision Research registry is used fail-closed: mixed records expose only their exact locked subclaims; pending/non-preference records are not converted into binding winners.

## 5. Promotion disposition

### Carried/promoted contract and correctness authority

T007 carries forward the following evidence-backed invariants/contracts:

- typed/versioned HTTP/OpenAPI resource/command boundary;
- SSE as default one-way browser run feed, conditional on deployment/security qualification;
- server-side authorization/redaction before serialization;
- replay cursor position is not authorization or stream-selection authority;
- stable tenant/provenance/source/citation identity;
- application-owned deny-by-default tenant authorization;
- durable state↔event logical consistency;
- CAS/ownership/stale-write rejection semantics;
- at-least-once physical execution/delivery with idempotent authoritative acceptance/projection;
- deterministic reconciliation/replay independent of sampled telemetry;
- fail-closed controlled document ingestion/quarantine/provenance;
- exact stable 3×3 branch identity, lossless join and retry≠repair semantics;
- authoritative live cockpit snapshot/event projection and reconnect mechanics;
- static W004 cockpit remains `DIAGNOSTIC_ONLY / COUNTERFACTUAL`, never production/final-evidence fallback.

These are constraints on downstream implementations. They are not vendor/framework winners.

### Explicitly not promoted

- production workflow/runtime: `PENDING_EVIDENCE`;
- production database/shared-state engine: `PENDING_EVIDENCE`;
- production parser/OCR: `NO_PRODUCTION_PARSER_WINNER`;
- frontend framework/editor: `PENDING_EVIDENCE`;
- identity provider: `NO_PREFERENCE / PENDING_EVIDENCE`;
- data isolation/RLS-specific engine: `PENDING_EVIDENCE`;
- object store/scanner/IAM implementation: `PENDING_EVIDENCE`;
- observability backend/collector topology/sampling/retention: `NO_PREFERENCE / PENDING_EVIDENCE`;
- deployment/cloud/runtime class: `NO_PREFERENCE`;
- package managers/task graph/repository topology: `PENDING_EVIDENCE`;
- SLO/capacity/RTO/RPO numeric targets: `UNKNOWN / PENDING_EVIDENCE`;
- scalar/business utility weights: `PENDING_EVIDENCE`.

## 6. W005-T011/T012 correction preservation

All material red-team/final-fan-in corrections remain intact:

1. repository topology is not locked; only “no migration without evidence” is invariant;
2. state↔event crash consistency is a hard correctness contract;
3. static W004 cockpit is diagnostic/counterfactual only;
4. SSE connect/resume must preserve auth expiry/revocation, membership revocation, org switch and cross-tenant cursor behavior;
5. exact/canonical Decision Research references are required; post-registry records use exact repo-relative path;
6. OTel lock is narrow: W3C trace context + OTel semantic/instrumentation boundary + OTLP-compatible export boundary only;
7. at-least-once remote execution is paired with idempotent local accepted-output semantics, immutable attempt identity, late/stale rejection and duplicate-side-effect accounting;
8. replay cursor cannot independently select tenant/resource/run authority.

## 7. Hard acceptance audit

- material production/default winner lacking exact DR evidence: `0`;
- hard-gate violator retained as eligible: `0`;
- scalar utility used without representative evidence: `0`;
- local T005 `[custom_cas]` Pareto point converted into production runtime lock: `0`;
- local/reference SQLite converted into production database lock: `0`;
- unsupported production parser winner: `0`;
- unsupported frontend/framework winner: `0`;
- unsupported infrastructure/deployment/observability/package-manager winner: `0`;
- W005-T011/T012 correction weakened: `0`;
- canonical coordination files modified by this worker: `0`.

## 8. Reversal / recheck conditions

The decision artifact records explicit recheck conditions. Highest-value triggers:

- runtime/database: run surviving candidates on intended production-capable multi-replica topology with the common failure harness plus representative concurrency, failover/network, recovery, saturation/backpressure, security/isolation and operational evidence; execute Temporal when a remaining hypothesis can change eligibility/Pareto membership;
- parser/OCR: use immutable source-original bytes, broader source groups, structured OCR/document-intelligence for scans and identical managed/local candidate cost/latency/failure capture;
- frontend/editor: identical real cockpit slice with build/dependency/runtime/deploy/auth/accessibility/CI measurements;
- identity/data/object infrastructure: selected enterprise requirements and identical production-adapter adversarial/operational bakeoffs;
- SSE transport: reopen if production proxy/load/auth evidence shows unacceptable scaling/buffering/security burden or a true bidirectional collaboration requirement materially changes the architecture;
- deployment/observability/package managers/repository topology: decide only after the selected real dependency graph exists and same-slice operational evidence distinguishes alternatives.

## 9. Production Contract impact

T007 is a promotion/evidence gate, not production qualification.

It advances architecture/implementation authority for portions of PROD-001/002/003/004/006/011/014/015 but does not make any Production Contract row globally PASS.

Material blockers remain for selected production persistence/runtime, parser/OCR, real vertical-slice framework/infrastructure, provider/model/adaptive runtime, reliability/capacity/recovery, deployment and final live single-product-path evidence.

`PRODUCTION_READY_FROM_T007: FALSE`.

## 10. SUCCESS_IMPACT

### Decision Quality — HIGH positive

Prevents local benchmark success from becoming an unsupported production lock while retaining every contract/invariant that has exact research and executable evidence.

### Production Engineering — HIGH positive

Creates one downstream authority surface separating portable correctness/security contracts from still-open technology choices, so T008/T009 can freeze the toolchain and vertical slice without silently inheriting local/reference implementations as production defaults.

### Evidence Rigor — HIGH positive

Binds exact Decision Research references, explicit uncertainty and reversal conditions, and preserves W005 benchmark v002 non-compensatory/Pareto behavior.

### Execution Robustness — HIGH positive

Carries the common state/event failure floor, idempotent acceptance semantics, tenant/cursor security and source-trust gates into every later substrate choice.

## 11. Artifacts

Created on `worker/W006-T007-A01`:

- `docs/production/W006_T007_PRODUCTION_SUBSTRATE_DECISION_FANIN.md`;
- `SYSTEM/RESULTS/W006-T007-A01.md`.

Lifecycle evidence:

- Issue #194 — protocol-valid `TASK_STARTED` after continuity PASS;
- Issue #194 — `TASK_PROGRESS` after decision fan-in persistence.

## 12. Verification posture

This attempt is documentation/decision fan-in only; it introduces no runtime implementation or test code. It relies on the accepted/integrated clean-checkout CI/evidence of W006-T002..T006 and on repository System Integrity for protocol/file-boundary validation of this branch.

The worker will open a PR and inspect the resulting repository checks before emitting the terminal `TASK_COMPLETE` signal. No CI result is claimed in this RESULT before that inspection.

## 13. Downstream instructions

For W006-T008/T009:

1. treat carried locks as contract/correctness/security constraints, not as permission to choose an unbenchmarked vendor;
2. reuse the T003 common failure assertions for production persistence/runtime contenders;
3. do not interpret T005 local Pareto membership as a production-topology winner;
4. do not let parser text recall/latency compensate for provenance/table-role/scanned-structure hard-gate failures;
5. keep duplicate external/provider attempts and cost separate from accepted-output idempotency;
6. keep cursor state separate from stream authorization;
7. require the final frontend to consume authoritative live state/events; static W004 remains diagnostic only;
8. require a DR record with exact evidence/confidence/reversal conditions before any new material winner is frozen;
9. keep scalar/business weighting unavailable until representative human/business evidence is frozen before outcomes and sensitivity is stable.

## 14. Completion statement

`W006-T007-A01` is complete at the worker decision/evidence-fan-in level once its RESULT-bearing PR passes the required repository integrity checks and the terminal signal is persisted.

Completion does **not** mean a production runtime, database, parser, frontend, infrastructure vendor or deployment class has been selected, and it does not authorize a production-ready claim.