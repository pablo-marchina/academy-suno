# RESULT — W006-T004-A01

`TASK_ID: W006-T004`  
`ATTEMPT_ID: A01`  
`STATUS: RESULT_READY_FOR_PR_CI`  
`CONFIDENCE: HIGH_FOR_FAIL_CLOSED_ROUTING / LOW_FOR_PRODUCTION_PARSER_SELECTION`  
`WORKER_BRANCH: worker/W006-T004-A01`  
`BASE_STATE_VERSION: 0047`  
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`  
`OBSERVED_MAIN_SHA_AT_START: 0c6dc39d4b5b0bf4ce469fcad98686c823df3189`  
`OBSERVED_STATE_VERSION_AT_START: 0050`  
`CONTINUITY_CHECK: PASS`

## Objective result

Implemented a controlled PDF upload/quarantine wrapper around the existing canonical ingest path and executed a source-group-aware local parser/OCR bakeoff covering digital, scanned, encrypted, malformed, and table-role-sensitive cases.

The benchmark uses deterministic reconstructions of role-sensitive semantic seeds from two first-party public financial sources (BCB Copom 277 and Petrobras 2T26), then derives scan/encrypted/malformed variants. The worker execution environment did not expose source-original PDF bytes to the local candidate runner, so hashes in the benchmark are hashes of the generated variants, **not** hashes of the first-party PDFs. Source-original parser-quality evidence is therefore not promoted and no production parser winner is selected.

Decision Research authority consumed: `dr://DR-0014`. The task preserves the accepted W006-T001-A02 provenance/registry boundary and introduces no evidence-gated technology lock.

## Durable outputs

### Controlled upload / quarantine

- `src/suno_content/ingest/controlled_upload.py`
- `src/suno_content/ingest/__init__.py`
- `tests/ingest/test_controlled_upload.py`

The path persists immutable SHA-256 provenance for every byte variant and a separate `source_group_id` for derived-variant accounting. It fails closed before parser execution on empty/oversized/invalid-signature/encrypted/active-content/malformed inputs; parser failures remain quarantined; empty text is routed to `OCR_REQUIRED`; table-role ambiguity or warning/confidence failures route to `REVIEW_REQUIRED`; only a result that also clears the existing canonical source gate can become `SOURCE_READY`.

### Bakeoff / evidence

- `experiments/document_parsing_w006/benchmark.py`
- `experiments/document_parsing_w006/corpus_manifest.json`
- `experiments/document_parsing_w006/raw_observations.json`
- `experiments/document_parsing_w006/decision.json`
- `experiments/document_parsing_w006/README.md`

Observed worker run:

- source groups: `2`;
- derived variants: `8` (`digital`, `scanned`, `encrypted`, `malformed` per source group);
- parser/OCR result rows: `20`;
- every raw result row has `source_group_id` + SHA-256: `20/20`;
- encrypted/malformed variants quarantined before parser: `4/4`;
- external managed-service spend observed in local run: `USD 0.0`; compute/license/ops cost intentionally not inferred.

Selected raw observations (median latency on this worker; diagnostic only):

| Source group | Variant | Candidate | Text anchors | Structured role pairs | Median ms |
|---|---|---|---:|---:|---:|
| BCB Copom 277 | digital | pypdf | 1.0000 | 0.0000 | 1.29 |
| BCB Copom 277 | digital | pdfplumber | 1.0000 | 1.0000 | 7.61 |
| BCB Copom 277 | digital | PyMuPDF tables | 1.0000 | 0.7500 | 12.97 |
| BCB Copom 277 | scanned | Tesseract text OCR | 0.2500 | 0.0000 | 502.24 |
| Petrobras 2T26 | digital | pypdf | 1.0000 | 0.0000 | 2.19 |
| Petrobras 2T26 | digital | pdfplumber | 1.0000 | 1.0000 | 9.12 |
| Petrobras 2T26 | digital | PyMuPDF tables | 1.0000 | 1.0000 | 14.18 |
| Petrobras 2T26 | scanned | Tesseract text OCR | 0.5833 | 0.0000 | 559.01 |

Text-layer success therefore does not satisfy the table-role gate, and text-only OCR does not satisfy the scanned-table gate.

## Decision method

`decision.json` applies the required order:

1. non-compensatory hard-gate eligibility;
2. explicit uncertainty review;
3. Pareto frontier only among hard-gate-eligible candidates;
4. no scalar utility / no latency-cost compensation for provenance or structural failure.

Result: `eligible_candidates = []`, `pareto_frontier = []`, `decision_state = NO_PRODUCTION_PARSER_WINNER`.

`pdfplumber` demonstrates complete role-pair recovery on both **digital reconstructions**, but it is not eligible as an overall production winner because scans recover no roles and source-original bytes were not executed. PyMuPDF is additionally incomplete on the BCB reconstruction. pypdf/pdftotext preserve digital text but zero table roles. Tesseract text OCR recovers partial scan text but zero structured roles.

## Hard acceptance mapping

- accepted evidence missing source hash/provenance: `0`;
- benchmark result missing source-group identity: `0`;
- ambiguous table/OCR role promoted as trusted evidence: `0` by routing contract and tests;
- encrypted/malformed variant passed to parser after preflight: `0` in observed harness;
- source-group metrics inflated by derived variants: `0` — N is `2` source groups, not `8` variants;
- raw latency/cost/failure observations persisted: `YES`;
- arbitrary scalar utility used for candidate selection: `NO`;
- hard-gate + uncertainty + Pareto decision persisted: `YES`;
- unsupported production parser winner introduced: `0`;
- canonical coordination files modified: `0`.

## Validation evidence

Worker-local validation before persistence:

- `controlled_upload.py` Python compilation: PASS;
- controlled-upload self-checks for signature, encryption, malformed tail, source-group stability, SHA identity, OCR routing, ambiguity routing, and trusted promotion: PASS;
- benchmark execution: PASS;
- observed environment: Python 3.13.5; pypdf 5.9.0; PyMuPDF 1.26.7; pdfplumber 0.11.9; pytesseract 0.3.13; ReportLab 4.4.9; Pillow 12.3.0; pdftotext 25.06.0; Tesseract 5.5.0.

Repository CI/System Integrity is intentionally not claimed until the PR is opened and checked.

## Evidence boundary / uncertainty

Production-selection uncertainty remains high because source-original bytes were unavailable to the local candidate runner, source-group N is only two, managed document-AI candidates were not executed, and compute/license/operations costs were not measured. These limitations are represented directly in `corpus_manifest.json` and `decision.json`; they are not converted into inferred scores.

Recheck triggers are explicit: source-original immutable bytes + SHA-256, broader source groups, structured OCR/document-intelligence on scans, and managed candidates on the identical corpus with region/model/API version + raw cost/latency.

## SUCCESS_IMPACT

Positive enabling impact: recipient PDF ingress now has a concrete fail-closed quarantine boundary and provenance-preserving source-group accounting; unsafe text/OCR fallback cannot silently become trusted table evidence. The task narrows parser eligibility without making an unsupported production technology lock.

## TRACEABILITY_UPDATES

- `PROD-004`: controlled source upload/provenance boundary implemented;
- `PROD-005`: parser/OCR candidate evidence and fail-closed source trust strengthened;
- `PROD-007`: table-role preservation explicitly measured and enforced as non-compensatory;
- `PROD-015`: raw operational observations include latency/failure/cost-observation fields;
- `RISK-0001`: malformed/encrypted/untrusted PDF path quarantined before parser;
- `RISK-0010`: source hash + source-group identity prevents silent provenance/sample accounting loss;
- `RISK-0032`: OCR/text-only role ambiguity cannot be promoted;
- `RISK-0034`: parser selection remains evidence-gated.

## State / decision proposal

Orchestrator may accept/integrate the controlled upload/quarantine implementation and the `NO_PRODUCTION_PARSER_WINNER` evidence state if PR CI/System Integrity passes. This worker does not edit `SYSTEM/STATE.md`, `TASK_LEDGER`, wave manifests, or merge its own PR.
