# RESULT — W006-T008-A01

`TASK_ID: W006-T008`  
`ATTEMPT_ID: A01`  
`ROLE: Developer Platform + Reproducibility + Supply-Chain Worker`  
`BASE_STATE_VERSION: 0047`  
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`  
`OBSERVED_STATE_VERSION_AT_START: 0053`  
`OBSERVED_MAIN_SHA_AT_START: edef0ba740de4b82c70dbb28024258b6f5aa7df7`  
`OBSERVED_MAIN_SHA_BEFORE_RESULT: edef0ba740de4b82c70dbb28024258b6f5aa7df7`  
`WORKER_BRANCH: worker/W006-T008-A01`  
`ISSUE: #195`  
`PR: #218`  
`STATUS: COMPLETE`  
`CONTINUITY_CHECK: PASS`  
`CONFIDENCE: HIGH for the current Python repository graph and release controls; production runtime/database/parser remain intentionally open`  
`PRODUCTION_READY_CLAIM: NOT_AUTHORIZED`

## 1. Continuity / provenance

The dispatch retains original STATE 0047 provenance while requiring execution against the current canonical checkpoint.

At worker start:

- canonical `main` was `edef0ba740de4b82c70dbb28024258b6f5aa7df7` / STATE 0053;
- STATE 0053 had accepted the W006-T007 fan-in and explicitly unlocked W006-T008-A01;
- `worker/W006-T008-A01` was created from that exact `main` SHA;
- protocol-valid `TASK_STARTED` was persisted on Issue #195 before substantive work.

Immediately before RESULT persistence, `main` remained exactly `edef0ba740de4b82c70dbb28024258b6f5aa7df7`. The worker branch is `ahead` of `main` and `behind_by: 0`.

No canonical coordination file (`SYSTEM/STATE.md`, task ledger, W006 wave manifest, canonical scorecard/state) was modified.

Therefore `CONTINUITY_CHECK: PASS`.

## 2. Objective outcome

W006-T008 freezes a reproducible Python toolchain and auditable release supply-chain path without smuggling still-open production technology choices into the lock.

Selected/frozen for the current repository graph:

- authoritative Python: `3.13.15`;
- Python package manager: `uv 0.12.18`;
- root manifest: `pyproject.toml`;
- committed dependency lock: `uv.lock`;
- toolchain metadata/canonical commands: `toolchain.lock.json`;
- repository topology: existing single repository;
- Node/JS production surface: none observed, so no JS package manager/task graph was added;
- release Actions: full-commit-SHA pins;
- default workflow token posture: `contents: read`;
- privileged provenance permissions isolated to the attestation job;
- release artifact: deterministic source bundle with SPDX 2.3 runtime SBOM, local digest-bound provenance and GitHub/Sigstore build-provenance attestation.

Explicitly **not** selected by this task:

- production workflow/runtime;
- production database/shared state engine;
- production parser/OCR implementation;
- frontend framework/editor;
- cloud/deployment topology.

## 3. Evidence-backed package-manager selection

The comparison used the same representative repository graph for uv, Poetry and PDM on one clean `ubuntu-24.04` GitHub runner with Python `3.13.15`.

Comparison dependencies/challengers:

- `pydantic==2.13.4`;
- `jsonschema==4.25.1`;
- `pytest==9.0.2`;
- `pypdf==5.9.0`;
- `dbos==2.31.0`;
- `langgraph==1.2.12`;
- `langgraph-checkpoint-sqlite==3.1.1`.

Predeclared non-compensatory hard gates:

1. all declared dependencies resolve;
2. locked install succeeds;
3. representative imports succeed;
4. warm re-sync succeeds;
5. repeated lock is byte-stable;
6. only after all hard gates pass may operational simplicity and same-runner timing distinguish candidates.

All candidates passed every hard gate over three repeats.

| Candidate | Version | Hard gates | Median first lock | Median first sync | Median warm sync |
|---|---:|---|---:|---:|---:|
| uv | 0.12.18 | PASS | 0.0200 s | 0.0340 s | 0.0086 s |
| Poetry | 2.5.1 | PASS | 2.0448 s | 1.9621 s | 0.7090 s |
| PDM | 2.29.2 | PASS | 18.8626 s | 10.0973 s | 0.4866 s |

Primary selection evidence:

- workflow run `35869987042` — SUCCESS;
- artifact `10754189048`;
- artifact digest `sha256:dda7bf276e5c74f3a7cc00542a7d1478185efd269ebfefbae0489a9905956b3c`;
- persisted summary `artifacts/w006-t008/a01/manager-benchmark.json`;
- executable harness `experiments/w006_t008_toolchain/benchmark_real_graph.py`.

Given equal hard-gate correctness, uv is frozen because it materially reduces lock/sync time on this graph while preserving the smallest operational surface: one standards-based Python manifest, one committed lock and no additional task graph.

## 4. Canonical lock scope and anti-smuggling boundary

The committed root application/test lock intentionally contains only accepted application/test dependencies:

- production/application dependency: `pydantic==2.13.4`;
- test dependencies: `jsonschema==4.25.1`, `pytest==9.0.2`;
- their transitive closure.

The package-manager bakeoff still exercises historical/experimental challengers through `[tool.academy-suno.toolchain-bakeoff].additional-dependencies`, but those entries do **not** become canonical application dependencies.

This preserves T007's evidence boundary: successful local runtime experiments do not become a production runtime/database/parser lock by inclusion in the developer toolchain.

Final CI observed:

- `uv lock --check`: PASS;
- resolved packages in canonical root lock: `17` including root project;
- `uv.lock` SHA-256: `cf1ecfeab5d4a6d01868db8ae45339e3cfc77f0e77d7ebb27a21dba6ef8036ea`;
- `pyproject.toml` SHA-256: `cd23fde7ba48562be54f2e5ec93921841ed9ecfb3314d3601852c94adc7d0af6`.

## 5. Canonical commands

Frozen in `toolchain.lock.json`:

- lock verification: `uv lock --check`;
- clean test install: `uv sync --locked --no-install-project --group test`;
- foundation regression: `uv run --no-sync python experiments/foundation_regression/run_foundation_regression.py`;
- deterministic release evidence: `uv run --no-sync python scripts/supply_chain/build_release_evidence.py --output-dir dist/release`;
- release verification: `uv run --no-sync python scripts/supply_chain/verify_release_evidence.py ... --lockfile uv.lock`.

The lock's integrity is established by Git commit identity + `uv lock --check`; the release provenance records the exact lock SHA-256. No duplicated hard-coded lock checksum is used as a second mutable authority.

## 6. Repository topology / task graph disposition

Repository baseline evidence showed no production `package.json`, no Node production surface and no existing task graph. The selected Python toolchain executes the accepted build/test/release path without Nx, Turborepo or another graph/cache layer.

Therefore:

`REPOSITORY_MIGRATION: NONE`  
`NODE_PACKAGE_MANAGER: NONE`  
`TASK_GRAPH: NONE`

This is a bounded `NO_ADDITIONAL_TOOLING` decision, not a claim that a future frontend/multi-project topology can never justify another tool. Re-open only when a real Node/TypeScript production surface, independently deployable projects or measured repeated CI work supplies representative evidence.

## 7. Release workflow pinning and least privilege

The final release-path audit covered the existing release smoke workflow plus the new W006-T008 supply-chain workflow.

Final audit:

- third-party Action references inspected: `10`;
- movable third-party release Action references: `0`;
- overly broad release token permissions: `0`;
- audit status: `PASS`.

Pinned Action commits frozen in `toolchain.lock.json` include:

- `actions/checkout@11d5960a326750d5838078e36cf38b85af677262`;
- `actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065`;
- `actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830`;
- `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02`;
- `actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093`;
- `actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a`.

The build/test/SBOM job receives only `contents: read`. The separate attestation job receives exactly `contents: read`, `id-token: write` and `attestations: write`.

## 8. Cache policy

The reusable uv cache key is derived from:

- runner OS;
- Python `3.13.15`;
- uv `0.12.18`;
- `hashFiles('uv.lock')`.

The reusable cache step is disabled on pull-request runs. This prevents low-trust PR execution from writing/restoring the shared cache path while still allowing trusted non-PR runs to use a lock-derived cache.

## 9. Final clean-runner validation

Authoritative final supply-chain run:

- workflow: `W006 T008 Supply Chain`;
- run: `35873267257` — SUCCESS;
- head SHA: `b467ae7d21d54ce1b943ca3f86033877ac9a1ddf`;
- PR merge SHA used by GitHub Actions: `a21bdfa3ac3175a0e3a7c5d2ab3c3bccd368ae66`;
- build/test/SBOM/provenance job `107222558772` — SUCCESS;
- GitHub attestation job `107222673525` — SUCCESS.

Clean-runner gates include:

- Python `3.13.15`: PASS;
- uv `0.12.18`: PASS;
- `uv lock --check`: PASS;
- clean locked sync from committed lock: PASS;
- compile accepted Python surfaces: PASS;
- critical foundation suites: `83 passed, 1 skipped, 15 subtests passed`;
- factual oracle: PASS;
- state/event failure harness: PASS;
- release workflow Action/permission audit: PASS;
- deterministic release built twice byte-identically: PASS;
- SBOM/local provenance verifier: PASS;
- GitHub/SLSA provenance attestation creation: PASS;
- `gh attestation verify`: exit `0`.

Independent checks on the same branch head also completed successfully:

- System Integrity run `35873267321` — SUCCESS;
- Foundation Regression run `35873267279` — SUCCESS.

## 10. Releasable artifact, SBOM and provenance

Deterministic release bundle:

- artifact SHA-256: `d238dd414b6bd214e9dc4ee934595523c6eb7b94c4287641b6da02df5c7b3f66`;
- SPDX 2.3 SBOM SHA-256: `1efa3b71c13520fcfd1941c32868e774629417dc210211c465b33ca998decc2a`;
- local provenance SHA-256: `a684d8ed11504aee5c7d28fe63b86b18f929624ea614c6219d099d559695a2e7`.

Runtime SBOM scope is deliberately production-only. It contains the release plus five runtime dependency packages:

- `pydantic==2.13.4`;
- `annotated-types==0.8.0`;
- `pydantic-core==2.46.4`;
- `typing-extensions==4.16.0`;
- `typing-inspection==0.4.4`.

Verifier assertions:

- exactly one direct runtime dependency edge from release to pydantic: PASS;
- test-only `pytest` / `jsonschema` absent from runtime SBOM: PASS;
- SBOM binds artifact SHA-256: PASS;
- local provenance binds artifact, SBOM and exact `uv.lock` SHA-256: PASS;
- provenance does not claim production readiness: PASS.

GitHub build provenance:

- predicate type: `https://slsa.dev/provenance/v1`;
- subject: `academy-suno-source.tar.gz@sha256:d238dd414b6bd214e9dc4ee934595523c6eb7b94c4287641b6da02df5c7b3f66`;
- attestation ID: `49550520`;
- attestation: `https://github.com/pablo-marchina/academy-suno/attestations/49550520`;
- Rekor transparency log index: `2920832033`;
- `gh attestation verify --repo pablo-marchina/academy-suno`: PASS / exit 0.

Workflow evidence artifacts:

- supply-chain artifact `10756795065`, digest `sha256:94edc0e3a9e85f395bfd2b75e7c4322e39d36ed711167cec31fedab1b5592602`;
- attestation-verification artifact `10756925060`, digest `sha256:6ab4ffd5dc776aa7a815e11cdd714928e63d27f603d36a620d26f6b9bbb0be1a`.

Stable summary is persisted at `artifacts/w006-t008/a01/supply-chain-verification.json`.

## 11. Decision Research binding

Exact evidence-backed record created:

- `docs/decisions/research/W006-T008-toolchain-reproducibility.md`.

It records:

1. package-manager `LOCK` — uv `0.12.18` for the current Python graph;
2. repository/task-graph `NO_ADDITIONAL_TOOLING` — keep existing single repository; no Node package manager/task graph absent measured need;
3. release/supply-chain `LOCK` — full-SHA release Actions, least-privilege tokens, lock-derived cache with PR cache suppression, deterministic release, SPDX runtime SBOM, local provenance and GitHub build attestation.

Each material decision includes evidence, confidence and explicit reversal/recheck conditions.

## 12. Acceptance audit

Dispatch acceptance conditions:

- clean locked install/build/test: `PASS`;
- movable third-party release Actions: `0`;
- overly broad release token permissions: `0`;
- releasable artifact with verifiable SBOM: `PASS`;
- releasable artifact with local + GitHub provenance: `PASS`;
- provenance verification: `PASS`;
- package manager selected from representative actual graph: `PASS`;
- repository migration without evidence: `0`;
- unsupported Node/task-graph introduction: `0`;
- production runtime/database/parser promoted by toolchain inclusion: `0`;
- canonical coordination files modified by worker: `0`.

## 13. Files / artifacts created or changed

Primary task-owned surfaces include:

- `pyproject.toml`;
- `uv.lock`;
- `.python-version`;
- `toolchain.lock.json`;
- `experiments/w006_t008_toolchain/benchmark_real_graph.py`;
- `.github/workflows/w006-t008-toolchain-bakeoff.yml`;
- `.github/workflows/w006-t008-supply-chain.yml`;
- `.github/workflows/release-smoke-w004-t012.yml` (migrated to the canonical locked toolchain and full-SHA Actions);
- `scripts/supply_chain/build_release_evidence.py`;
- `scripts/supply_chain/verify_release_evidence.py`;
- `scripts/supply_chain/audit_release_workflows.py`;
- `docs/decisions/research/W006-T008-toolchain-reproducibility.md`;
- `artifacts/w006-t008/a01/manager-benchmark.json`;
- `artifacts/w006-t008/a01/repository-baseline.json`;
- `artifacts/w006-t008/a01/supply-chain-verification.json`;
- `SYSTEM/RESULTS/W006-T008-A01.md`.

PR: `https://github.com/pablo-marchina/academy-suno/pull/218`.

## 14. Reversal / recheck conditions

Re-open the package-manager decision if:

1. locked resolution/install becomes non-reproducible on a supported target while a challenger remains correct;
2. the real dependency graph changes materially enough to invalidate this benchmark;
3. packaging/deployment semantics require capabilities the selected uv path cannot satisfy;
4. repeated lock stability or clean-runner correctness regresses.

Re-open topology/task-graph only after a real multi-project or Node/TypeScript production surface and representative repeated-work evidence exists.

Re-open supply-chain format/signing choices if a selected registry/platform requires container-native signing, CycloneDX specifically, another provenance verifier or non-GitHub identity semantics. Adding Cosign remains an evidence-triggered escalation, not a current default.

## 15. Production readiness boundary

This task proves developer-toolchain/release reproducibility and supply-chain evidence for the current accepted repository surfaces. It does **not** prove the overall product is production-ready.

Material production choices remain open per T007, including runtime, database/shared state, parser/OCR, frontend and deployment topology.

`PRODUCTION_READY_FROM_T008: FALSE`.

## 16. Completion statement

`W006-T008-A01` is complete at the worker implementation/evidence level.

The worker produced representative package-manager evidence, froze the canonical Python toolchain, established a committed reproducible lock/install path, hardened release Actions/permissions/cache behavior, generated and verified deterministic artifact + SPDX SBOM + local provenance + GitHub/Sigstore build provenance, persisted exact Decision Research and evidence records, and opened PR #218 for controller integration.

No worker-side merge or canonical STATE/ledger mutation is performed; integration remains a controller responsibility.
