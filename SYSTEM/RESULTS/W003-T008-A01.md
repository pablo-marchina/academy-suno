# RESULT — W003-T008-A01 — calibration / ablation / anti-gaming release gate

`TASK_ID: W003-T008`  
`ATTEMPT_ID: A01`  
`ROLE: Evaluator/Analyst`  
`STATUS: COMPLETE_DIAGNOSTIC_ONLY — READY_FOR_ORCHESTRATOR_REVIEW`  
`CONFIDENCE: HIGH for release-gate invariants and current evidence disposition; NO CLAIM for final audience thresholds/backend/provider preference`  
`PROTOCOL_VERSION: 1.6.0`  
`BASE_STATE_VERSION: 0018`  
`BASE_COMMIT_SHA: 1a8999c954ab86000e2ae7608616ab5828fbcdfe`  
`OBSERVED_MAIN_SHA_AT_START: 1a8999c954ab86000e2ae7608616ab5828fbcdfe`  
`ISSUE: #66`  
`WORKER_BRANCH: worker/W003-T008-A01`

## 1. CONTINUITY_CHECK

`PASS`.

- Issue #66 binds state `0018` to exact main SHA `1a8999c954ab86000e2ae7608616ab5828fbcdfe`.
- observed `main` matched that SHA at start.
- dependencies W003-T001, W003-T002, W003-T004 and W003-T007 are integrated in the bound base and their RESULT artifacts were consumed.
- isolated branch `worker/W003-T008-A01` was created from the exact bound SHA before substantive work.
- mandatory `TASK_STARTED` and one material `TASK_PROGRESS` were persisted on Issue #66.
- no canonical STATE / TASK_LEDGER / wave / scorecard / traceability / risk file was modified.

## 2. Objective executed

Executed B12 as a development-only calibration release gate with explicit anti-circularity, anti-gaming, semantic hard-gate precedence, and provider-evidence guards.

Primary success targets: `REQ-004..006`, `REQ-010..012`, `REQ-016`, `REQ-019`, Evidence & Analytical Rigor; reduce `RISK-0002`, `RISK-0003`, `RISK-0004`, `RISK-0014`; preserve `CRIT-001`.

## 3. Required inputs reconciled

Consumed:

- `SYSTEM/RESULTS/W003-T001-A01.md` — source-level dev/held-out split, independent annotation contract, target/human/evaluator anti-circularity and threshold posture;
- `SYSTEM/RESULTS/W003-T002-A01.md` — semantic ablation contract and source/factual/policy hard-gate precedence;
- `SYSTEM/RESULTS/W003-T004-A01.md` — PT-BR readability, terminology/ACV and anti-gaming fixtures/tests;
- `SYSTEM/RESULTS/W003-T007-A01.md` — telemetry N/A semantics and synthetic-pricing limitation;
- `data/evals/gold/benchmark_manifest_v001.json` — current benchmark truth;
- Issue #66 / exact state-0018 base.

## 4. Current evidence result

Persisted report: `experiments/calibration/release_gate_report_v001.json`.

Current disposition:

```text
overall_mode = DIAGNOSTIC_ONLY
held_out_tuning_exposure = FORBIDDEN
anti_gaming.status = PASS
audience_calibration.status = NOT_COMPUTABLE
semantic_ablation.status = NOT_RUN
semantic_ablation.decision = NO_BACKEND_PREFERENCE
provider_comparison.status = NOT_COMPARABLE
provider_comparison.decision = NO_PREFERENCE
threshold_posture.freeze_allowed = false
numeric_thresholds = null
```

This is the correct evidence-preserving outcome, not a missing implementation.

### Why audience matrices are not emitted as observed evidence

`gold-v001` currently has:

- 3 accepted source documents;
- 2 development sources;
- 1 held-out source with tuning exposure forbidden;
- `gold_labels: []`;
- no independent human agreement/adjudication statistics;
- manifest threshold freeze explicitly disabled.

Therefore there are no valid development `human_gold_level` rows against which to compute observed `target→human` or `human→evaluator` matrices. This task does not use requested generation target as a substitute for human truth.

## 5. Executable metric contract

Implemented `experiments/calibration/release_gate.py`.

When valid development labels exist, it computes independently:

1. `generation_target_level → human_gold_level`;
2. `human_gold_level → evaluator_predicted_level`.

For each relation it returns:

- deterministic truth-row / prediction-column confusion matrix;
- per-level precision, recall, F1 and support;
- macro precision, recall and F1;
- accuracy.

Any row with split other than exactly `DEVELOPMENT` raises `CalibrationInputError`; held-out cannot silently enter tuning/calibration.

## 6. Threshold/floor posture

The harness does not contain invented numeric audience floors.

Freeze remains blocked by exactly:

- `NO_INDEPENDENT_HUMAN_GOLD_LABELS`;
- `NO_INDEPENDENT_HUMAN_AGREEMENT`;
- `MANIFEST_FREEZE_NOT_AUTHORIZED`.

The report additionally marks `AUDIENCE_CONFUSION_METRICS_NOT_COMPUTABLE` until development human gold + evaluator predictions exist.

Even if a future manifest authorizes freeze, reviewed numeric values must be supplied as versioned evidence; the gate never manufactures them.

## 7. Anti-gaming release evidence

Persisted packet: `experiments/calibration/current_evidence_v001.json`.

The gate requires the mandatory T004 failure modes:

- jargon stuffing → `JARGON_STUFFING`;
- sentence chopping / fragmentation → `SENTENCE_CHOPPING`;
- acronym manipulation → `ACRONYM_HACK`;
- required concept deletion → `REQUIRED_CONCEPT_OMISSION`.

The packet also carries alias stuffing and glossary dumping.

Current anti-gaming disposition is `PASS`, grounded in integrated W003-T004 evidence where the focused deterministic audience suite passed 15 tests. T008 uses that integrated result as evidence; it does not reinterpret those synthetic/deterministic fixtures as human gold.

## 8. Semantic ablation

The gate supports paired semantic-off / semantic-on development rows and verifies:

- hard-fail codes are invariant;
- semantic information may add diagnostics;
- semantic cannot turn a decision into PASS while source/factual/policy hard failure remains.

The current benchmark has no measured development-human-gold semantic ablation rows, so the release report records:

```text
status = NOT_RUN
decision = NO_BACKEND_PREFERENCE
```

This is not evidence against a backend; it preserves backend neutrality until measured incremental value exists.

## 9. Provider/model/cost comparison

Provider/model/backend comparison requires measured evidence with observed quality and latency. Synthetic pricing is explicitly ineligible as provider-cost evidence.

The only cost evidence consumed from T007 is the synthetic telemetry demo, so the current release report returns:

```text
status = NOT_COMPARABLE
decision = NO_PREFERENCE
```

No provider/model/backend preference is introduced.

## 10. Kill criteria

The gate persists these release-stopping conditions:

- `HELD_OUT_TOUCHED_BY_TUNING`;
- `TARGET_PROMOTED_TO_GOLD`;
- `UNVERSIONED_OR_UNPROVENANCED_GOLD`;
- `HARD_GATE_COMPENSATED_BY_SOFT_OR_SEMANTIC_SIGNAL`;
- `ANTI_GAMING_MANDATORY_CASE_MISSED`;
- `SYNTHETIC_PRICING_PRESENTED_AS_PROVIDER_COST`.

## 11. Verification

Focused exact-code local execution:

```text
python -m unittest discover -s tests/calibration -p 'test_*.py' -v
8 tests PASS
```

Covered:

- reproducible confusion/macro/per-level metrics;
- target→human separated from human→evaluator;
- held-out calibration rejection;
- diagnostic-only threshold posture under missing human gold/agreement;
- mandatory anti-gaming failure modes;
- semantic hard-gate non-compensation;
- synthetic pricing exclusion;
- current evidence shape resolving to diagnostic-only / no preference.

A fresh network clone was attempted but the worker shell could not resolve `github.com`. Therefore this RESULT claims the focused exact-code execution above, not an unobserved repository-wide clean-checkout pass. GitHub PR automation/integrator validation remains the appropriate whole-repository check.

## 12. Findings / evidence

### F-001 — current evidence cannot support threshold calibration

**FACT:** benchmark manifest has no human gold labels and disallows freeze.  
**CONCLUSION:** audience thresholds/floors remain `DIAGNOSTIC_ONLY`; creating values would be fabrication.

### F-002 — requested target and evaluator accuracy are different questions

The gate makes `target→human` and `human→evaluator` separate metric surfaces so generation intent can never become circular gold.

### F-003 — held-out preservation is executable

Calibration input is structurally restricted to `DEVELOPMENT`; a held-out row is rejected before metrics can be computed.

### F-004 — anti-gaming evidence is currently stronger than audience calibration evidence

The deterministic T004 anti-gaming contract is executable and integrated, while independent human gold is still absent. Release posture must represent those two evidence levels separately rather than averaging them into a synthetic score.

### F-005 — semantic/provider selection remains evidence-driven

No measured dev-gold semantic ablation and no comparable measured provider quality/latency/cost set exist. Neutrality is therefore the defensible current decision.

## 13. SUCCESS_IMPACT

- `REQ-004..006`: adds a reproducible independent-label metric contract while explicitly preventing target-as-gold circularity.
- `REQ-010/011`: converts readability/terminology anti-gaming evidence into a release gate rather than an advisory diagnostic.
- `REQ-012` / `CRIT-001`: semantic ablation cannot compensate source/factual/policy hard failure.
- `REQ-016`: gate outputs are versioned/reproducible and carry explicit N/A/not-computable semantics.
- `REQ-019`: confusion-matrix/macro/per-level computation is implemented, with observed execution correctly pending valid human labels.
- Evidence & Analytical Rigor: the task records exactly what cannot currently be claimed instead of manufacturing thresholds/backend/provider winners.

## 14. TRACEABILITY_UPDATES — proposals only

For Orchestrator integration:

- link W003-T008 gate/report/tests to `REQ-004..006`, `REQ-010..012`, `REQ-016`, `REQ-019`;
- mark `REQ-019` implementation capability as built but observed matrix evidence pending independent development gold;
- retain audience threshold state as `DIAGNOSTIC_ONLY`;
- preserve semantic backend / provider / model as OPEN / no preference;
- add release-gate evidence for `CRIT-001` non-compensation and held-out isolation.

No canonical traceability file was edited by this worker.

## 15. Assumptions / risks affected

- `RISK-0002`: reduced by mandatory anti-gaming gate; not closed because human audience gold remains absent.
- `RISK-0003`: reduced structurally by anti-circularity and semantic hard-gate invariants; semantic value remains unmeasured.
- `RISK-0004`: explicitly controlled as diagnostic-only; not closed due tiny corpus and no human agreement.
- `RISK-0014`: reduced by mandatory fragmentation/acronym/concept-deletion defenses; broader format generalization remains pending.

No assumption or risk is claimed closed solely by this task.

## 16. Artifacts

- `experiments/calibration/release_gate.py`
- `experiments/calibration/current_evidence_v001.json`
- `experiments/calibration/release_gate_report_v001.json`
- `tests/calibration/test_release_gate.py`
- `docs/evals/calibration/release_gate_v001.md`
- `SYSTEM/RESULTS/W003-T008-A01.md`

## 17. Next actions

1. obtain/freeze natural development outputs and independent blinded human annotations;
2. compute agreement/adjudication evidence before considering any threshold freeze;
3. feed development-only `generation_target_level`, `human_gold_level`, and `evaluator_predicted_level` rows into this gate;
4. execute measured semantic-off/on ablation on the same development gold while preserving hard-gate invariants;
5. compare provider/model/backend only after measured quality + latency and real versioned pricing/usage evidence exist;
6. keep held-out untouched until a separately declared frozen release evaluation.
