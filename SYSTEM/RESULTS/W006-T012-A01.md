# RESULT — W006-T012-A01

`TASK_ID: W006-T012`  
`ATTEMPT_ID: A01`  
`STATUS: RESULT_READY_FOR_PR_CI`  
`CONFIDENCE: HIGH_FOR_REFERENCE_INTEGRATION / LOW_FOR_PRODUCTION_BACKEND_SELECTION`  
`WORKER_BRANCH: worker/W006-T012-A01`  
`BASE_STATE_VERSION: 0047`  
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`  
`OBSERVED_MAIN_SHA_AT_START: 598e633e43399138c06813ef38de2fe90b7686da`  
`OBSERVED_STATE_VERSION_AT_START: 0058`  
`CONTINUITY_CHECK: PASS`

## Objective result

Integrated a dependency-neutral W3C/OpenTelemetry-compatible observability boundary into the accepted W006-T009 real/reference product path while preserving the three-plane authority model:

1. durable product/run/data state — authoritative;
2. durable ordered product/domain events — authoritative for live cockpit replay/state;
3. correlated telemetry — sampled/best-effort and explicitly non-authoritative.

The implementation adds W3C `traceparent` extraction/injection, provider/eval/repair spans, structured event logs, bounded metrics, OTLP/HTTP JSON-compatible traces/metrics/logs export, opaque server-authorized cockpit trace references, privacy allowlisting/redaction and export-outage isolation.

No observability backend, Collector deployment topology, production sampling policy, retention, SLO, capacity or production-ready claim is promoted by this task.

## Durable outputs

### Application instrumentation / export boundary

- `src/suno_content/observability.py`
  - W3C trace context parse/inject;
  - best-effort span/metric/log recorder;
  - metadata allowlist + sensitive-key redaction;
  - one-way safe correlation references for tenant/run/job/attempt identifiers;
  - bounded metric-label policy;
  - OTLP/HTTP JSON-compatible payload/export paths for traces, metrics and logs;
  - exporter failures retained as telemetry evidence but never raised into product execution;
  - opaque scoped trace-link registry.

- `src/suno_content/production_observability.py`
  - provider generation spans across the actual 3×3 orchestration path;
  - evaluator spans and explicit repair spans;
  - run start/resume and authoritative-product-event commit spans;
  - structured product-event telemetry correlated without raw run/job IDs;
  - opaque cockpit trace links with exact org/workspace/run server-side authorization;
  - legacy T009 telemetry callback preserved for backward regression compatibility;
  - telemetry remains downstream of authoritative mutations.

### Reproducible acceptance

- `tests/integration/w006/test_observability_live_ops.py`
- `.github/workflows/w006-t012-observability.yml`
- `artifacts/w006-t012/a01/acceptance_matrix.json`
- `artifacts/w006-t012/a01/README.md`

The workflow preserves exact accepted Python `3.13.15`, exact `uv@0.12.18`, `uv lock --check`, frozen `uv sync --locked`, the T012 acceptance suite, the accepted T009 vertical-slice suite and foundation/runstore/orchestration/cockpit/ingest regressions.

### Backend / topology evidence refresh

- `docs/decisions/research/DR-5701-observability-backend-refresh.md`

The refresh compares direct OTLP, Collector + Grafana-family, Collector/Data Prepper + OpenSearch and Collector + Jaeger + separate signal backends against predeclared hard/quantitative criteria. Application portability is preserved; backend/topology disposition is:

`NO_OVERALL_PREFERENCE / PENDING_REPRESENTATIVE_BACKEND_BAKEOFF`.

## Pre-PR local validation

Executed under local Python `3.13.5` only as a development sanity check, not as the authoritative frozen-toolchain proof:

- `py_compile` for the new observability module, production adapter and T012 integration test: `PASS`;
- standalone observability contract tests: `3/3 PASS`.

Authoritative acceptance remains the PR workflow on Python `3.13.15` + `uv@0.12.18`.

## Hard acceptance mapping

| Hard boundary | Implementation / evidence |
|---|---|
| raw credentials/secrets in telemetry = `0` | metadata allowlist + sensitive-key/value redaction; canary test |
| raw private content emitted by default = `0` | prompt/output/document/body fields redacted; controlled source canary absent from telemetry |
| high-cardinality tenant/run/job IDs as metric labels = `0` | metric label allowlist rejects raw identity dimensions |
| controlled 9-branch traceability = `PASS` | nine provider + nine eval spans correlated by safe job refs on the real/reference 3×3 path |
| unauthorized cockpit trace-link access = `0` | random opaque reference + exact org/workspace/run authorization test |
| telemetry outage corrupting/blocking product path = `0` | failing exporter test still completes 9/9 and replays all 11 authoritative events |
| sampled telemetry used as live cockpit authority = `0` | cockpit phase/cells/revision remain derived from durable product events; trace link is marked `TELEMETRY_NON_AUTHORITATIVE` |
| secret/canary leakage in product events/telemetry = `0` in defined test | source canary does not reach event/telemetry JSON; credential/private fields are redacted |
| backend/collector topology promoted without evidence = `0` | DR-5701 returns `NO_OVERALL_PREFERENCE` |
| unsupported scalar/business utility = `0` | comparison reports raw capability/evidence gaps only |
| production-ready claim | `NOT_AUTHORIZED` |

## Evidence boundary / unknowns

This attempt proves instrumentation mechanics, correlation, privacy defaults, scoped trace links and outage isolation on the accepted reference product path. It does **not** prove production backend performance or operations.

Explicitly unresolved:

- production backend/vendor;
- Collector agent/gateway/direct topology;
- production sampling policy;
- retention policy;
- ingest/query p50/p95/p99;
- telemetry storage/resource cost;
- production SLO/capacity;
- deployed multi-replica observability behavior.

Those require a representative deployed bakeoff under DRG before promotion.

## SUCCESS_IMPACT

- **Production Engineering:** strengthens the real/reference path with correlated, exportable telemetry while keeping product correctness independent of observability availability.
- **Security / Privacy:** narrows default telemetry to metadata and one-way references, keeps secret/private fields redacted and trace access server-authorized.
- **Observability / SRE:** establishes W3C propagation, provider/eval/repair instrumentation, bounded metrics, structured logs and OTLP-compatible export without a premature backend lock.
- **Evidence Quality:** preserves raw trade-offs and explicit unknowns instead of inventing a vendor winner, SLO or capacity claim.

## TRACEABILITY_UPDATES

- `PROD-011`: real/reference live cockpit gains an authorized opaque trace-link surface while product events remain authoritative.
- `PROD-012`: W3C correlation + provider/eval/repair spans + structured logs + bounded metrics + OTLP-compatible export implemented and test-covered.
- `PROD-014`: telemetry privacy/redaction and server-side trace-link authorization added to the reference path.
- `RISK-0035`: telemetry remains non-authoritative; durable event replay remains live truth.
- `RISK-0037`: metadata allowlisting, redaction and bounded metric dimensions materially reduce leakage/cardinality risk in the tested scope.

## Assumptions / risks affected

- The reference telemetry recorder/exporter proves protocol/application mechanics but is not itself a production SDK/backend recommendation.
- In-memory opaque trace-link storage is reference-only; production shared persistence/session/authorization integration remains part of later deployment/security qualification.
- Backend tenancy, retention, query performance, operational recovery and cost remain unmeasured.
- Collector redaction/filtering should remain defense-in-depth; privacy starts at application instrumentation allowlists.
- No production sampling rate is selected because volume/cost/reliability evidence is absent.

## State / decision proposal

Orchestrator may accept/integrate this reference observability/live-ops foundation if the RESULT-bearing PR passes exact frozen-toolchain CI and applicable regressions.

This worker does not modify canonical `STATE`, `TASK_LEDGER`, wave manifests or production technology locks.

## Next actions

1. Use W006-T013 to exercise deployed security/reliability/capacity/recovery with observability enabled.
2. Run a representative backend/topology bakeoff when a deploy target exists, using DR-5701 criteria and raw multidimensional measurements.
3. Validate shared/durable trace-reference storage and production authz integration under multi-replica deployment.
4. Define sampling/retention/SLO/capacity only from measured workload evidence.
