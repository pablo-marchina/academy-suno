# RESULT — W004-T002-A01

`TASK_ID: W004-T002`  
`ATTEMPT_ID: A01`  
`ROLE: Analyst/Evaluator`  
`STATUS: COMPLETE`  
`CONFIDENCE: HIGH_FOR_PREPARATION / NO_HUMAN_CALIBRATION_CLAIM`  
`BASE_STATE_VERSION: 0020`  
`BASE_COMMIT_SHA: 2555308805c2b3eac4ef494605c35e01697f8884`  
`EXECUTION_BASE_MAIN_SHA: 2555308805c2b3eac4ef494605c35e01697f8884`  
`PROTOCOL_VERSION: 1.6.0`  
`ISSUE: #82`  
`WORKER_BRANCH: worker/W004-T002-A01`

## CONTINUITY_CHECK

PASS.

- `SYSTEM/STATE.md` remains `STATE_VERSION: 0020` and declares W004-T002 READY on Issue #82.
- Issue #82 binds A01 to base `2555308805c2b3eac4ef494605c35e01697f8884`.
- `main` was rechecked before completion and remained exactly on that SHA.
- The worker branch was created from the exact base and contains no canonical state integration.
- `TASK_STARTED` was already persisted in Issue #82 before substantive work.

## Objective executed

Expanded the three-source diagnostic bootstrap into a versioned six-source W004 corpus, froze natural development outputs, preserved source-document held-out isolation, and prepared an executable blind double-annotation + adjudication workflow for W004-T005.

The task intentionally does **not** claim completed human calibration, threshold validity, provider quality, or production readiness.

## Findings / evidence

### F1 — corpus expanded with source-level split

`data/evals/w004/source_manifest_v001.json` freezes `w004-corpus-v001` with six source identities:

- 4 `DEVELOPMENT` source documents;
- 2 `HELD_OUT` source documents;
- split unit `SOURCE_DOCUMENT`;
- no source crosses splits;
- legacy held-out membership is preserved;
- one new candidate was assigned held-out before generation;
- held-out tuning exposure is explicitly `FORBIDDEN`.

Coverage expands beyond the original three-source bootstrap across policy, earnings/results, market expectations, issuer disclosure and operational-production source families. It is still smaller than the stronger 18-document design target and must not be represented as statistically complete.

### F2 — 36 natural development outputs frozen and versioned

`data/evals/w004/frozen_outputs_manifest_v001.json` freezes `w004-natural-v001` as 36 development outputs:

- 4 development sources × 3 formats × 3 requested audience levels;
- formats: `ARTICLE`, `CAROUSEL`, `SHORT_VIDEO`;
- targets: `BEGINNER`, `INTERMEDIATE`, `ADVANCED`;
- bytes/text are versioned in three shards with hashes;
- generation was natural generation without gold/evaluator feedback;
- `human_gold_level` remains null;
- `gold_promotion_status` remains `NOT_GOLD`;
- zero held-out outputs were generated.

### F3 — generation target cannot become human gold

The corpus manifest, W004 annotation schema and protocol all enforce:

```text
generation_target_level != human_gold_level != evaluator_predicted_level
```

The requested target is hidden from primary annotation input and cannot be promoted automatically to gold. A downstream human gold label requires two independent primaries plus adjudication when triggered, with annotation provenance.

### F4 — blinded double annotation and adjudication are executable

Artifacts now define:

- a blinded 36-item annotation bank;
- W004-specific JSON schema for `PRIMARY_A`, `PRIMARY_B` and `ADJUDICATOR`;
- explicit forbidden fields for primary annotation;
- two independent primaries per item;
- adjudication triggers for audience disagreement, `UNSCORABLE`, check disagreement, `FAIL`, or `REVIEW_REQUIRED`;
- mandatory references to both primary annotation IDs in adjudicator records;
- a written rationale for adjudication.

### F5 — agreement plan is measurable and reproducible

`experiments/human_calibration/compute_agreement.py` computes before adjudication:

- paired item count;
- raw agreement / disagreement rate;
- `UNSCORABLE` rate per primary;
- Cohen's kappa;
- quadratic weighted kappa on scorable ordinal pairs;
- A→B confusion matrix;
- deterministic adjudication queue with reasons.

The script rejects duplicate item IDs and mismatched primary item sets rather than silently comparing partial populations.

### F6 — held-out isolation and preparation invariants are machine-checkable

`experiments/human_calibration/validate_preparation.py` checks:

- six frozen source identities with 4 development / 2 held-out;
- source-document split posture;
- held-out tuning exposure forbidden;
- 36-output development-only 3×3 grids;
- shard hashes and decoded hashes where declared;
- no pre-populated human gold;
- zero held-out generated outputs;
- blinded-bank item count;
- absence of forbidden target/evaluator/generator fields in blinded payloads;
- threshold freeze remains disabled.

The repository-integrity/CI workflows remain the integration authority after PR creation. No canonical files were changed by this worker.

## Acceptance check

| Acceptance | Result |
|---|---|
| split por source document | PASS |
| generation target não vira human gold | PASS |
| outputs development frozen/versionados | PASS — 36 items |
| blind double annotation | PASS — protocol/schema/bank ready |
| adjudication plan executable | PASS |
| agreement measurable | PASS — executable calculator |
| held-out tuning exposure proibida | PASS |
| nenhum threshold freeze sem evidência suficiente | PASS — explicitly disabled |
| lifecycle signals | PASS — start persisted; terminal emitted after RESULT |

## SUCCESS_IMPACT

Primary contribution: reduces the W004 bottleneck `REPRESENTATIVE_HUMAN_PROVIDER_PARSER_EVIDENCE_AND_RELEASE_PROOF` on the representative/human-calibration preparation axis.

Supports:

- `REQ-004..006` — native audience/format variants can now be independently judged on a broader corpus;
- `REQ-010/011` — factual/material preservation remains non-compensatory in human records;
- `REQ-016` — evaluation can be calibrated against independent human judgments rather than requested targets;
- `REQ-019` — the benchmark path is versioned, reproducible and leakage-aware;
- Evidence & Analytical Rigor — facts, generated targets, human gold and evaluator predictions remain distinct.

This result does **not** reduce provider/model uncertainty, and does not convert human calibration from planned to observed. That evidence belongs to W004-T005.

## TRACEABILITY_UPDATES

Proposals for Orchestrator integration only:

- link `REQ-016` / `REQ-019` to `w004-corpus-v001`, frozen outputs, blind bank, protocol and agreement tooling;
- retain threshold/evaluator claims as `DIAGNOSTIC_ONLY` until W004-T005 produces observed human agreement;
- preserve W004 source split as immutable for this version once annotation begins.

Worker did not edit `TRACEABILITY_MATRIX.md` or other canonical files.

## Assumptions / risks affected

- `RISK-0002`: reduced by broader source coverage, but not eliminated; six documents remain below the stronger design target.
- `RISK-0003`: reduced structurally via independent blind gold workflow; observed calibration remains pending.
- `RISK-0004`: reduced via explicit anti-circularity and held-out isolation; evaluator performance remains unobserved against W004 human gold.
- `RISK-0014`: reduced by source-level held-out controls and predeclared adjudication/agreement flow.
- `RISK-0020`: reduced by frozen hashes, machine-checkable invariants and explicit diagnostic-only posture.

Unknowns retained:

- actual human agreement and adjudication burden;
- representativeness beyond six source documents;
- target→human separability on the frozen outputs;
- human→evaluator calibration;
- any stable threshold suitable for release.

## State / decision proposals

For Orchestrator review:

1. accept W004-T002 as preparation-complete if PR/CI/provenance are valid;
2. release W004-T005 to execute independent annotation/agreement/adjudication;
3. keep `threshold_freeze_allowed = false` until W004-T005 evidence explicitly supports a versioned change;
4. do not expose W004 held-out sources during T005 development calibration;
5. retain the 18-document benchmark design as the stronger target for later statistical confidence rather than pretending six documents are sufficient.

## Artifacts

- `data/evals/w004/source_manifest_v001.json`
- `data/evals/w004/frozen_outputs_manifest_v001.json`
- `data/evals/w004/frozen_outputs/part_01_v001.jsonl`
- `data/evals/w004/frozen_outputs/part_02_v001.jsonl`
- `data/evals/w004/frozen_outputs/part_03_remaining_v001.jsonl.gz.b64`
- `data/evals/w004/blind_items/blind_bank_v001.jsonl.gz.b64`
- `data/evals/w004/annotation_schema_v001.json`
- `data/evals/w004/annotation_plan_v001.json`
- `docs/evals/human_calibration/protocol_v001.md`
- `experiments/human_calibration/README.md`
- `experiments/human_calibration/compute_agreement.py`
- `experiments/human_calibration/validate_preparation.py`

## Next actions

1. Orchestrator validates this attempt/PR and integrates it without worker-side canonical edits.
2. W004-T005 executes two independent primary annotations on the frozen blind bank.
3. Compute agreement before adjudication, adjudicate every triggered item, then construct target→human and human→evaluator matrices.
4. Keep held-out untouched until the development policy is frozen and a declared release evaluation is authorized.
5. If agreement/sample evidence remains weak, retain `DIAGNOSTIC_ONLY`; do not manufacture a threshold.
