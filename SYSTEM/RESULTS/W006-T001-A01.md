# RESULT — W006-T001-A01

`TASK_ID: W006-T001`  
`ATTEMPT_ID: A01`  
`STATUS: COMPLETE`  
`WORKER_BRANCH: worker/W006-T001-A01`  
`BASE_STATE_VERSION: 0047`  
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`  
`OBSERVED_MAIN_SHA_AT_START: adbaff1eeeb08a0a79c3b11684f47cae44c634f4`  
`CONTINUITY_CHECK: PASS`

## Objective completed

Materialized the Phase 9 versioned contract package and normalized Decision Research into one global, deterministic namespace without selecting evidence-gated technologies.

## Durable outputs

- `docs/production/contracts/v1/core-contracts.schema.json`
  - versioned tenant/resource identities;
  - internal event envelope and monotonic run cursor shape;
  - renderer-aligned provenance and parsed-document contracts;
  - provider/evaluator metadata abstractions;
  - persistence records plus content-addressed references and CAS write intent;
  - telemetry allowlist contract.
- `docs/production/contracts/v1/decision-research-registry.schema.json`
  - machine-readable global DR registry contract.
- `docs/production/contracts/v1/README.md`
  - normative invariants, cursor semantics, provenance rules, persistence/CAS semantics, telemetry default-deny policy, versioning, and evidence-gated boundaries.
- `docs/decisions/DECISION_RESEARCH_REGISTRY.v1.json`
  - canonical global `DR-####` namespace;
  - deterministic collision/allocation policy;
  - canonical aliases for current research records;
  - explicit preservation of existing locked claims.
- `SYSTEM/DECISION_RESEARCH_GATE.md`
  - DRG bumped to 1.1 with canonical registry resolution and normalized status semantics.

## Decision Research normalization findings

The current research directory contained two materially different source records using the same legacy ID `DR-0001`, plus task-local research IDs. The registry resolves this without deleting or rewriting source evidence:

- the lexicographically first existing `DR-0001` source retains canonical `DR-0001`;
- the colliding orchestration record maps to canonical `DR-5910` while retaining legacy ref `DR-0001`;
- task-local research records are assigned deterministic canonical IDs above the previous maximum;
- explicit source `LOCK` decisions remain authoritative;
- mixed records preserve only their exact locked subclaims as binding;
- `PENDING_EVIDENCE`, `NO_PREFERENCE`, candidate and unresolved provider/framework choices remain non-binding.

## Contract invariants established

1. Protected downstream objects carry explicit `contract_version`, `org_id`, and `workspace_id`; relevant resources retain stable document/run/job/branch/attempt/eval/repair/experiment identities.
2. Event cursors are replay positions only and are strictly increasing inside `(org_id, workspace_id, run_id)`; cursors never authorize tenant/resource access.
3. Parsed nodes require source provenance and preserve renderer/page/region lineage without inventing unavailable offsets.
4. Provider/evaluator metadata is opaque and versioned; no concrete provider or evaluator winner is selected.
5. Persistence record shapes cover document, blob, provenance, run, event, artifact, evaluation, index, and publish attempt; content-addressed references and expected-revision CAS are explicit.
6. Telemetry is allowlist/default-deny. Raw extractor/backend payloads, source bodies, headers, tokens, cookies, secrets, credentials, and private backend locations are excluded by default.
7. Decision Research canonical IDs are globally unambiguous through the registry; already represented locked work is not duplicated.

## Validation

- continuity check against observed `main`: PASS;
- local JSON Schema Draft 2020-12 structural validation for the contract/registry schemas: PASS;
- registry instance validation and canonical-ID uniqueness check: PASS (`16` records, `16` unique canonical IDs);
- branch compare before RESULT: `ahead_by=5`, `behind_by=0` versus observed main SHA;
- changed-file review confirmed only task-scoped contract/DRG artifacts before RESULT;
- no database/workflow/parser/provider/evaluator/observability/frontend/hosting/public-URL technology winner was introduced.

## Evidence-gated work intentionally left open

Concrete persistence/workflow runtime, parser/OCR provider, provider/model portfolio, evaluation tooling, observability backend, frontend/editor framework, deployment topology, and public URL extraction remain downstream DRG/bakeoff decisions.

## Acceptance result

All W006-T001 dispatch acceptance criteria are satisfied. Worker result is ready for Orchestrator review/integration. The worker does not merge its own PR.
