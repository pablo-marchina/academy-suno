# RESULT — W007-T001-A01

`TASK_ID: W007-T001`  
`ATTEMPT_ID: A01`  
`ROLE: Production Architect + SRE + Evidence Auditor`  
`BASE_STATE_VERSION: 0061`  
`BASE_COMMIT_SHA: 3146b06323f8b15a6feef2ae2e6802d7fb7cafef`  
`OBSERVED_STATE_VERSION_AT_START: 0062`  
`OBSERVED_MAIN_SHA_AT_START: 6fbe0cd430a5c9090b92eb8d555d1cf0bd5fdb45`  
`WORKER_BRANCH: worker/W007-T001-A01`  
`ISSUE: #236`  
`STATUS: COMPLETE`  
`CONTINUITY_CHECK: PASS`  
`DECISION_STATE: PENDING_DEPLOYED_EVIDENCE`  
`PRODUCTION_TOPOLOGY_LOCK: NONE`  
`PRODUCTION_READY_CLAIM: FALSE / NOT_AUTHORIZED`

## 1. Continuity / provenance

The dispatch preserves original provenance at STATE 0061 / `3146b06323f8b15a6feef2ae2e6802d7fb7cafef`.

At execution start the canonical repository was STATE 0062 / main `6fbe0cd430a5c9090b92eb8d555d1cf0bd5fdb45`. STATE 0062 explicitly lists `W007-T001-A01` as READY, and Issue #236 declares that exact SHA as `CANONICAL_READY_MAIN`. Therefore the advancement is compatible orchestrator dispatch drift and `CONTINUITY_CHECK: PASS`.

Branch `worker/W007-T001-A01` was created from the observed main SHA and one protocol-valid `TASK_STARTED` was posted to Issue #236 before substantive work.

No canonical `STATE`, `TASK_LEDGER`, wave manifest or production-ready coordination file was edited.

## 2. Objective outcome

Defined a representative production-topology candidate set and an exact deployed qualification protocol for T002.

Three materially different production deployment classes advance:

1. `C1_GCP_CLOUD_RUN` — Cloud Run request service + continuous worker pool + Cloud SQL PostgreSQL HA/PITR + Cloud Storage + Secret Manager + OTLP.
2. `C2_AWS_ECS_FARGATE` — ECS Fargate API/worker services + ALB + RDS PostgreSQL Multi-AZ/PITR + S3 + Secrets Manager + OTLP.
3. `C3_GKE_AUTOPILOT` — GKE Autopilot API/worker Deployments + Cloud SQL PostgreSQL HA/PITR + Cloud Storage + Secret Manager/Workload Identity + OTLP.

The accepted W006 reference topology remains `B0_REFERENCE_CONTROL / DIAGNOSTIC_ONLY`.

This set intentionally includes two deployment classes on a shared GCP data-plane family to isolate controller/deployment-class effects, plus an AWS managed-task counterfactual. No winner is selected from documentation.

## 3. Research gate

Persisted DRG record:

- `docs/decisions/research/DR-7001-production-topology-qualification.md`

The record includes:

- exact decision question/workload;
- baseline + >=3 material alternatives;
- predeclared criteria/hard gates;
- systematic primary-first search and dated source table;
- security/reliability/cost/lock-in treatment;
- reproducible T002 benchmark design;
- decision/missingness rules;
- evidence saturation and reversal conditions;
- production-contract traceability.

Primary sources are captured machine-readably in:

- `artifacts/w007-t001/a01/source-register.json`

Current-source research was performed on 2026-09-24. Provider/standards documentation is used only for capability/constraint planning; it is not treated as Academy performance evidence.

## 4. T002 qualification contract

Persisted:

- `docs/production/W007_T001_TOPOLOGY_QUALIFICATION_PROTOCOL.md`
- `artifacts/w007-t001/a01/topology-candidates.json`
- `artifacts/w007-t001/a01/qualification-protocol.json`

T002 is required to execute the same immutable application image/toolchain and exact 3x3 identities across candidates, with multi-instance shared durability and no local-filesystem correctness dependency.

The protocol covers:

- exact 9/9 workflow semantics;
- concurrent ownership/fencing/idempotency;
- adaptive load ladder and backpressure;
- failure/restart + managed DB failover;
- backup and isolated restore/PITR;
- expand/contract schema migration + application rollback;
- cross-tenant/object/secret/IAM negative tests;
- OTLP observability, redaction and telemetry-outage isolation;
- clean deploy/health/rollback operational procedure;
- raw metered cost/usage;
- uncertainty, missingness and point Pareto.

## 5. Non-compensatory gates

The following remain hard:

- hard-gate compensation `0`;
- cross-tenant unauthorized success `0`;
- required provenance missing `0`;
- exact branches `9/9`;
- accepted join loss/duplication `0`;
- critical schema violations promoted `0`;
- stale accepted overwrite `0`;
- duplicate accepted branch output `0`;
- uncommitted event accepted `0`;
- quarantine bypass `0`;
- secret/credential canary leakage `0`;
- defined restart/resume `100% PASS`;
- defined backup/restore `100% PASS`.

Performance/cost cannot compensate for any failure.

## 6. Runtime framework boundary

W006-T005-A02 already established a local comparative surface for custom/CAS, LangGraph and DBOS but correctly retained production runtime/database as `PENDING_EVIDENCE`.

T001 does not reopen that result into an unsupported winner. T002 must bind eligible runtime candidates to representative PostgreSQL-backed deployment where supported while preserving the common state/event acceptance semantics.

Temporal remains `OPEN_CHALLENGER_CONDITIONAL`: execute only if a remaining durability/operations hypothesis can plausibly change hard-gate eligibility or Pareto membership after the minimum runtime set is deployed.

## 7. Decision method / current outcome

Accepted ordering:

1. non-compensatory hard gates;
2. raw multidimensional metrics;
3. uncertainty + missingness;
4. point Pareto.

No scalar utility.

`DECISION_STATE: PENDING_DEPLOYED_EVIDENCE`.

`PRODUCTION_TOPOLOGY_LOCK: NONE`.

T002 may emit a production lock only after representative execution if every hard gate passes, required objective families are observed, material alternatives are not silently excluded, and the result remains supported under uncertainty. Otherwise the required outcome is `NO_PREFERENCE` or `PENDING_EVIDENCE`.

A local/documentation-only Pareto singleton is never enough.

## 8. Production blockers addressed by this plan

The protocol directly targets T001 scope from the W006-T014 audit:

- `PROD-001` deployed identity/session/data/object/event tenant isolation surface;
- `PROD-002` deployed timeout/retry/backpressure/proxy/load behavior;
- `PROD-003` multi-replica shared durability/failover/fencing/restore;
- `PROD-004` controlled object storage/quarantine/provenance topology;
- `PROD-006` exact production shared-state 3x3 orchestration;
- `PROD-012` deployed observability topology and ingest/query/cost;
- `PROD-013` saturation/recovery evidence without invented SLO/RTO/RPO;
- `PROD-014` deployed secret/IAM/network/object/session security controls;
- `PROD-016` reproducible deployment, migration and rollback;
- `PROD-017` eventual same-path production deployment identity.

T001 closes the **qualification-plan ambiguity**, not the blockers themselves. The blockers remain open until deployed downstream evidence exists.

## 9. SUCCESS_IMPACT

### Decision quality — HIGH positive

Moves the next production-substrate choice from generic “research more” to a predeclared three-class deployed comparison with explicit no-winner behavior.

### Production engineering — HIGH positive

Defines the shared build/deploy/state/security/observability/recovery evidence T002 must collect before any substrate lock.

### Risk control — HIGH positive

Prevents latency/cost from compensating for tenant, provenance, branch, fencing, recovery or secret failures and prevents documentation capability from being misreported as production evidence.

### Evidence integrity — HIGH positive

Separates capability research, executed Academy evidence, missingness and downstream lock authority.

## 10. TRACEABILITY_UPDATES proposed for Orchestrator

No canonical register was edited. Proposed integration effects:

- link T001 artifacts to `PROD-001,002,003,004,006,012,013,014,016,017`;
- keep production runtime/database/deployment decisions `PENDING_EVIDENCE`;
- promote `W007-T002-A01` only after Orchestrator accepts/integrates this result;
- preserve W005 benchmark methodology and W006 audit blockers unchanged until T002 execution.

## 11. Assumptions / risks

Still open by design:

- exact production region/residency choice;
- final production runtime framework;
- concrete IdP/session/object-scanner/secret/IAM adapters (downstream T003 after topology);
- provider/model/default/routing (T006);
- production frontend/backend observability product choices;
- representative cost/capacity/saturation;
- production SLO/RTO/RPO;
- any final production-ready claim.

The candidate set depends on current provider capabilities documented on 2026-09-24; material provider changes are a recheck condition.

## 12. Artifacts

- `docs/decisions/research/DR-7001-production-topology-qualification.md`
- `docs/production/W007_T001_TOPOLOGY_QUALIFICATION_PROTOCOL.md`
- `artifacts/w007-t001/a01/topology-candidates.json`
- `artifacts/w007-t001/a01/qualification-protocol.json`
- `artifacts/w007-t001/a01/source-register.json`
- `SYSTEM/RESULTS/W007-T001-A01.md`

## 13. Next action

Orchestrator review/acceptance of this worker result can promote `W007-T002-A01` from PLANNED to READY. T002 must then execute the deployed bakeoff; no downstream task should treat T001 planning artifacts as a production topology lock.
