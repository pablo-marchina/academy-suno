# RESULT — W006-T009-A01

`TASK_ID: W006-T009`  
`ATTEMPT_ID: A01`  
`BASE_STATE_VERSION: 0047`  
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`  
`OBSERVED_STATE_VERSION_AT_START: 0056`  
`OBSERVED_MAIN_SHA_AT_START: 975009cae927a952589e1a757f77f4097054a524`  
`WORKER_BRANCH: worker/W006-T009-A01`  
`PR: #225`  
`STATUS: COMPLETE`  
`CONTINUITY_CHECK: PASS`  
`PRODUCTION_READY_CLAIM: NOT_AUTHORIZED`

## 1. Continuity and lifecycle

A01 preserves its original dispatch provenance at STATE 0047 / `0fa1fd46d02d8fb2ad3410823ba417d83b596eac`. At task start, canonical coordination had advanced compatibly to STATE 0056 / `975009cae927a952589e1a757f77f4097054a524`, with T007 accepted and T008-A03 integrated. The worker created `worker/W006-T009-A01`, emitted exactly one `TASK_STARTED` on Issue #196 before substantive work, and made no canonical STATE/ledger/wave edits.

## 2. Integrated path

The implementation in `src/suno_content/production_slice.py` composes one executable path over accepted contracts:

`authenticated session/workspace -> controlled PDF bytes upload/provenance -> parse/trust gate -> exact durable 3x3 fan-out -> evaluation/branch-local repair -> lossless join/aggregate -> authoritative durable state/events -> live replay/reconnect`.

`src/suno_content/production_observability.py` adds best-effort telemetry strictly downstream of authoritative mutations. Telemetry failure is recorded but cannot veto or alter run state, authoritative events, replay, or restore.

## 3. Reference/non-production boundary

This task does not manufacture unresolved production winners. The exercised adapters are explicitly reference/non-production:

- runtime: `plain_async+SQLiteRunStore:REFERENCE_NON_PRODUCTION`;
- database/event substrate: `SQLiteDurableStateEventStore:REFERENCE_NON_PRODUCTION`;
- parser: `pypdf==5.9.0:CHALLENGER_REFERENCE_ONLY` for task smoke only;
- live UI projection: `LiveCockpitProjector:FRAMEWORK_NEUTRAL_REFERENCE`.

Production runtime/database/parser/frontend locks remain `NONE`. Deployment remains unresolved. `ReferenceVerticalSlice.assert_production_qualified()` fails closed, so this result cannot authorize a production-ready claim.

## 4. Integrated evidence

Machine-readable evidence is persisted at `artifacts/w006-t009/a01/vertical-slice-evidence.json`.

The deterministic integration reconstruction yields source hash `8edb6d4e51604e2d6617f889d20a6d1d64f8d2b685b20f9d3998312db2220479`, source ID `source-8edb6d4e51604e2d`, run ID `run-w006-t009-smoke`, exactly nine deterministic job IDs, correlated attempt identities, and 11 authoritative product/domain events with revisions 1..11. Source-original bytes are intentionally not included: this fixture proves integration mechanics and does not substitute for parser bakeoff or source-fidelity evidence.

The slice exposes raw PDF bytes as its upload input. It does not expose an arbitrary untrusted server-filesystem-path production input route.

## 5. Durability, replay, and restore

The smoke pauses after planning, reconstructs the service over the same durable stores, resumes to `COMPLETE`, verifies exactly `9/9` accepted branches, materializes 11 authoritative events, republishes the same mutations without duplicate accepted delivery, rebuilds the live cockpit from authoritative snapshot/event replay, backs up both run-state and event databases, restores them into a fresh directory, and proves replay/view equivalence after restore.

Durable event audit invariants are all zero: state without latest transition `0`, transition without state `0`, transition without outbox `0`.

## 6. Retry, repair, and telemetry failure behavior

A dedicated fault-injection test introduces one transient generator timeout in the BEGINNER×ARTICLE branch and one quality repair in the ADVANCED×CAROUSEL branch. The final run remains `COMPLETE` with exactly nine joined outputs; transport retry count remains isolated to the targeted branch and quality repair count remains isolated to its distinct targeted branch. No branch is lost or duplicated.

A separate telemetry-outage test uses a sink that always raises `ConnectionError`. Authoritative completion still reaches `COMPLETE`, all nine outputs remain present, 11 authoritative events commit, replay remains identical, and telemetry failures are captured only as non-authoritative evidence.

## 7. Auth, tenant, provenance, and live projection

The integrated upload path creates an authenticated browser session, requires same-origin + CSRF, binds org/workspace membership, and requires `OBJECT_WRITE` authorization before the accepted source enters orchestration. Every joined output is asserted to preserve org/workspace/run/cell/job/attempt/source identifiers and source hash.

Accepted foundation regression runs alongside the slice and remains green, preserving the tenant adversarial contract with cross-tenant unauthorized success `0`. Live projection uses the accepted authoritative reconnect path; no static W004 cockpit artifact is used as live truth or final-evidence fallback.

## 8. CI evidence

Binding implementation head before RESULT/evidence-only commits: `b72232438d7c448f4b2c27a4e8272b4c9d38b304`.

All applicable runs on that head completed successfully:

- W006 T009 Vertical Slice — run `35898083420`: `SUCCESS`; focused slice tests plus accepted foundation/runstore/orchestration/cockpit/ingest regressions passed;
- System Integrity — run `35898083454`: `SUCCESS`;
- Foundation Regression — run `35898083487`: `SUCCESS`;
- W006 T008 Supply Chain — run `35898083493`: `SUCCESS`.

An earlier smoke iteration exposed test-fixture issues (first an incomplete dedicated dependency surface, then a lowercase-vs-canonical-enum mismatch). Both were repaired transparently; the binding evidence above is from the corrected green head.

## 9. Hard-acceptance result

- accepted provenance missing: `0`;
- exact branch coverage: `9/9`;
- accepted branch loss/duplication: `0`;
- silent stale same-run overwrite accepted: `0` under the accepted durable-state/event regression contract;
- duplicate accepted branch output after retry/republication: `0`;
- arbitrary untrusted server filesystem-path route exposed by this slice: `0`;
- controlled upload/quarantine/provenance boundary: `PASS` / fail-closed;
- tenant adversarial regression: `PASS`, cross-tenant unauthorized success `0`;
- authoritative replay + reconnect: `PASS`;
- telemetry outage corruption/blocking: `0`;
- static W004 cockpit live-truth fallback: `0`;
- hard-gate compensation: `0`;
- production-ready claim: `NOT_AUTHORIZED`.

## 10. Completion posture

W006-T009-A01 is complete as a real integrated **reference vertical slice** over accepted contracts. It proves that the product path composes end-to-end, including durability, recovery, identity correlation, branch-local failure isolation, authoritative replay, tenant/security regressions, and observability failure isolation, while preserving the evidence-gated production substrate boundaries established by T007/T008.

Controller integration/merge remains separate. The worker does not edit canonical coordination surfaces and does not select a production runtime, database, parser, frontend, observability backend, or deployment class.
