# RESULT — W006-T009-A02

`TASK_ID: W006-T009`  
`ATTEMPT_ID: A02`  
`BASE_STATE_VERSION: 0056`  
`BASE_COMMIT_SHA: 975009cae927a952589e1a757f77f4097054a524`  
`OBSERVED_STATE_VERSION_AT_START: 0057`  
`OBSERVED_MAIN_SHA_AT_START: f4c4f0e8fb099bb30940beef05df4038d53eaa83`  
`WORKER_BRANCH: worker/W006-T009-A02`  
`PR: #227`  
`STATUS: COMPLETE`  
`CONFIDENCE: HIGH`  
`CONTINUITY_CHECK: PASS`  
`PRODUCTION_READY_CLAIM: NOT_AUTHORIZED`

## 1. Continuity and lifecycle

A02 preserves its dispatch provenance at STATE 0056 / `975009cae927a952589e1a757f77f4097054a524`. At task start, canonical coordination had advanced to STATE 0057 / `f4c4f0e8fb099bb30940beef05df4038d53eaa83`. That drift is compatible and consists of the orchestrator recording A01 as diagnostic/not accepted and readying this exact A02 retry; the dispatch base is a parent of the observed main. The worker created fresh branch `worker/W006-T009-A02`, emitted exactly one protocol-valid `TASK_STARTED` on Issue #196 before substantive work, and made no canonical STATE/ledger/wave edits.

## 2. What A02 changed

A02 reconstructs the useful A01 reference vertical-slice implementation as diagnostic input on the fresh A02 branch, but does not reuse A01 lifecycle, RESULT or evidence. The executable path remains:

`authenticated session/workspace -> controlled PDF bytes upload/quarantine/provenance -> parser contract/trust gate -> exact durable 3x3 fan-out -> evaluation/branch-local repair -> lossless join/aggregate -> authoritative durable state/events -> live cockpit replay/reconnect`.

The material correction is validation. `.github/workflows/w006-t009-vertical-slice.yml` now uses the accepted T008-A03 frozen surface:

- exact Python `3.13.15`;
- exact `uv==0.12.18`;
- committed root `pyproject.toml` and `uv.lock`;
- `uv lock --check` before install;
- `uv sync --locked --no-install-project --group test`;
- task and accepted regression tests run with `uv run --no-sync` from that environment;
- no hand-selected direct-pip dependency subset is used to execute tests;
- third-party GitHub Actions used by the new workflow are pinned by immutable full SHA and permissions are `contents: read`.

## 3. Parser/reference boundary

A01's focused smoke relied on `pypdf==5.9.0` installed outside the accepted lock, which was part of the acceptance failure. A02 does not smuggle that dependency into CI and does not mutate the accepted graph merely to preserve the old smoke.

Instead, the A02 integration test injects `LockedFixturePdfAdapter:REFERENCE_TEST_ONLY`, a dependency-free deterministic adapter that exercises the accepted PDF-parser protocol and the real controlled-upload/trust boundary. This proves integration mechanics, not parser quality. It is explicitly not a parser winner and does not change the canonical `production-parser-lock = NONE` posture. Active-content input is proven to quarantine before the parser is called, and low-confidence extraction is proven unable to enter orchestration.

## 4. Integrated path evidence

`src/suno_content/production_slice.py` composes the accepted security, ingest, orchestration, durable run/event store and live-cockpit contracts. `src/suno_content/production_observability.py` keeps telemetry best-effort and downstream of authoritative mutations.

The focused A02 suite proves:

- a controlled PDF byte upload enters through the provenance-bound upload path;
- authenticated org/workspace context is required before orchestration;
- the run can pause after planning and resume from durable state;
- exactly nine unique jobs are planned and exactly nine outputs are accepted;
- each joined output preserves org/workspace/run/cell/job/attempt/source identities plus source hash;
- authoritative publication produces 11 events with revisions `1..11`;
- republishing the same authoritative mutations does not duplicate the event stream;
- cockpit projection is rebuilt from authoritative state/events and reaches 9 live cells;
- backup/restore reproduces the authoritative event stream and cockpit view;
- one injected transport retry remains branch-local;
- one injected quality repair remains branch-local and distinct from transport retry accounting;
- a telemetry sink outage cannot block completion or corrupt authoritative replay;
- the reference API accepts `raw_pdf` bytes and exposes no server-filesystem path parameter;
- production qualification remains fail-closed.

Machine-readable evidence is persisted at `artifacts/w006-t009/a02/vertical-slice-evidence.json`.

## 5. Binding clean-checkout CI evidence

Binding implementation head before evidence/RESULT-only commits: `5f076249baea118eb026661d268d85aa0c864bca`.

All applicable workflows on that head completed successfully:

- `W006 T009 Vertical Slice` — run `35902195655`: `SUCCESS`; exact Python/uv verification, committed-lock check, frozen sync, compile, focused slice tests and accepted regressions passed;
- `Foundation Regression` — run `35902195531`: `SUCCESS`;
- `System Integrity` — run `35902195448`: `SUCCESS`;
- `W006 T008 Supply Chain` — run `35902195535`: `SUCCESS`; clean locked build/test, release workflow audit, deterministic release evidence, SBOM/provenance verification and GitHub build-provenance attestation all passed.

The RESULT/evidence-only commits intentionally do not alter the accepted dependency graph or production substrate choices. The final branch head is rechecked after RESULT persistence before the terminal signal.

## 6. Hard-acceptance result

- accepted provenance missing: `0`;
- exact branch coverage: `9/9`;
- accepted branch loss: `0`;
- accepted branch duplication: `0`;
- silent stale same-run overwrite accepted: `0` under accepted durable-state/event regressions;
- duplicate accepted retry/republication output: `0`;
- arbitrary untrusted server-filesystem production route exposed by this slice: `0`;
- controlled upload/quarantine/provenance boundary: `PASS`, fail-closed;
- active-content parser bypass: `0` in the defined focused test;
- low-confidence extraction entering orchestration: `0` in the defined focused test;
- cross-tenant unauthorized success in accepted regressions: `0`;
- authoritative replay/reconnect: `PASS`;
- cursor used as authorization authority: `0` under accepted cockpit/security regressions;
- telemetry outage corruption/blocking: `0`;
- static W004 cockpit used as production live truth/final-evidence fallback: `0`;
- locked-toolchain clean install/build/test: `PASS`;
- runtime/database/parser/frontend/deployment/vendor winner manufactured without compliant evidence: `0`;
- hard-gate compensation: `0`;
- production-ready claim: `NOT_AUTHORIZED`.

## 7. SUCCESS_IMPACT

A02 removes the specific blocker that invalidated A01: the vertical slice is now exercised on the same accepted frozen Python dependency/toolchain surface used by T008-A03 rather than a bespoke direct-pip environment. This strengthens reproducibility and supply-chain evidence without converting reference adapters into production decisions.

## 8. TRACEABILITY_UPDATES

Worker proposes the following controller-side interpretation only; no canonical coordination files were edited:

- `PROD-005/006/011/017`: fresh reference integration evidence under accepted frozen toolchain is available in PR #227 and the A02 evidence artifact;
- `RISK-0021/RISK-0031/RISK-0035`: defined integration regressions remain green on the binding head; unresolved substrate/vendor decisions remain evidence-gated rather than silently promoted;
- W006-T009 may be reviewed for acceptance/integration by the Orchestrator; T011/T012 remain controller-gated until that review occurs.

## 9. Assumptions, risks and decision posture

Facts: focused and accepted regression CI passed on the binding implementation head; exact toolchain checks passed; supply-chain/SBOM/provenance/attestation passed.

Inference: these results establish reference end-to-end composability and reproducibility under the accepted frozen graph, not production suitability of SQLite, plain async, the fixture parser, a frontend framework, a deployment class or any vendor.

Unknown/pending: representative human/business utility evidence, production parser bakeoff winner, runtime/database/frontend/deployment/observability/vendor winners and blanket production readiness remain unresolved outside this worker scope.

No new Decision Research Gate lock/default is proposed.

## 10. Artifacts and next actions

Artifacts:

- PR #227;
- `src/suno_content/production_slice.py`;
- `src/suno_content/production_observability.py`;
- `tests/integration/w006/test_vertical_slice.py`;
- `.github/workflows/w006-t009-vertical-slice.yml`;
- `artifacts/w006-t009/a02/vertical-slice-evidence.json`;
- this RESULT.

Next action: Orchestrator reviews A02 and decides acceptance/integration. Worker does not merge the PR, edit canonical state, or release T011/T012.
