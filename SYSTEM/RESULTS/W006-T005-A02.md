# RESULT — W006-T005-A02

`TASK_ID: W006-T005`  
`ATTEMPT_ID: A02`  
`ROLE: Production Runtime / Shared-State Decision`  
`BASE_STATE_VERSION: 0050`  
`BASE_COMMIT_SHA: 7139b482a3e61e70b957a2573f11a1cbf7e0d3a5`  
`OBSERVED_STATE_VERSION_AT_START: 0051`  
`OBSERVED_MAIN_SHA_AT_START: cb3d1f44feccd4544eb997087cde82aff3e14124`  
`OBSERVED_MAIN_SHA_BEFORE_RESULT: cb3d1f44feccd4544eb997087cde82aff3e14124`  
`WORKER_BRANCH: worker/W006-T005-A02`  
`ISSUE: #192`  
`STATUS: COMPLETE`  
`CONTINUITY_CHECK: PASS`  
`DECISION_STATE: PENDING_EVIDENCE`  
`PRODUCTION_RUNTIME_LOCK: NONE`  
`PRODUCTION_DATABASE_LOCK: NONE`  
`PRODUCTION_READY_CLAIM: NOT_AUTHORIZED`

## 1. Continuity / retry provenance

A02 preserves the orchestrator-issued retry provenance at STATE 0050 / `7139b482a3e61e70b957a2573f11a1cbf7e0d3a5` while consuming the explicitly compatible current STATE 0051 integration at `cb3d1f44feccd4544eb997087cde82aff3e14124`.

At execution start:

- canonical protocol was `1.8.0`;
- current STATE was `0051`;
- `main` was `cb3d1f44feccd4544eb997087cde82aff3e14124`;
- W006-T003-A01 was accepted/integrated and supplied the common durable state/event/CAS failure floor;
- STATE 0051 explicitly opened W006-T005-A02 as the dependency-capable retry of blocked A01;
- branch `worker/W006-T005-A02` was created from the observed `main` SHA;
- protocol-valid `TASK_STARTED` was posted to Issue #192 before substantive work;
- `main` was rechecked before this RESULT and remained unchanged.

Therefore `CONTINUITY_CHECK: PASS`. This worker did not modify canonical `STATE`, `TASK_LEDGER`, wave manifests, decision indexes, or canonical traceability registers.

## 2. Objective outcome

Executed the required common-workload runtime/shared-state bakeoff in a dependency-capable GitHub Actions clean checkout.

The mandatory minimum candidate set was actually installed and executed:

- task-owned custom/CAS control;
- LangGraph `1.2.12` with `langgraph-checkpoint-sqlite==3.1.1`;
- DBOS `2.31.0`.

All three executed the exact Academy workload membership:

- audiences: beginner, intermediate, advanced;
- formats: article, carousel, short_video;
- expected accepted branches: `3 × 3 = 9`.

All three candidates passed the defined non-compensatory hard gates in this benchmark topology.

## 3. Shared semantic floor

Before candidate comparison, the runner executed the accepted W006-T003 common state/event failure harness from `tests/runstore/failure_harness_w006_t003.py`.

Observed floor:

- scenarios: `14/14 PASS`;
- silent stale same-run overwrite accepted: `0`;
- permanent logical event gap after reconciliation: `0`;
- event without authoritative committed transition: `0`;
- duplicate logical projection: `0`;
- cross-tenant replay success: `0`;
- restart/replay/repair pass rate: `100%`;
- backup/restore pass rate: `100%`.

The common authoritative acceptance layer remains the T003 SQLite reference substrate. It is held constant so candidate runtime semantics are compared against the same acceptance contract; it is **not** promoted as a production database.

## 4. Candidate hard-gate evidence

| Hard gate | custom/CAS | LangGraph | DBOS |
|---|---:|---:|---:|
| exact accepted branch membership 9/9 | PASS | PASS | PASS |
| accepted join branch loss | `0` | `0` | `0` |
| accepted join branch duplication | `0` | `0` | `0` |
| duplicate accepted output | `0` | `0` | `0` |
| stale/late overwrite | `0` | `0` | `0` |
| uncommitted event accepted | `0` | `0` | `0` |
| defined restart/resume/replay | `100% PASS` | `100% PASS` | `100% PASS` |
| stale ownership writer silently accepted | `0` | `0` | `0` |
| raw duplicate side-effect accounting present | PASS | PASS | PASS |
| critical schema/provenance violations | `0` | `0` | `0` |

The candidate-specific common scenarios included duplicate/replayed delivery, timeout after provider side effect before authoritative acceptance, CAS contention, ownership-epoch fencing, stale/late result, forged/uncommitted event, out-of-order result, durable replay, and a real separate-process crash/restart probe.

## 5. Real process-crash / side-effect evidence

For each candidate, a child process executed the provider-side effect and was deliberately terminated with `os._exit(91)` before the runtime checkpoint completed. A fresh process then reopened the candidate's durable runtime state and retried/recovered the same execution identity.

Observed for all three candidates:

- first process exit code: `91`;
- restart process exit code: `0`;
- recovered branch identity: correct;
- authoritative accepted transitions: `1`;
- duplicate accepted output: `0`;
- physical provider attempts in the crash window: `2`;
- duplicate provider-side attempt in the crash window: `1`;
- externally billed cost: `NOT_OBSERVED`.

This demonstrates the intended distinction between accepted-output idempotency and external/provider-side idempotency. No hidden claim is made that retries are cost-free.

## 6. Provider-side duplicate accounting

Across the complete candidate harness:

| Candidate | physical attempts | unique effects | duplicate attempts | billed cost |
|---|---:|---:|---:|---|
| custom/CAS | `49` | `48` | `1` | `NOT_OBSERVED` |
| LangGraph | `51` | `48` | `3` | `NOT_OBSERVED` |
| DBOS | `49` | `48` | `1` | `NOT_OBSERVED` |

LangGraph's repeated same-thread invocations in this harness re-entered the graph node, so the benchmark probe observed duplicate provider attempts under duplicate/timeout delivery. The common authoritative acceptance layer still admitted zero duplicate accepted outputs. This is raw task-specific evidence, not a general claim about every LangGraph application or provider integration.

## 7. Raw latency / uncertainty

Runner metadata:

- GitHub Actions `ubuntu24`, image `20260920.314.1`;
- Python `3.13.15`;
- SQLite `3.45.1`;
- LangGraph `1.2.12`;
- LangGraph SQLite checkpointer `3.1.1`;
- DBOS `2.31.0`.

Each candidate has 18 repeated invocation samples:

| Candidate | median ms | p95 ms | stdev ms |
|---|---:|---:|---:|
| custom/CAS | `1.732` | `1.892` | `0.079` |
| LangGraph | `3.576` | `63.683` | `22.261` |
| DBOS | `5.731` | `28.011` | `32.839` |

Outliers were retained. These measurements combine runtime orchestration, local persistence and benchmark-probe overhead on a hosted runner. They are **not** production request latency, capacity, saturation, cost, RTO/RPO or SLO evidence.

## 8. Decision method and outcome

Applied the accepted W005 v002 decision ordering:

1. non-compensatory hard gates;
2. raw multidimensional metrics;
3. uncertainty where meaningful;
4. point Pareto among eligible systems;
5. evidence-sufficiency decision state.

With the explicitly declared point objectives `median_runtime_invocation_ms` and `duplicate_side_effect_attempts`, the observed point Pareto set for this local benchmark is:

`[custom_cas]`

This **does not authorize a production winner**.

Why the production decision remains `PENDING_EVIDENCE`:

- all candidates were evaluated over a common local SQLite-backed acceptance topology, not the intended multi-replica/shared-state production topology;
- the point latency samples are synthetic hosted-runner measurements and do not establish representative production performance or operational burden;
- no production database/control-plane candidate was compared here;
- representative failover, network/replica coordination, saturation/backpressure, operational recovery and production security/isolation remain unmeasured;
- external/provider billed cost is `NOT_OBSERVED`;
- a local Pareto singleton cannot substitute for the missing production-topology evidence.

Therefore:

`DECISION_STATE: PENDING_EVIDENCE`  
`PRODUCTION_RUNTIME_LOCK: NONE`  
`PRODUCTION_DATABASE_LOCK: NONE`

## 9. Temporal challenger disposition

`TEMPORAL_STATUS: OPEN_CHALLENGER_NOT_EXECUTED_THIS_ATTEMPT`.

The dispatch made Temporal conditional after the mandatory minimum set. The custom/CAS + LangGraph + DBOS execution produced a valid hard-gate and Pareto decision surface. The remaining material uncertainty is now the representative distributed/shared-state topology, not a missing fourth local SQLite-backed datapoint.

Temporal therefore remains open for a later challenger run when its service/worker/history model can be compared on a production-capable topology and can plausibly change the eligible frontier enough to justify its larger migration/operations surface.

## 10. Clean-checkout CI evidence

Dedicated workflow:

- `.github/workflows/w006-t005-a02-runtime-bakeoff.yml`;
- GitHub Actions run `35809604146`;
- head SHA `936de5482c4f6cdd2a76a3cbeab885179c2f9407`;
- job `dependency-capable-clean-checkout`: `success`;
- pinned dependency install + `pip check`: `success`;
- exact 3×3 runtime bakeoff: `success`;
- raw evidence upload: `success`;
- artifact ID `10728918067`;
- artifact digest `sha256:37358620505667ca1a3d6e0a13742bb212111ab63d9ab6a8e6ef09083aab7f33`.

This resolves the environmental blocker that made W006-T005-A01 non-comparative. A01 remains immutable diagnostic evidence; A02 is the fresh comparative retry.

## 11. Decision Research Gate

Persisted:

- `docs/decisions/research/DR-6005-runtime-shared-state-bakeoff-a02.md`;
- `artifacts/w006-t005/a02/decision.json`.

The record reconciles this execution with the earlier W005 orchestration research, the accepted T003 state/event semantics, the W005 benchmark methodology, and official framework persistence/idempotency configuration documentation. It explicitly separates official capability documentation from Academy-specific executed evidence.

## 12. Machine-readable evidence

Persisted:

- `artifacts/w006-t005/a02/candidate-observations.json` — branch membership, scenario evidence, state/revision/ownership snapshots, retries/restarts, side-effect counts, latency samples, exact runner/library versions and empty observed failure taxonomy;
- `artifacts/w006-t005/a02/decision.json` — hard-gate eligibility, Pareto objectives/set, uncertainty, decision state, Temporal disposition and recheck conditions;
- `artifacts/w006-t005/a02/runner.json` — clean-runner provenance and exact dependency versions.

## 13. SUCCESS_IMPACT

### Production Engineering — HIGH positive

Replaces the W005 research-only candidate table and W006-T005-A01 environment blocker with an actual three-candidate clean-runner comparison over the exact 3×3 workload and common failure contract.

### Execution Robustness — HIGH positive

Makes duplicate delivery, timeout-after-side-effect, stale writers, ownership fencing, stale/late results, out-of-order delivery, replay, and real process crash/restart observable under the same authoritative acceptance semantics.

### Decision Quality — HIGH positive

Produces hard-gate/Pareto evidence while refusing to turn a local synthetic point-Pareto singleton into an unsupported production technology lock.

### Cost / side-effect transparency — positive

Persists physical/unique/duplicate provider attempts and marks billed cost `NOT_OBSERVED`, preventing accepted-output correctness from hiding retry-side external cost exposure.

## 14. TRACEABILITY_UPDATES

Proposed for Orchestrator integration; this worker did not edit canonical registers:

- `PROD-003`: gains concrete candidate-level runtime/CAS/restart/fencing evidence, but production multi-replica substrate remains pending;
- `PROD-006`: exact 3×3 runtime branch membership, lossless accepted join and recovery semantics gain comparative evidence;
- `PROD-011`: replay/replayed-delivery and durable authoritative acceptance evidence strengthened;
- `PROD-013`: deterministic failure injection now includes dependency-capable runtime challengers and real process crash/restart;
- `PROD-017`: clean-runner artifact identity and exact dependency/runtime provenance are persisted;
- `RISK-0033`: runtime-level evidence materially improves, but distributed persistence/control-plane risk remains open;
- `RISK-0035`: accepted live state/event truth remains explicitly separated from telemetry/provider attempts.

## 15. Remaining risks / assumptions

Still open by design:

- production shared-state database/event/control-plane selection;
- multi-host/multi-replica behavior and network partitions;
- representative failover and executor fencing on the selected production topology;
- representative throughput, saturation, backpressure and tail latency;
- deployment/upgrade/rollback and operational recovery burden for each surviving runtime;
- production security/RBAC/secrets/network isolation for runtime state;
- externally billed provider cost under real retry behavior;
- Temporal challenger execution if a production-topology hypothesis requires it;
- end-to-end vertical-slice and final production readiness evidence.

## 16. Artifacts created

- `experiments/w006_t005_runtime_bakeoff/runtime_bakeoff_adapters.py`;
- `experiments/w006_t005_runtime_bakeoff/run_runtime_bakeoff.py`;
- `experiments/w006_t005_runtime_bakeoff/requirements.txt`;
- `.github/workflows/w006-t005-a02-runtime-bakeoff.yml`;
- `artifacts/w006-t005/a02/candidate-observations.json`;
- `artifacts/w006-t005/a02/decision.json`;
- `artifacts/w006-t005/a02/runner.json`;
- `docs/decisions/research/DR-6005-runtime-shared-state-bakeoff-a02.md`;
- `SYSTEM/RESULTS/W006-T005-A02.md`.

Operational evidence:

- Issue #192 — valid `TASK_STARTED` plus material `TASK_PROGRESS` signal;
- Actions run `35809604146` — successful dependency-capable clean checkout and comparative bakeoff;
- Actions artifact `10728918067` — raw runner evidence bundle.

## 17. State / decision proposals

For Orchestrator review:

1. accept W006-T005-A02 as the completed dependency-capable common-workload comparative evidence retry;
2. retain runtime technology selection as `PENDING_EVIDENCE` rather than converting the local point Pareto singleton into a production lock;
3. retain production database/shared-state selection as pending;
4. consume the A02 raw evidence and decision artifact in W006 fan-in/T007 when the remaining dependencies are available;
5. schedule production-topology comparison for the eligible candidates before any runtime lock;
6. execute Temporal only when that production-topology comparison leaves a material orchestration/durability hypothesis that Temporal can resolve;
7. do not infer provider cost where the harness reports `NOT_OBSERVED`.

## 18. Completion statement

`W006-T005-A02` is complete as a dependency-capable common-workload runtime/shared-state bakeoff task.

Completion means custom/CAS, LangGraph and DBOS were installed and actually executed on the exact 3×3 workload with shared hard gates, failure/restart probes, raw side-effect accounting, reproducible runner provenance and a Decision-Research-compliant decision artifact. It does **not** mean a production runtime/database is selected or that Academy Suno is production-ready.
