# RESULT W004-T011-A01

`TASK_ID: W004-T011`  
`ATTEMPT_ID: A01`  
`ROLE: Writer/Auditor/Builder`  
`STATUS: COMPLETE — READY_FOR_ORCHESTRATOR_REVIEW`  
`CONFIDENCE: HIGH for release/demo artifact hardening; NO CLAIM for calibrated audience thresholds, real provider/model quality, real provider cost, parser implementation winner or production readiness`  
`BASE_STATE_VERSION: 0022`  
`BASE_COMMIT_SHA: 7059381fd34aee20f159a477ba61eb4c4048af45`  
`OBSERVED_MAIN_SHA_AT_START: 7059381fd34aee20f159a477ba61eb4c4048af45`  
`ISSUE: #97`  
`PR: #99`  
`WORKER_BRANCH: worker/W004-T011-A01`

## 1. CONTINUITY_CHECK

`PASS`.

- Issue #97 bound STATE 0022 to exact SHA `7059381fd34aee20f159a477ba61eb4c4048af45`.
- `main` matched that SHA at start.
- Worker branch was created from that exact base before substantive writes.
- `TASK_STARTED` and material `TASK_PROGRESS` signals were persisted on Issue #97.
- Changes are limited to README, `docs/release/**`, `docs/demo/**`, `scripts/release_smoke/**`, `tests/release_smoke/**`, plus this required RESULT.
- No canonical coordination file was modified.

## 2. Objective executed

W004-T011 hardened the release/demo layer without bypassing the external human/provider gates.

Delivered:

1. reproducible clean-start README with executable architecture and evidence posture;
2. a release/cockpit smoke runner that regenerates the existing W003 mechanics proof, renders the evidence cockpit from the same persisted lineage, and reruns the W004 parser/source-trust bakeoff;
3. a focused smoke test covering FAIL→repair→PASS lineage and evidence-state non-promotion;
4. an evidence-packet skeleton with explicit insertion points for human confusion matrices, provider quality/latency/usage/cost, semantic ablation and final T008 proof;
5. a final-review/submission checklist that makes unresolved external blockers release-stopping;
6. a demo storyboard targeting 4:40 with a hard <=5:00 cap and real code/UI + persisted lineage proof.

T011 intentionally does **not** replace W004-T008.

## 3. README / clean-start posture

`README.md` now provides a minimal clean-start path using the pinned dependency versions already documented by the foundation gate:

```bash
python3 -m venv .venv
. .venv/bin/activate
python -m pip install --disable-pip-version-check --no-cache-dir \
  'pydantic==2.13.4' 'pytest==9.0.2'
python experiments/foundation_regression/run_foundation_regression.py
python scripts/release_smoke/run_release_smoke.py \
  --workdir /tmp/academy-suno-release-smoke \
  --output /tmp/academy-suno-release-smoke/manifest.json
```

The README makes the architecture explicit from source/provenance through 3×3 jobs, hard gates, targeted repair, RunStore/telemetry, evidence cockpit and release packet.

It also preserves the required release vocabulary:

- `PROVEN`;
- `DIAGNOSTIC_ONLY`;
- `PRODUCTION_UNKNOWN`;
- `BLOCKED`;
- `PENDING`.

No aggregate readiness claim was added.

## 4. Reproducible release/cockpit smoke

Artifact: `scripts/release_smoke/run_release_smoke.py`.

The runner composes already-integrated evidence paths rather than inventing a parallel demo:

1. executes `experiments/w003_e2e/run_proof.py`;
2. persists proof report, RunStore history and telemetry events to the smoke workdir;
3. renders `app/evidence_cockpit.py` from those exact artifacts;
4. reruns `experiments/parser_w004/run_bakeoff.py`;
5. asserts critical evidence boundaries and writes a machine-readable manifest.

Acceptance-critical assertions include:

- exact 9/9 mechanics completion;
- `MECHANICS_ONLY` and `deterministic_stub` stay visible;
- provider quality claim remains `NOT_MADE`;
- real provider cost remains `N/A`;
- repair lineage starts at persisted `FAIL` and ends at persisted `PASS`;
- before/after output hashes differ;
- hard gates are fresh after repair;
- accepted siblings remain immutable;
- audience thresholds remain `DIAGNOSTIC_ONLY`;
- human confusion matrices remain unavailable at the bound checkpoint;
- provider quality/latency/cost remains `PRODUCTION_UNKNOWN`;
- semantic ablation remains not fabricated;
- parser flat-value loss remains `REVIEW_REQUIRED`, wrong-role corruption remains `FAIL`, and parser implementation remains `UNLOCKED`;
- cockpit must show the exact before/after hashes, `MECHANICS_ONLY`, evidence markers and the no-aggregate-readiness contract.

The emitted smoke manifest preserves downstream states as `BLOCKED/PENDING`, `PRODUCTION_UNKNOWN/BLOCKED` and `PENDING_W004_T008`.

## 5. Focused test

Artifact: `tests/release_smoke/test_release_smoke.py`.

The test invokes the smoke runner in an isolated temporary workdir and asserts:

- manifest `PASS`;
- persisted `FAIL -> PASS` repair lineage;
- fresh hard-gate execution;
- sibling immutability;
- `PROVEN` mechanics classification;
- `DIAGNOSTIC_ONLY` audience thresholds;
- provider `PRODUCTION_UNKNOWN`;
- final release readiness remains `PENDING_W004_T008`;
- all four explicit non-claims are retained.

## 6. Evidence packet skeleton

Artifact: `docs/release/EVIDENCE_PACKET.md`.

The packet separates current facts from required future evidence. It includes dedicated placeholders for:

- target→human confusion matrix;
- human→evaluator confusion matrix;
- real provider/model quality;
- observed provider latency/usage;
- commercial cost only from observed usage + eligible official pricing provenance;
- semantic backend ablation;
- final clean-E2E T008 result.

The provenance section requires source/document reference, source hash when observed, run/job/attempt, evidence scope, artifact ref, observed timestamp when available, provider/model/version for real runs and pricing provenance before any cost claim.

Unknown fields must remain unknown rather than being copied or inferred from adjacent artifacts.

## 7. Demo <=5:00

Artifact: `docs/demo/DEMO_SCRIPT.md`.

Operational target: **4:40**, leaving 20 seconds of margin before the hard 5:00 cap.

Storyboard:

- 0:00–0:25 — problem + evidence contract;
- 0:25–1:05 — real proof/cockpit code path;
- 1:05–1:55 — persisted FAIL→repair→PASS;
- 1:55–2:50 — live evidence cockpit with provenance;
- 2:50–3:35 — parser/source-trust guard;
- 3:35–4:15 — explicit human/provider unknowns;
- 4:15–4:40 — release boundary / T008 dependency.

The demo explicitly forbids calling deterministic-stub mechanics “provider quality” or “production”. It also provides overrun cuts that never remove the persisted repair proof or blocker/unknown section.

## 8. Final review / submission skeleton

Artifact: `docs/release/FINAL_REVIEW_CHECKLIST.md`.

The checklist makes a positive production/release claim conditional on all of the following becoming real versioned evidence:

- two genuine independent human primary annotation streams;
- pre-adjudication agreement and valid confusion matrices;
- observed credentialed provider evidence sufficient for T007;
- observed usage + eligible official pricing provenance for any cost claim;
- semantic ablation on the same valid development human gold;
- final W004-T008 clean-E2E proof and review.

If any required external blocker remains unchecked, production/release readiness stays `PENDING`.

## 9. Validation evidence

### 9.1 Worker-side static validation

The new runner and focused test were fetched back from the worker branch and compiled with Python:

```text
python -m py_compile scripts/release_smoke/run_release_smoke.py tests/release_smoke/test_release_smoke.py
SYNTAX_OK
```

This validates Python syntax only; it is not represented as an executed end-to-end smoke.

### 9.2 Clean-checkout repository CI on implementation head

PR #99 implementation head `9652e866c68f1e1ad9c096e3483b1810d151c147` ran both repository checks successfully:

- System Integrity — run `35293887207`: `completed / success`;
- Foundation Regression — run `35293887144`: `completed / success`.

Important limitation: the existing Foundation Regression runner compiles `tests/**` but its explicit pytest target list predates `tests/release_smoke`; therefore these two green checks are **not** claimed as execution of the new focused release-smoke test. Final W004-T008 should execute the new smoke/test explicitly from a clean checkout.

That limitation is preserved because T011 prepares the release proof; it does not manufacture final release evidence.

## 10. Evidence classification after T011

| Claim | Posture |
|---|---|
| controlled W003 mechanics + persisted repair lineage | `PROVEN` within `MECHANICS_ONLY` |
| evidence-cockpit rendering contract | `PROVEN` within existing artifact projection scope |
| W004 parser behavior on current fixtures | `PROVEN` within fixture scope |
| numeric audience thresholds | `DIAGNOSTIC_ONLY` |
| observed human confusion matrices | `BLOCKED / PENDING` |
| real provider/model quality | `PRODUCTION_UNKNOWN / BLOCKED` |
| real provider latency/usage/cost | `PRODUCTION_UNKNOWN / BLOCKED` |
| parser implementation winner | `PENDING / UNLOCKED` |
| semantic backend preference | `PENDING` |
| production/release readiness | `PENDING W004-T008` |

## 11. SUCCESS_IMPACT

T011 reduces release/demo risk without laundering missing evidence:

- clean-start and architecture are directly reproducible from README;
- demo now has a fixed <=5:00 operational script grounded in real code/UI and persisted mechanics evidence;
- evidence packet has explicit slots for missing human/provider evidence rather than fabricated tables;
- final-review checklist turns blockers into explicit stop conditions;
- smoke runner centralizes the proof/cockpit/parser checks needed for downstream clean release validation;
- final T008 now has a prepared packet/test entry point instead of bespoke release assembly.

## 12. Artifacts

- `README.md`
- `scripts/release_smoke/run_release_smoke.py`
- `tests/release_smoke/test_release_smoke.py`
- `docs/release/EVIDENCE_PACKET.md`
- `docs/release/FINAL_REVIEW_CHECKLIST.md`
- `docs/demo/DEMO_SCRIPT.md`
- `SYSTEM/RESULTS/W004-T011-A01.md`
- PR #99

## 13. Next actions

1. Orchestrator reviews/integrates PR #99 if accepted.
2. T005 must only resume in a new attempt when two valid independent human streams exist.
3. T007 must wait for valid human evidence plus observed provider evidence.
4. T006 must use the same valid development human gold.
5. W004-T008 should run the release smoke/focused test explicitly from a clean checkout, fill only evidence placeholders backed by versioned artifacts, and keep release readiness pending if any external gate remains unresolved.
