# RESULT W004-T016-A01

`TASK_ID: W004-T016`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0025`
`BASE_COMMIT_SHA: 0980534866ed84c2f6af28453f6cb20cd1c7efba`
`WORKER_BRANCH: worker/W004-T016-A01`
`STATUS: TASK_BLOCKED_MANUAL_CAPTURE_REQUIRED`
`CONFIDENCE: HIGH`

## Continuity

- `PROTOCOL_VERSION: 1.6.0`
- observed canonical `STATE_VERSION: 0025`
- observed main SHA at start: `0980534866ed84c2f6af28453f6cb20cd1c7efba`
- Issue #109 base SHA: `0980534866ed84c2f6af28453f6cb20cd1c7efba`
- dependencies W004-T012, W004-T014 and W004-T015 were integrated in STATE 0025 before this attempt.
- `CONTINUITY_CHECK: PASS`
- required `TASK_STARTED` signal was emitted after branch creation and before substantive work.

## Outcome

A real final video could **not** be produced truthfully in this worker runtime. The runtime could access GitHub only through the connected GitHub tool, while a direct repository clone failed because `github.com` DNS resolution was unavailable. Without a runnable full checkout/browser capture path, this attempt cannot claim that a real UI recording exists or that a video duration was measured.

Per the dispatch's explicit fallback rule, the attempt terminates as `TASK_BLOCKED_MANUAL_CAPTURE_REQUIRED` and persists a deterministic capture package instead of fabricating a storyboard-as-video, screenshots, provider evidence, human evidence, or a made-up runtime.

## Deterministic capture package

### Exact repository provenance

`scripts/demo_capture/prepare_capture.py`:

- requires the recording checkout to be a descendant of corrected recipient-app integration SHA `0980534866ed84c2f6af28453f6cb20cd1c7efba`;
- records the actual capture `HEAD` SHA and branch;
- rejects working-tree changes outside `artifacts/demo/**`;
- requires the integrated recipient app, release-smoke runner and W004-T012 manifest to exist;
- runs the existing release smoke by default and persists its repair lineage/evidence posture into capture preflight metadata.

This prevents a later manual recording from being silently captured against unrelated or locally modified application code.

### Public financial PDF provenance

The package selects a primary public source for the real PDF path:

- publisher: Banco Central do Brasil;
- document: `Relatório de Estabilidade Financeira — maio 2026`;
- landing page: `https://www.bcb.gov.br/publicacoes/ref/202605`;
- PDF: `https://www.bcb.gov.br/content/publicacoes/ref/202605/RELESTAB202605-refPub.pdf`.

At capture time the preflight downloads the raw PDF, validates `%PDF-` magic bytes and computes SHA-256 over the exact bytes. The runbook requires the app's recipient-visible raw-input SHA-256 to match that preflight hash. This attempt does **not** invent the PDF hash because the restricted worker runtime could not fetch the raw bytes.

### Duration and immutable artifact measurement

`scripts/demo_capture/finalize_capture.py`:

- invokes `ffprobe` on the actual recorded file;
- fails if duration exceeds `300.000s`;
- records exact measured duration, video byte size and SHA-256;
- preserves the target `280s` / hard cap `300s` distinction;
- does not claim visual-content correctness from metadata alone.

`scripts/demo_capture/verify_capture.py` re-hashes the video and source PDF and verifies the persisted cap/hash metadata.

### Visual/content truth boundary

The generated final manifest deliberately leaves visual inspection as `REQUIRES_HUMAN_VISUAL_REVIEW`. The timed runbook requires a single real take to show:

1. exact capture SHA + public-PDF provenance;
2. real recipient-facing UI;
3. raw-text ingestion;
4. public-PDF ingestion and matching raw-byte hash;
5. canonical 3×3 output/evidence view;
6. persisted `FAIL → repair → PASS` lineage with fresh gates/sibling immutability;
7. visible `MECHANICS_ONLY`, `DIAGNOSTIC_ONLY`, and `PRODUCTION_UNKNOWN/BLOCKED` boundaries.

No provider/human evidence is added by this package.

## Validation performed in this attempt

Local validation of the new self-contained capture tooling:

```text
python -m compileall -q scripts/demo_capture tests/demo_capture
PASS

python -m unittest discover -s tests/demo_capture -v
Ran 4 tests in 0.003s
OK
```

Covered invariants:

- raw-file SHA-256 is deterministic;
- non-PDF magic bytes are rejected;
- a 279.5s recording is accepted mechanically while visual review remains pending;
- a 300.001s recording is rejected.

GitHub compare from exact base before RESULT persistence:

- status: ahead-only;
- ahead by: 8 commits;
- behind by: 0;
- changed package files: 8;
- all non-RESULT writes are inside declared ownership paths (`docs/demo/final/**`, `scripts/demo_capture/**`, `tests/demo_capture/**`, `artifacts/demo/**`).

The full recipient app/release smoke could not be rerun in this runtime because the repository could not be cloned, so no such run is claimed here.

## Acceptance status

- actual video artifact or immutable/discoverable submission-ready reference: **BLOCKED — no false success**
- measured runtime `<=5:00`: **BLOCKED until real recording exists; measurement tooling prepared**
- target around `4:40`: **PASS for deterministic runbook target only; not a recorded-duration claim**
- real app/UI + text/PDF ingestion + 3×3/evidence + persisted repair lineage: **BLOCKED for video proof; deterministic required shot path prepared**
- evidence labels preserved truthfully: **PASS in package/runbook; final visual review still required**
- no provider/human evidence fabrication: **PASS**
- version/SHA and capture provenance: **PASS for capture mechanism; actual capture HEAD pending real recording**
- actual public financial PDF path selected with source provenance: **PASS for source selection; raw-byte hash pending capture-time fetch**
- deterministic fallback if capture unavailable: **PASS**
- lifecycle signals: **PASS at START/PROGRESS; terminal BLOCKED follows this persisted RESULT**

## Blocker

`BLOCKER: worker runtime cannot obtain/run the full repository checkout or perform a trustworthy real screen recording; direct git clone failed because github.com DNS resolution is unavailable.`

`NEEDS: run the persisted capture package on a normal workstation with repository checkout, internet access to the selected BCB PDF, a browser/screen recorder, and ffprobe; persist the real video (or immutable/discoverable submission-ready reference) plus final_capture_manifest.json only after visual review.`

## SUCCESS_IMPACT

Positive but incomplete. This attempt reduces the remaining F-001/F-008 execution risk by converting manual recording into an exact-SHA, source-hashed, hard-duration-capped procedure and by making it difficult to accidentally launder unknowns during capture. It does **not** close F-001/F-008 because no real video exists yet.

Human calibration and real-provider evidence remain independent external blockers exactly as before; this attempt neither advances nor weakens those claims.

## TRACEABILITY_UPDATES proposed for Orchestrator

- retain F-001/F-008 as OPEN/BLOCKED pending real capture artifact;
- attach this result/capture package as deterministic remediation evidence, not final video evidence;
- keep W004-T005 human evidence and W004-T004/T007 provider evidence states unchanged;
- do not promote release readiness from the existence of capture tooling.

## Assumptions / risks affected

- risk that the final video is recorded against the wrong commit: reduced by ancestor/exact-HEAD provenance capture;
- risk that a public PDF is shown without raw-byte provenance: reduced by download/hash/app-hash match procedure;
- risk of exceeding 5:00: reduced by `ffprobe` hard rejection and 4:40 runbook;
- risk of claiming frame contents from metadata automation: explicitly controlled by `REQUIRES_HUMAN_VISUAL_REVIEW`;
- actual recording availability remains unresolved and is the terminal blocker for this attempt.

## Artifacts

- `docs/demo/final/README.md`
- `docs/demo/final/CAPTURE_RUNBOOK.md`
- `scripts/demo_capture/prepare_capture.py`
- `scripts/demo_capture/finalize_capture.py`
- `scripts/demo_capture/verify_capture.py`
- `tests/demo_capture/test_capture_tools.py`
- `artifacts/demo/CAPTURE_REQUIRED.md`
- `artifacts/demo/capture_package.json`
- `SYSTEM/RESULTS/W004-T016-A01.md`

## Next actions

1. On a normal workstation, checkout the exact accepted branch/commit and run `python scripts/demo_capture/prepare_capture.py`.
2. Start the recipient app and record one take following `docs/demo/final/CAPTURE_RUNBOOK.md`.
3. Run `finalize_capture.py` and `verify_capture.py`; reject/re-record anything above 300 seconds or with hash mismatch.
4. Visually review that every required path/evidence label is actually visible; only then may a subsequent attempt/orchestrator state promote the video requirement.
