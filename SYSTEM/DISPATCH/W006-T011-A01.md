# DISPATCH W006-T011-A01

`STATUS: READY`
`TASK_ID: W006-T011`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0047`
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`
`ISSUE: #198`
`ROLE: AI Runtime + Eval + FinOps`
`DEPENDENCIES: W006-T009,W006-T010`
`WORKER_BRANCH: worker/W006-T011-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Objective

Refresh exact provider/model/pricing/lifecycle facts and run representative same-corpus comparisons across static candidates, deterministic cascades and learned shadow routing only where justified; promote defaults only with DRG-valid evidence.

T009-A02 is now accepted/integrated and supplies frozen-toolchain end-to-end reference integration evidence. T010 eval/human-calibration foundation is already integrated, but independent human streams remain `0`, adjudicated human gold remains absent, HELD_OUT remains `NOT_RUN`, and audience thresholds remain `DIAGNOSTIC_ONLY`. Do not use model/self-consistency evidence as a substitute for missing human calibration.

## Continuity / lifecycle

Before substantive work:

1. preserve original attempt provenance `STATE 0047 / 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`;
2. fetch current `main` and `SYSTEM/STATE.md` and run a fresh `CONTINUITY_CHECK` against STATE 0058/current main;
3. create/use `worker/W006-T011-A01`;
4. emit exactly one protocol-valid `TASK_STARTED` on Issue #198;
5. persist `SYSTEM/RESULTS/W006-T011-A01.md` plus raw/reproducible benchmark and promotion evidence before exactly one terminal signal;
6. do not edit canonical STATE/ledger/wave coordination files.

## Decision Research / evidence boundary

Material provider/model/routing/adaptive-policy defaults are DRG-governed. Use accepted W005 benchmark v002: non-compensatory hard gates → raw multidimensional metrics → uncertainty/missingness → point Pareto. Do not introduce scalar/business utility, neutral imputation or weighted winner selection without representative human/business utility evidence and stable sensitivity.

Refresh current provider/model capability, lifecycle, rate-limit and pricing facts from primary sources where available, with timestamps/effective dates. Benchmark only capability-eligible candidates on a representative same-corpus workload. Separate facts, measured observations, uncertainty and inference. Keep unresolved tradeoffs `NO_PREFERENCE` / `PENDING_EVIDENCE` rather than forcing a winner.

Adaptive/learned routing may only be evaluated inside the deterministic eligibility/hard-gate envelope and should remain shadow/non-default unless evidence supports promotion and rollback conditions are explicit.

## Hard acceptance

- hard-gate violating output accepted for cost/latency = `0`;
- capability-ineligible fallback = `0`;
- invalid auth/config/tenant/policy blindly retried = `0`;
- accepted run missing policy/catalog/pricing identity = `0`;
- provider/model/pricing evidence lacking effective-date/context = `0` for promoted defaults;
- canary critical-failure rollback = `PASS` before any adaptive/default promotion;
- forced winner on unresolved Pareto tradeoff = `0`;
- scalar/business utility used without representative evidence = `0`;
- human-gold absence silently converted into production audience thresholds = `0`;
- production-ready claim = `NOT_AUTHORIZED` from this task alone.

## Output

Persist `SYSTEM/RESULTS/W006-T011-A01.md` plus raw benchmark, current-fact source table, uncertainty/Pareto analysis, policy/catalog/pricing identities, rollback evidence and any DRG record required for a material promotion. If evidence is insufficient for a material default, preserve the unresolved state rather than blocking non-material implementation evidence.
