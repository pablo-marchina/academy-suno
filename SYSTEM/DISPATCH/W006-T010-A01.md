# DISPATCH W006-T010-A01

`STATUS: READY`
`TASK_ID: W006-T010`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0047`
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`
`ISSUE: #197`
`ROLE: Eval Science + Human Calibration`
`DEPENDENCIES: W006-T001,W006-T004`
`WORKER_BRANCH: worker/W006-T010-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Readiness at STATE 0051

Both dependencies are now accepted/integrated:

- `W006-T001-A02` — versioned production contracts / DR traceability foundation;
- `W006-T004-A01` — controlled ingestion/provenance + parser/OCR evidence, with explicit `NO_PRODUCTION_PARSER_WINNER` boundary.

Preserve the original attempt provenance `STATE 0047 / 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`. Do not rewrite it to the current state. Before substantive work, run `CONTINUITY_CHECK` against observed current main / STATE 0051 and consume the accepted T001/T004 artifacts from main.

## Objective
Establish versioned DEV/CALIBRATION/HELD_OUT source-group partitions, blind independent human-calibration protocol, agreement/adjudication, secondary judge calibration and paired baseline/candidate experiment contracts.

## Hard acceptance
Automated/model-only label called human gold = `0`; hard-gate compensation = `0`; audience thresholds remain `DIAGNOSTIC_ONLY` until independent calibration + held-out replication.

Actual human annotation is an external evidence dependency and must remain explicit if unavailable.

## Evidence boundary

This task may materialize the calibration/eval protocol, partition contracts, annotation packets/forms, agreement/adjudication computation, judge-calibration harness, paired experiment schemas and reproducible dry-run validation. It must not silently replace unavailable independent human annotations with model-generated labels or infer production audience thresholds from W004 automated calibration.

Parser/OCR selection remains outside this task: T004 explicitly produced no production parser winner. Human-calibration source groups must preserve provenance and source-group identity rather than treating derived 3×3 siblings as independent source draws.

## Output / lifecycle
Persist `SYSTEM/RESULTS/W006-T010-A01.md` + eval/human-calibration foundation artifacts. Start only after T001 and T004 integration; CONTINUITY_CHECK → `TASK_STARTED` → work → RESULT → one terminal signal. No canonical coordination edits.
