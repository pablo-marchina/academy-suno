# DISPATCH W006-T008-A02

`STATUS: READY`
`TASK_ID: W006-T008`
`ATTEMPT_ID: A02`
`BASE_STATE_VERSION: 0053`
`BASE_COMMIT_SHA: edef0ba740de4b82c70dbb28024258b6f5aa7df7`
`ISSUE: #195`
`ROLE: Developer Platform + Supply Chain + Release Engineering + Evidence Auditor`
`DEPENDENCIES: W006-T007 (accepted/integrated)`
`WORKER_BRANCH: worker/W006-T008-A02`
`TASK_SIGNALS_REQUIRED: YES`

## Why A02 exists

A01 completed lifecycle and produced useful executable toolchain/supply-chain evidence, but it is diagnostic/not accepted. PR #218 is closed without merge because A01 promoted uv to a material package-manager `LOCK` while its research record did not satisfy the mandatory `SYSTEM/DECISION_RESEARCH_GATE.md` record contract.

A01 must not be reused as an attempt. It may be cited as diagnostic/counterfactual evidence, but A02 must independently persist its own RESULT and any product/toolchain changes on the fresh A02 branch.

## Objective

Produce a DRG-compliant, reproducible toolchain/supply-chain freeze for the actual Academy Suno repository/dependency graph, then validate it from clean checkout. Preserve every open T007 technology boundary that this task does not have representative evidence to close.

## Mandatory Decision Research repair

Before promoting any package manager, repository topology, task graph, release-control alternative, or other material choice to `LOCK`/default, create a canonical research record under:

`docs/decisions/research/DR-####-<slug>.md`

Use an available unique DR identifier consistent with the repository's Decision Research registry/conventions. The record must satisfy all applicable items in `SYSTEM/DECISION_RESEARCH_GATE.md`, including:

1. exact decision question and workload/constraints;
2. at least three materially different alternatives where available, including current baseline when applicable;
3. evaluation criteria declared before interpreting results; no arbitrary scalar utility;
4. systematic source-search strategy, query/category coverage, research date and evidence-saturation stopping rule;
5. source table with source/type/date/authority/claim/limitations;
6. primary docs/specifications/security guidance first where available;
7. explicit security, reliability, cost, operational burden and lock-in analysis where relevant;
8. representative reproducible same-workload benchmark/experiment;
9. raw results, repeats and uncertainty/limitations rather than conclusion-only reporting;
10. `LOCK | NO_PREFERENCE | PENDING_EVIDENCE` (or equivalent) only as evidence supports;
11. confidence with justification;
12. reversal/recheck conditions;
13. traceability to PROD/RISK/metrics/artifacts affected;
14. explicit evidence-saturation justification.

A01's non-canonical `docs/decisions/research/W006-T008-toolchain-reproducibility.md` does not satisfy this requirement by itself.

## Executable evidence requirements

A02 may reproduce or improve the A01 same-runner uv/Poetry/PDM benchmark, but must not merely copy the prior conclusion. At minimum:

- clean dependency resolution/locked install/import/warm-resync/repeat-lock stability under comparable conditions;
- identical representative dependency graph for compared candidates;
- repeated measurements with raw persisted evidence and limitations;
- current versions and source evidence recorded;
- clean locked install/build/test from the selected candidate if a LOCK is justified;
- foundation regression under the locked toolchain;
- release-path third-party Actions pinned to full commit SHA;
- least-privilege permissions; movable release Action refs = `0`; unnecessarily broad release token permissions = `0`;
- lock-derived cache keying with low-trust PR cache safety;
- deterministic releasable artifact;
- machine-verifiable SBOM;
- digest-bound provenance/attestation and verification;
- repository migration only if compliant DRG evidence actually justifies it.

If the research does not justify a material package-manager winner, keep `NO_PREFERENCE`/`PENDING_EVIDENCE` and do not force a lock merely to progress the DAG.

## Boundary conditions

Do not convert package-manager/toolchain work into a hidden production runtime/database/parser/frontend/deployment choice. T007 authority remains binding:

- production workflow/runtime: `PENDING_EVIDENCE`;
- production database/shared-state: `PENDING_EVIDENCE`;
- parser/OCR: `NO_PRODUCTION_PARSER_WINNER`;
- frontend/editor, identity/data/object vendors, observability backend, deployment class and unrelated technology choices remain open unless this attempt produces directly representative DRG-compliant evidence for that exact question.

No canonical coordination edits from the worker.

## Required output / lifecycle

1. Read current `main`, `SYSTEM/STATE.md`, this dispatch, T007 RESULT/fan-in and A01 diagnostic RESULT/research artifact.
2. Preserve A02 provenance `STATE 0053 / edef0ba740de4b82c70dbb28024258b6f5aa7df7` and run a fresh `CONTINUITY_CHECK` against current canonical main/STATE.
3. Create/use `worker/W006-T008-A02`.
4. Emit `TASK_STARTED` before substantive work.
5. Perform research + experiments + implementation + clean-checkout validation.
6. Persist `SYSTEM/RESULTS/W006-T008-A02.md`, canonical `DR-####` research record(s), raw machine-readable evidence and reproducibility/supply-chain artifacts.
7. Open the worker PR, inspect required CI, and emit exactly one terminal `TASK_COMPLETE | TASK_BLOCKED | TASK_STALE`.

## Hard acceptance

- complete applicable DRG record for every material `LOCK`/default = `100%`;
- material toolchain/default winner without complete DRG = `0`;
- clean locked install/build/test = `PASS` if a lock is promoted;
- movable third-party release Actions = `0`;
- unnecessarily broad release token permissions = `0`;
- releasable artifact has verifiable SBOM + provenance/attestation = `PASS`;
- repository migration without DRG evidence = `0`;
- hard-gate compensation = `0`;
- production-ready claim = `NOT_AUTHORIZED`.
