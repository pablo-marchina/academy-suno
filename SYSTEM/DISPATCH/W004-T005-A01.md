# DISPATCH W004-T005-A01

`STATUS: READY`
`TASK_ID: W004-T005`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0021`
`BASE_COMMIT_SHA: READ_FROM_ISSUE_85_AT_START`
`DEPENDENCIES: W004-T002`
`ISSUE: #85`
`WORKER_BRANCH: worker/W004-T005-A01`
`TASK_SIGNALS_REQUIRED: YES`
`OWNERSHIP_PATHS: experiments/human_calibration_run/**, tests/human_calibration_run/**, docs/evals/human_calibration_run/**`

## Objective

Execute blinded independent human annotation/agreement/adjudication and audience calibration on the frozen W004 development evidence only.

## Required inputs

- accepted W004-T002 corpus/preparation artifacts;
- `data/evals/w004/source_manifest_v001.json`;
- frozen development outputs and blind item bank;
- W004 annotation schema/plan and agreement tooling;
- existing audience/evaluator diagnostics and calibration gate;
- Issue #85 for exact base SHA at start.

## Critical acceptance

- two independent primary annotations are materially independent; do not simulate independence by copying one label stream into another;
- requested generation target must remain hidden from primary annotators and cannot become human truth;
- agreement is measured before adjudication, including raw agreement, disagreement, Cohen's kappa, quadratic weighted kappa where computable, UNSCORABLE rates and confusion surface;
- adjudicate every predeclared trigger with explicit provenance/rationale;
- construct target→human and human→evaluator matrices separately when evidence is valid;
- held-out remains untouched throughout development calibration;
- threshold freeze only if observed evidence/agreement supports it; otherwise preserve `DIAGNOSTIC_ONLY` without false precision;
- document sample limitations, uncertainty, disagreement slices and any inability to obtain genuinely independent human annotations;
- lifecycle signals required: `TASK_STARTED` before substantive work and exactly one terminal signal.

## Kill criteria

Stop/return `TASK_BLOCKED` rather than fabricate evidence if two genuinely independent human annotation streams cannot be obtained. Do not substitute requested target, evaluator prediction, duplicated labels or model-generated pseudo-human labels for independent human gold.

## Output

Persist `SYSTEM/RESULTS/W004-T005-A01.md` plus artifacts only under owned paths. Do not modify canonical STATE/ledger/wave/scorecards.