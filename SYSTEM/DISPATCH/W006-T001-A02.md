# DISPATCH W006-T001-A02

`STATUS: READY`
`TASK_ID: W006-T001`
`ATTEMPT_ID: A02`
`BASE_STATE_VERSION: 0048`
`BASE_COMMIT_SHA: adbaff1eeeb08a0a79c3b11684f47cae44c634f4`
`ISSUE: #188`
`ROLE: API/Domain Architect + Evidence Auditor`
`DEPENDENCIES: none`
`WORKER_BRANCH: worker/W006-T001-A02`
`TASK_SIGNALS_REQUIRED: YES`
`PRIOR_ATTEMPT: W006-T001-A01 diagnostic/not accepted`

## Why this fresh attempt exists

A01 completed lifecycle and produced useful contract/registry artifacts, but PR #203 failed repository `System Integrity` because it modified protocol-governed `SYSTEM/DECISION_RESEARCH_GATE.md` without the required protocol bump/new canonical decision and mixed that protocol mutation with product/docs changes. A01 already emitted terminal `TASK_COMPLETE`, so it must not be reused.

A02 is a clean retry. Treat A01 as diagnostic input only; independently revalidate anything retained.

## Authority

Implement the contract/schema and Decision Research traceability foundation defined by accepted `docs/production/W005_FINAL_FANIN_PHASE9_IMPLEMENTATION_PLAN.md` and canonical STATE 0049. Existing `SYSTEM/DECISION_RESEARCH_GATE.md` remains governing protocol and is **read-only for this attempt**.

## Objective

Materialize versioned Phase 9 contracts and a globally unambiguous Decision Research registry without changing protocol-governed files and without selecting evidence-gated technologies.

Required contract surface:

- stable org/workspace/user/document/run/job/branch/attempt/eval/repair/experiment identities;
- protected resource tenant/provenance binding;
- event envelope, transition/revision and replay-cursor contract;
- parsed-document/provenance contract;
- provider/evaluator metadata abstractions;
- persistence ownership/revision/CAS contract;
- telemetry allowlist/default-deny contract;
- globally unambiguous DR registry/schema/alias mapping that preserves source evidence and does not silently promote open choices.

## Mandatory correction from A01

1. **Protocol files are read-only.** Do not modify `SYSTEM/DECISION_RESEARCH_GATE.md`, `SYSTEM/CONSTITUTION.md`, `SYSTEM/TASK_SIGNALS.md`, `SYSTEM/AUTOPILOT.md`, `scripts/validate_system.py`, `.github/workflows/system-integrity.yml`, or any other protocol-governed file.
2. Registry/normalization semantics needed by Phase 9 belong in task-owned docs/schema artifacts, e.g. `docs/decisions/` and `docs/production/contracts/v1/`.
3. Do not claim a DRG/protocol version bump. Any future protocol evolution is a separate Orchestrator transaction requiring protocol bump + canonical decision.
4. Preserve historical source records; resolve collisions via an explicit registry/alias mapping rather than rewriting evidence history.
5. Source `LOCK` subclaims may be represented as binding only when the exact source record supports them; mixed/open records must not become wholesale locked.
6. `PENDING_EVIDENCE`, `NO_PREFERENCE`, candidates and unresolved vendor/framework/runtime/parser/backend/package-manager choices remain non-binding.

## Hard acceptance

- protected resource contract missing required tenant/provenance identity = `0`;
- critical schema violation promoted = `0`;
- cursor cannot independently authorize or select tenant/resource;
- event/state transition identity + revision fields are mandatory where defined;
- canonical DR references are globally unambiguous through the registry;
- duplicate canonical DR IDs = `0`;
- evidence-gated technology winner introduced without DRG evidence = `0`;
- protocol-governed files changed by A02 = `0`;
- repository `System Integrity` on the A02 PR = `PASS` before Orchestrator integration.

## Validation requirements

Persist reproducible validation for:

- JSON Schema structural validity using a declared draft/version;
- registry instance validation against its schema;
- canonical-ID uniqueness and resolvability;
- legacy/collision alias resolution;
- representative negative cases: missing tenant/provenance identity, malformed cursor/event revision, ambiguous/unknown DR reference;
- changed-file review demonstrating no protocol-governed files changed.

Do not manufacture numeric performance/quality targets in this task.

## Output / persistence

Required:

- `SYSTEM/RESULTS/W006-T001-A02.md`;
- owned contract/schema/docs/validation artifacts under `docs/production/contracts/v1/`, `docs/decisions/` and/or task-owned test/evidence paths;
- PR against `main` from `worker/W006-T001-A02`.

Do not modify canonical coordination files (`SYSTEM/STATE.md`, `SYSTEM/TASK_LEDGER.md`, `SYSTEM/WAVES/*`, checkpoints) or protocol-governed files.

## Lifecycle

`CONTINUITY_CHECK` against current main/STATE → create/use `worker/W006-T001-A02` → emit `TASK_STARTED` on #188 → work/validate → persist RESULT → open PR → inspect repository CI → emit exactly one terminal `TASK_COMPLETE | TASK_BLOCKED | TASK_STALE`.

If satisfying the objective truly requires a protocol mutation, do **not** mutate protocol in this attempt; emit `TASK_BLOCKED` with the exact protocol change required for Orchestrator review.
