# DISPATCH W006-T005-A02

`STATUS: READY`
`TASK_ID: W006-T005`
`ATTEMPT_ID: A02`
`BASE_STATE_VERSION: 0050`
`BASE_COMMIT_SHA: 7139b482a3e61e70b957a2573f11a1cbf7e0d3a5`
`ISSUE: #192`
`ROLE: Workflow/Runtime + Reliability + Provider Platform`
`DEPENDENCIES: W006-T001,W006-T003`
`WORKER_BRANCH: worker/W006-T005-A02`
`TASK_SIGNALS_REQUIRED: YES`
`PRIOR_ATTEMPT: W006-T005-A01 BLOCKED / immutable diagnostic`

## Why this fresh attempt exists

A01 performed a valid continuity check and correctly emitted `TASK_BLOCKED` instead of fabricating comparative runtime evidence. Its worker environment did not have LangGraph/DBOS/Temporal installed and external package resolution failed at DNS. The dispatch explicitly required executed side-by-side evidence, so documentation or framework reputation could not substitute for execution.

A01 is immutable blocked evidence and must not be reused. A02 is a fresh retry that moves the comparative execution into a dependency-capable clean-checkout environment.

## Authority / accepted inputs

Consume current canonical main after STATE 0051 is merged, including:

- accepted W006-T001-A02 contract/schema foundation;
- accepted W006-T003-A01 state↔event/CAS/outbox reference semantics and 14-scenario failure harness;
- accepted W005-T009-A02 benchmark methodology: non-compensatory hard gates → raw multidimensional metrics → uncertainty where meaningful → Pareto; scalar utility only with representative evidence;
- accepted W005/W006 runtime decision state: production runtime remains `PENDING_EVIDENCE` / no winner.

Preserve this retry provenance `STATE 0050 / 7139b482a3e61e70b957a2573f11a1cbf7e0d3a5` and run a fresh `CONTINUITY_CHECK` against observed current STATE/main before substantive work.

## Objective

Execute a reproducible side-by-side runtime/shared-state bakeoff for the exact Academy `3 audiences × 3 formats = 9` workload under identical failure and ownership semantics, using a dependency-capable runner, then persist raw evidence and a Decision-Research-compliant decision state without manufacturing a winner.

## Mandatory execution path

1. Use GitHub Actions clean-checkout execution as the default unblock path if it can provide the needed dependency/network/service access. A dedicated task-owned workflow is permitted.
2. Pin exact benchmark dependency/runtime versions. Benchmark pins are evidence-harness inputs only and do not lock the product package manager/runtime stack.
3. Minimum executed comparative set:
   - custom/CAS-capable control using accepted T003 semantics;
   - LangGraph;
   - DBOS.
4. Temporal remains an explicit challenger. Execute it when needed to resolve an unresolved production-runtime hypothesis, when the minimum set cannot produce a valid decision surface, or when it is materially necessary to distinguish orchestration semantics. If not executed, explain precisely why the minimum set is sufficient for this attempt and keep Temporal non-selected/open.
5. A Postgres/service container may be used as a common benchmark substrate where required by candidates. Such use is **benchmark infrastructure only** and must not be interpreted as a production database winner.
6. Do not substitute vendor documentation, synthetic API shims pretending to be the candidate runtime, or unexecuted pseudocode for actual candidate execution.

If dependency/network/service access remains unavailable even in the CI path, emit `TASK_BLOCKED` with exact runner logs/artifacts rather than inventing evidence.

## Exact workload identity

Use immutable branch IDs for the Cartesian product:

- audiences: `beginner`, `intermediate`, `advanced`;
- formats: `article`, `carousel`, `short_video`;
- branch ID: `<audience>:<format>`;
- accepted branch membership must be exactly `9/9` for any hard-gate-eligible candidate.

Failure axes are separate from branch identity and must never create or silently rename accepted branch IDs.

## Required failure / recovery matrix

Run materially equivalent scenarios for every candidate, preserving identical assertions wherever candidate semantics allow:

- duplicate delivery;
- timeout after remote/provider side effect succeeds but before local commit/acceptance;
- worker/process crash and restart;
- ownership/revision/CAS contention or the candidate's equivalent fencing primitive;
- stale/late result arrival;
- duplicate/replayed result delivery;
- out-of-order result arrival where applicable;
- resume/replay from durable state;
- split/rejoin or concurrent ownership transition where the candidate exposes that semantics;
- provider/side-effect duplicate-cost accounting.

Use the accepted T003 failure harness as the semantic floor. Candidate adapters may add runtime-specific cases but may not weaken shared assertions.

## Hard acceptance

For any candidate to enter the decision surface:

- exact accepted branch membership = `9/9`;
- accepted join branch loss = `0`;
- accepted join branch duplication = `0`;
- duplicate accepted output from retry/delivery = `0`;
- stale/late overwrite of current accepted state = `0`;
- event/result accepted without authoritative committed transition/state = `0` where applicable;
- defined restart/resume/replay scenarios = `100% PASS`;
- ownership/revision/fencing semantics prevent silent stale writers;
- duplicate external/provider side-effect count and cost exposure are recorded raw rather than hidden;
- candidate-specific critical schema/provenance violations promoted = `0`.

A candidate that fails any required hard gate is ineligible regardless of latency/cost convenience.

## Required raw evidence

Persist machine-readable candidate-by-scenario evidence including at minimum:

- exact runtime/library versions and dependency pins;
- runner OS/Python/service versions;
- branch membership accepted/rejected per scenario;
- state/revision/ownership snapshots needed to audit stale/late behavior;
- retry/restart counts;
- duplicate delivery and duplicate side-effect counts;
- observed runtime/orchestration overhead latency with repeated samples where meaningful;
- raw error/failure taxonomy;
- any externally billed cost actually observed, otherwise explicitly `NOT_OBSERVED`;
- uncertainty/variance/percentiles where repeated stochastic/latency samples make them meaningful;
- source/workload identity so all candidates are provably compared on the same workload.

Do not invent provider pricing, capacity, SLO, RTO/RPO or business utility weights.

## Decision method

Apply in this order:

1. hard-gate eligibility;
2. raw multidimensional metrics with units/missingness;
3. uncertainty where meaningful;
4. point Pareto over eligible candidates;
5. decision state `LOCK | NO_PREFERENCE | PENDING_EVIDENCE` only to the extent justified by executed evidence.

No arbitrary weighted score. No hard-gate compensation. If evidence does not support a winner, persist `NO_PREFERENCE`/`PENDING_EVIDENCE` and the reversal/recheck conditions.

## Output / persistence

Required:

- `SYSTEM/RESULTS/W006-T005-A02.md`;
- task-owned runtime adapter/harness code;
- dedicated clean-checkout workflow or existing workflow extension sufficient to reproduce the executed bakeoff;
- machine-readable raw candidate/scenario observations;
- machine-readable decision artifact with eligibility/Pareto/uncertainty and explicit open challengers;
- PR against `main` from `worker/W006-T005-A02`.

Do not modify canonical coordination files (`SYSTEM/STATE.md`, `SYSTEM/TASK_LEDGER.md`, `SYSTEM/WAVES/*`, checkpoints) or protocol-governed files.

## Lifecycle

`CONTINUITY_CHECK` against current main/STATE → create/use `worker/W006-T005-A02` → emit `TASK_STARTED` on #192 → implement/execute clean-checkout bakeoff → persist RESULT/raw evidence → open PR → inspect repository CI/task workflow → emit exactly one terminal `TASK_COMPLETE | TASK_BLOCKED | TASK_STALE`.
