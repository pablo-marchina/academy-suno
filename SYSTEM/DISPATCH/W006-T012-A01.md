# DISPATCH W006-T012-A01

`STATUS: READY`
`TASK_ID: W006-T012`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0047`
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`
`ISSUE: #199`
`ROLE: Observability + SRE + Security`
`DEPENDENCIES: W006-T009`
`WORKER_BRANCH: worker/W006-T012-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Objective

Integrate W3C propagation, OTel instrumentation, OTLP-compatible export, provider/eval/repair spans, bounded metrics, authorized cockpit trace links and backend comparison on the real workload.

T009-A02 is now accepted/integrated and supplies the frozen-toolchain end-to-end reference path, including authoritative durable product/domain events, replay/reconnect and explicit telemetry-outage isolation. Preserve the accepted three-plane authority model: durable product/run/data state; durable ordered product/domain events; correlated sampled telemetry. Sampled telemetry must never become the live cockpit authority.

## Continuity / lifecycle

Before substantive work:

1. preserve original attempt provenance `STATE 0047 / 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`;
2. fetch current `main` and `SYSTEM/STATE.md` and run a fresh `CONTINUITY_CHECK` against STATE 0058/current main;
3. create/use `worker/W006-T012-A01`;
4. emit exactly one protocol-valid `TASK_STARTED` on Issue #199;
5. persist `SYSTEM/RESULTS/W006-T012-A01.md` plus reproducible observability/live-ops evidence before exactly one terminal signal;
6. do not edit canonical STATE/ledger/wave coordination files.

## Architecture / evidence boundary

W3C Trace Context + OpenTelemetry + OTLP/Collector is the accepted architectural boundary; the observability backend, sampling policy, retention and production SLO/capacity values remain evidence-gated. Any material backend/collector/deployment default requires DRG-compliant evidence; do not manufacture a winner from convenience or a single synthetic benchmark.

Product/domain events are authoritative for cockpit state/replay. Telemetry is correlated, sampled and operational. Raw source, prompts and outputs remain off by default. Trace links exposed to product users must be authorized server-side and use opaque references rather than secret-bearing or tenant-enumerable identifiers.

Benchmark backend/collector candidates, where materially selected, using predeclared multidimensional objectives and representative workload; report raw metrics, uncertainty/missingness and Pareto status. Do not use unsupported scalar utility.

## Hard acceptance

- raw credentials/secrets in telemetry = `0`;
- raw private content emitted by default = `0`;
- high-cardinality tenant/run/job IDs as default metric labels = `0`;
- controlled 9-branch traceability = `PASS` across source/run/job/attempt/event/trace correlation;
- unauthorized cockpit trace-link access = `0` in defined adversarial tests;
- telemetry backend/export outage corrupting or blocking authoritative product path = `0`;
- sampled telemetry used as authoritative live cockpit state = `0`;
- secret/canary leakage in product events/telemetry = `0`;
- backend/collector topology promoted without compliant evidence = `0`;
- scalar/business utility used without representative evidence = `0`;
- production-ready claim = `NOT_AUTHORIZED` from this task alone.

## Output

Persist `SYSTEM/RESULTS/W006-T012-A01.md` plus instrumentation/live-ops implementation, trace/metric/log evidence, outage/adversarial tests, backend comparison/DRG evidence if a material default is proposed, and explicit remaining sampling/retention/SLO/capacity unknowns.
