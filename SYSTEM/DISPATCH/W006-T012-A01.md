# DISPATCH W006-T012-A01

`STATUS: PLANNED`
`TASK_ID: W006-T012`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0047`
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`
`ISSUE: #199`
`ROLE: Observability + SRE + Security`
`DEPENDENCIES: W006-T009`
`WORKER_BRANCH: worker/W006-T012-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Objective
Integrate W3C propagation, OTel instrumentation, OTLP-compatible export, provider/eval/repair spans, bounded metrics, authorized cockpit trace links and backend comparison on the real workload.

## Hard acceptance
Raw credentials/secrets in telemetry = `0`; raw private content emitted by default = `0`; high-cardinality tenant/run/job IDs as default metric labels = `0`; controlled 9-branch traceability PASS; telemetry backend/export outage leaves product path healthy; backend/collector topology not promoted without evidence.

## Output / lifecycle
Persist `SYSTEM/RESULTS/W006-T012-A01.md` + observability/live-ops implementation/evidence. Start only after T009 integration; CONTINUITY_CHECK → `TASK_STARTED` → work → RESULT → one terminal signal. No canonical coordination edits.
