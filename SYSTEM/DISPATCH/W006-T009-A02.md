# DISPATCH W006-T009-A02

`STATUS: READY`
`TASK_ID: W006-T009`
`ATTEMPT_ID: A02`
`BASE_STATE_VERSION: 0056`
`BASE_COMMIT_SHA: 975009cae927a952589e1a757f77f4097054a524`
`ISSUE: #196`
`ROLE: Full-stack Production Integration + Developer Platform Compliance`
`DEPENDENCIES: W006-T007,W006-T008`
`WORKER_BRANCH: worker/W006-T009-A02`
`TASK_SIGNALS_REQUIRED: YES`

## Retry reason

A01 completed a useful integrated reference vertical slice and green task/regression CI, but is diagnostic/not accepted. Its dedicated clean-checkout workflow bypassed the accepted W006-T008-A03 toolchain lock by using non-exact Python `3.13` plus direct `pip install` of a hand-selected dependency subset instead of exact Python `3.13.15`, exact `uv@0.12.18`, committed `pyproject.toml` and `uv.lock`.

A01/PR #225 may be consulted as diagnostic implementation input only. Do not reuse A01 lifecycle or terminal status. A02 requires a fresh branch, fresh lifecycle, its own RESULT and fresh validation under the accepted toolchain.

## Objective

Integrate and prove the same real product path under the accepted frozen toolchain:

`auth -> workspace -> controlled PDF bytes upload/quarantine/provenance -> parse/trust boundary -> durable exact 3x3 workflow -> eval/repair -> aggregate -> durable authoritative product/domain events -> live cockpit`

This is the same product path, not a disposable/parallel demo.

## Continuity / lifecycle

Before substantive work:

1. preserve A02 dispatch provenance `STATE 0056 / 975009cae927a952589e1a757f77f4097054a524`;
2. fetch current `main` and `SYSTEM/STATE.md` and run a fresh `CONTINUITY_CHECK`;
3. create/use `worker/W006-T009-A02` from the dispatch base or otherwise document compatible orchestrator drift exactly per protocol;
4. emit exactly one protocol-valid `TASK_STARTED` on Issue #196;
5. persist `SYSTEM/RESULTS/W006-T009-A02.md` plus reproducible evidence before exactly one terminal signal;
6. do not edit canonical STATE/ledger/wave coordination files.

## Accepted toolchain constraint — mandatory

The clean-checkout evidence for A02 must execute from the accepted W006-T008-A03 frozen surface:

- Python exactly `3.13.15`;
- `uv` exactly `0.12.18`;
- committed root `pyproject.toml`;
- committed `uv.lock`;
- lock freshness/frozen sync must fail closed if stale;
- task tests and accepted regressions must run from that locked environment (`uv sync --frozen`/equivalent accepted locked operation and `uv run ...` or an equally exact proof that does not bypass the lock);
- do not replace the frozen graph with a hand-selected direct-pip dependency subset.

If A02 adds or changes third-party workflow Actions, preserve the accepted supply-chain posture: immutable/full-SHA third-party refs where applicable and least-privilege permissions. Do not weaken T008 release/supply-chain controls.

## Hard acceptance

- current live/reference run correlated by source/run/job/attempt/event identities;
- accepted provenance missing = `0`;
- exact branch coverage = `9/9`;
- accepted branch loss/duplication = `0`;
- silent stale same-run overwrite accepted = `0`;
- duplicate accepted branch output after retry/republication = `0`;
- arbitrary untrusted server filesystem-path production route = `0`;
- controlled upload/quarantine/provenance boundary remains fail closed;
- cross-tenant unauthorized success in defined adversarial regressions = `0`;
- authoritative replay/reconnect = `PASS`;
- cursor is never authorization authority;
- telemetry outage corruption/blocking of authoritative product state = `0`;
- static W004 cockpit used as production live truth/final-evidence fallback = `0`;
- clean locked install/build/test under exact accepted T008 toolchain = `PASS`;
- runtime/database/parser/frontend/deployment/vendor winner manufactured without compliant evidence = `0`;
- hard-gate compensation = `0`;
- production-ready claim = `NOT_AUTHORIZED`.

## Technology boundary

A02 may keep reference adapters explicit and fail closed for production qualification. It must not promote plain async, SQLite, pypdf, a frontend framework, deployment class, identity/data/object vendor, observability backend, provider/model or other unresolved substrate choice merely because the integration test passes.

Any new material technology default beyond the already accepted T008 toolchain requires a complete Decision Research Gate record and evidence. If such evidence cannot be produced, keep the choice `NO_PREFERENCE` / `PENDING_EVIDENCE` or emit `TASK_BLOCKED` rather than silently selecting it.

## Output

Persist:

- `SYSTEM/RESULTS/W006-T009-A02.md`;
- production/reference vertical-slice implementation changes needed for A02;
- machine-readable evidence for hard acceptance;
- clean-checkout CI evidence demonstrating the exact accepted frozen toolchain;
- applicable foundation/security/orchestration/cockpit/ingest/supply-chain regressions.

T011 and T012 remain gated until a T009 attempt is accepted/integrated.
