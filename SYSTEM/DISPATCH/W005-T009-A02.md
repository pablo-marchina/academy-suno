# DISPATCH W005-T009-A02

`STATUS: READY`
`TASK_ID: W005-T009`
`ATTEMPT_ID: A02`
`BASE_STATE_VERSION: 0043`
`BASE_COMMIT_SHA: cfdf5e91cd627b6e204fc4cbfae3da6fc5286d96`
`ISSUE: #159`
`ROLE: Analyst + Eval Scientist + Project Auditor`
`PRIORITY: P0 — sole blocker before T010`
`DEPENDENCIES: none`
`WORKER_BRANCH: worker/W005-T009-A02`
`TASK_SIGNALS_REQUIRED: YES`

## Why A02 exists

A01 completed with valid lifecycle/provenance and useful hard-gate, paired-statistics, uncertainty and Pareto work, but it attempted to `LOCK` a 40/30/15/15 scalar score and fixed practical-effect thresholds without representative human/business evidence. That violates the original A01 scope (`manufacturing a single weighted score` was OUT), the Decision Research Gate, and the operator requirement that material choices be evidence-driven. The Orchestrator therefore preserved A01 as diagnostic research but did not accept it as the W005-T009 dependency.

A02 is a fresh attempt; do not amend/reuse A01 or its terminal signal.

## Success / traceability

- SUCCESS_TARGETS: Evidence & Analytical Rigor; Decision Quality; Execution Robustness.
- PRODUCTION_REFS: PROD-009,PROD-013,PROD-015.
- RISK_REFS: RISK-0004,RISK-0034,RISK-0038,RISK-0040.
- DRG_APPLICABILITY: REQUIRED.
- REQUIRED_INPUT: inspect A01 result/artifacts as diagnostic input, but independently revalidate every retained methodology decision.

## Objective

Produce the reusable quantitative benchmark methodology required by W005 without inventing business utility, evaluation weights, effect thresholds, SLOs, capacity targets, or preference rules unsupported by representative evidence.

The default decision surface must be non-compensatory hard gates + raw multidimensional metrics + uncertainty + Pareto/constraint reporting. A scalar utility/weighted score may exist only if its weights/anchors/thresholds are supported by predeclared representative evidence and sensitivity analysis; otherwise its status must be `PENDING_EVIDENCE` / `NO_PREFERENCE`, not `LOCK`.

## Scope in

- preserve/revalidate representative workload slices and provenance rules;
- common experiment metadata/config/version schema;
- paired comparison identity and baseline parity;
- repeated-run/variance policy;
- confidence intervals/effect-size/reporting guidance when statistically meaningful;
- cost/latency/throughput/reliability measurement conventions;
- quality/eval metric interoperability;
- disjunctive non-compensatory hard-gate handling;
- raw multidimensional and Pareto reporting;
- statistically justified multiple-comparison controls where applicable;
- missingness/evidence-class/held-out rules;
- evidence saturation/stopping rules;
- artifact formats/reproducibility/anti-cherry-picking;
- explicit protocol for how future human/business evidence could justify utility weights or practical thresholds.

## Mandatory corrections versus A01

1. Remove `40/30/15/15` or any other fixed scalar weights from `LOCK` status unless new representative evidence actually justifies them.
2. Remove fixed `2.0 score points`, `5% relative`, or equivalent practical-effect thresholds from `LOCK` unless supported by domain/workload evidence; statistical significance alone must not manufacture practical importance.
3. Do not rank or select a technology/provider from a synthetic composite score.
4. Preserve hard failures as disqualifying dimensions, never compensable.
5. Report raw metrics and Pareto/constraint status before any optional utility view.
6. Run sensitivity analysis across plausible weighting/threshold ranges if a provisional scalar view is demonstrated; material rank instability must result in `NO_PREFERENCE/PENDING_EVIDENCE`.
7. Clearly separate `STATISTICAL_METHOD_LOCK` from `BUSINESS_UTILITY_PENDING_EVIDENCE`.

## Quantitative requirements

- machine-readable schema/config remains required;
- units, estimands, pair/group identities and aggregation levels must be explicit;
- source-group correlation from 3×3 outputs must not be mistaken for independent samples;
- preserve raw observations/candidate failures;
- require uncertainty where repeated/probabilistic measurement makes it meaningful;
- do not force CI/effect-size machinery onto deterministic binary invariants where pass/fail is the relevant evidence;
- all randomization/bootstrap procedures must be seeded/versioned/reproducible;
- no numeric target may be introduced only because a benchmark needs one.

## Definition of done

- corrected shared methodology usable by T010 and later waves;
- A01 useful components explicitly accepted/rejected with rationale;
- machine-readable methodology/result schemas updated or superseded cleanly;
- hard-gate + multidimensional + Pareto decision contract is executable/validated;
- scalar business utility is absent or explicitly `PENDING_EVIDENCE` unless supported by representative evidence;
- statistical and practical-effect policy avoids invented thresholds;
- compatibility with W003/W004/W005 accepted evidence documented;
- local validation/tests executed and reported honestly;
- RESULT persisted before exactly one terminal signal.

## Output / persistence

- `SYSTEM/RESULTS/W005-T009-A02.md`;
- corrected/superseding methodology/DR records under owned docs paths;
- updated machine-readable schemas/harness/tests under existing T009-owned paths as appropriate;
- no canonical coordination edits.

## Kill / fail criteria

Emit `TASK_BLOCKED` rather than fabricating evidence if the only way to produce a scalar utility is arbitrary weighting. `NO_PREFERENCE/PENDING_EVIDENCE` is an acceptable and preferred outcome when evidence does not identify weights/thresholds.

## Lifecycle

CONTINUITY_CHECK against STATE 0043 / observed main → create/use `worker/W005-T009-A02` → `TASK_STARTED` on #159 → execute correction/revalidation → persist RESULT/artifacts → exactly one terminal signal.
