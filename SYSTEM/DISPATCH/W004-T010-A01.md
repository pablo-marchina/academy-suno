# DISPATCH W004-T010-A01

`STATUS: READY`
`TASK_ID: W004-T010`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0022`
`BASE_COMMIT_SHA: READ_FROM_ISSUE_96_AT_START`
`DEPENDENCIES: none`
`ISSUE: #96`
`WORKER_BRANCH: worker/W004-T010-A01`
`TASK_SIGNALS_REQUIRED: YES`
`OWNERSHIP_PATHS: .github/workflows/provider-manual-*.yml, experiments/provider_manual/**, tests/provider_manual/**, docs/provider_manual/**`

## Objective

Turn the integrated provider harness into a minimal, manual, credential-safe execution/import path so a future authorized secret can produce versioned observed evidence with one controlled action instead of bespoke setup.

## Must preserve

- manual invocation only; do not trigger a real provider call automatically;
- secrets must never be committed, echoed or persisted in artifacts;
- missing credential must yield explicit SKIP/BLOCKED semantics, not a fake successful provider run;
- provider/model/version/endpoint provenance required for any observed run;
- cost may be derived only from observed usage plus eligible official pricing provenance;
- content-quality evidence remains separate from mechanics/telemetry evidence;
- no provider preference may be inferred from harness preparation alone.

## Acceptance

- manual workflow/runner wired to the existing T004 harness;
- safe no-secret path demonstrated;
- versioned artifact/export schema compatible with downstream T007;
- importer/validator rejects incomplete or synthetic evidence as comparable provider proof;
- focused tests/validation and concise operator docs;
- lifecycle signals required.

## Output

Persist `SYSTEM/RESULTS/W004-T010-A01.md` plus owned artifacts only. Do not modify canonical coordination files.