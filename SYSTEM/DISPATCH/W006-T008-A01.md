# DISPATCH W006-T008-A01

`STATUS: READY`
`TASK_ID: W006-T008`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0047`
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`
`ISSUE: #195`
`ROLE: Developer Platform + Supply Chain + Release Engineering`
`DEPENDENCIES: W006-T007 (accepted/integrated at STATE 0053)`
`WORKER_BRANCH: worker/W006-T008-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Continuity requirement
Preserve the original attempt provenance `STATE 0047 / 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`. Before substantive work, perform a fresh `CONTINUITY_CHECK` against canonical STATE 0053/current main, consume accepted T007 fan-in authority, and emit `TASK_STARTED` on Issue #195.

## Objective
Benchmark package/dependency managers on the actual selected repository/dependency graph, decide repository topology/task graph only from measured need, and freeze authoritative manifests/locks/canonical commands plus release/supply-chain controls. T007's unresolved technology states remain binding: do not turn runtime/database/parser/frontend/infrastructure/deployment/observability/package-manager convenience into production winners without exact DRG evidence.

## Required evidence / scope
- inventory the actual Python/JS/tooling dependency graph before choosing a manager or task graph;
- compare materially different viable package/dependency-manager alternatives where the choice is material, using reproducible clean-checkout evidence and predeclared criteria;
- decide repository topology only if measured dependency/build/release needs justify migration; otherwise retain current topology without falsely labeling it a universal architectural lock;
- produce committed authoritative manifests/locks and canonical clean install/build/test commands for the selected implementation graph;
- third-party GitHub Actions used for release/supply-chain paths must be pinned to full commit SHAs;
- release permissions must be least-privilege and explicit;
- caches must derive from authoritative lock/manifests rather than movable environment assumptions;
- generate and verify an SBOM for releasable artifacts;
- generate verifiable provenance/attestation evidence appropriate to the selected release path;
- preserve exact dependency/tool versions and raw timing/failure observations needed for reproducibility;
- any new material technology winner must have exact DR evidence, confidence, uncertainty and reversal conditions.

## Hard acceptance
Clean locked install/build/test PASS; movable third-party release Actions = `0`; unnecessarily broad release token permissions = `0`; releasable artifact has verifiable SBOM + provenance; repository migration occurs only with DRG evidence; hard-gate failure cannot be compensated by convenience, speed or familiarity.

## Output / lifecycle
Persist `SYSTEM/RESULTS/W006-T008-A01.md` + reproducibility/supply-chain artifacts + any exact DR record needed for a material toolchain/topology decision. Use `worker/W006-T008-A01`. Lifecycle: `CONTINUITY_CHECK` → `TASK_STARTED` → work/progress → RESULT → exactly one terminal signal. Do not edit canonical coordination files.
