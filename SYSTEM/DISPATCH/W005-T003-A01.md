# DISPATCH W005-T003-A01

`STATUS: READY`
`TASK_ID: W005-T003`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0040`
`BASE_COMMIT_SHA: 1cfeb9803036767f4b2cf14320e885751c266f10`
`ISSUE: #153`
`ROLE: Production Architect`
`PRIORITY: P0`
`DEPENDENCIES: none`
`WORKER_BRANCH: worker/W005-T003-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Success / traceability
- SUCCESS_TARGETS: Production Engineering; Execution Robustness; Decision Quality.
- PRODUCTION_REFS: PROD-003,PROD-006,PROD-010,PROD-013,PROD-016.
- RISK_REFS: RISK-0033,RISK-0034,RISK-0039.
- DRG_APPLICABILITY: REQUIRED.

## Objective
Run a systematic orchestration/durability bakeoff. The existing `AsyncGraphOrchestrator` + SQLite RunStore is the mandatory baseline. Compare credible stateful/durable workflow challengers, including LangGraph and any other materially relevant alternative, using the Academy Suno workload and failure modes. Additional layers such as Temporal must earn their complexity with evidence.

## Scope in
- exact 9-branch fan-out/lossless join;
- branch-local eval/repair and retry semantics;
- checkpoint/resume after interruption;
- concurrency and multi-replica/shared-state implications;
- transport vs quality retry separation;
- crash/restart/failure probes;
- state inspection/auditability and integration with live events;
- operational/dependency complexity and migration cost from baseline.

## Scope out
- replacing the baseline merely because a framework is popular;
- full migration implementation;
- assuming SQLite is production-suitable or unsuitable without testing the relevant constraints.

## Quantitative targets
Use identical representative jobs where possible. Measure at least correctness/invariants, recovery result, latency/overhead, state writes/checkpoints, dependency/operational burden proxies and failure outcomes. Preserve hard gates: 9/9, no branch loss/duplication, no hard-gate relaxation. Multiple repetitions where variance matters.

## Definition of done
- DR record with primary/current sources and alternatives;
- reproducible benchmark/failure harness or explicit justification for non-testable dimensions;
- raw results + limitations;
- migration/compatibility implications;
- `LOCK | NO_PREFERENCE | PENDING_EVIDENCE` outcome, confidence and reversal conditions;
- RESULT persisted before terminal signal.

## Output / persistence
- `SYSTEM/RESULTS/W005-T003-A01.md`;
- `docs/decisions/research/DR-*.md`;
- owned benchmark artifacts under `experiments/` or `artifacts/`;
- do not modify canonical coordination files.

## Lifecycle
CONTINUITY_CHECK → worker branch → `TASK_STARTED` on #153 → work → persist RESULT/artifacts → exactly one terminal signal.
