# DISPATCH W006-T008-A03

`STATUS: READY`
`TASK_ID: W006-T008`
`ATTEMPT_ID: A03`
`BASE_STATE_VERSION: 0054`
`BASE_COMMIT_SHA: 869e94a8694c96ee460b8f27d9678623838fa61e`
`ISSUE: #195`
`ROLE: Developer Platform + Supply Chain + Release Engineering + Evidence Auditor`
`DEPENDENCIES: W006-T007 (accepted/integrated)`
`WORKER_BRANCH: worker/W006-T008-A03`
`TASK_SIGNALS_REQUIRED: YES`

## Why A03 exists

A01 is diagnostic/not accepted because its material package-manager lock lacked a complete mandatory Decision Research record.

A02 independently repaired the DRG structure and produced strong current source/benchmark/supply-chain evidence, but it is also diagnostic/not accepted. A02 promoted uv through preregistered scalar weights and synthetic equal scores despite accepted W005 benchmark methodology v002 keeping scalar/business utility `PENDING_EVIDENCE` until representative human/business evidence exists. Pre-registration is not a substitute for utility evidence.

A01/A02 attempts are immutable and must not be reused. Their artifacts may be cited as diagnostic/counterfactual evidence only.

## Objective

Produce the W006-T008 toolchain/supply-chain decision under the already-accepted non-compensatory multidimensional decision surface, then validate any promoted baseline from a clean checkout. Preserve every unrelated T007 open technology boundary.

## Mandatory decision surface

A03 is bound to W005-T009-A02 and W006-T007-A01:

1. non-compensatory hard gates first;
2. raw multidimensional metrics/objectives with units and direction;
3. explicit missingness/non-comparability rather than synthetic neutral scores;
4. uncertainty/repeats where meaningful;
5. point Pareto among eligible candidates only when all required objectives are valid/comparable;
6. no scalar weights, weighted utility, synthetic composite score, invented practical-effect threshold or invented lexicographic priority unless representative business/human evidence exists, is cited, and was frozen before outcomes;
7. if one eligible candidate is uniquely non-dominated on the predeclared representative objective set, a scoped `LOCK` may be justified; if trade-offs or missing required objectives remain, emit `NO_PREFERENCE` / `PENDING_EVIDENCE`.

Do not claim a preference merely because a candidate is fastest if another required objective is unmeasured or materially traded off. Conversely, do not invent equal `1.0` values for unmeasured dimensions to manufacture a Pareto or utility winner.

## Decision Research requirements

Create a fresh canonical record at `docs/decisions/research/DR-6009-reproducible-toolchain-supply-chain-pareto.md` if `DR-6009` remains available at worker start; otherwise choose another available unique DR identifier and record the reason.

The record must satisfy every applicable `SYSTEM/DECISION_RESEARCH_GATE.md` field:

- exact decision question and workload/constraints;
- at least three materially different alternatives where available, baseline included where applicable;
- predeclared raw objectives/hard gates before observing A03 results;
- systematic primary-source-first search, date/freshness rule and evidence-saturation stopping rule;
- source table with authority/claims/limitations;
- explicit security/reliability/cost/operational burden/lock-in treatment where relevant;
- representative reproducible benchmark on the actual graph;
- raw per-repeat results + uncertainty/limitations;
- explicit Pareto computation and missingness policy;
- decision status `LOCK | NO_PREFERENCE | PENDING_EVIDENCE` only as evidence supports;
- confidence + reversal/recheck conditions;
- traceability to PROD/RISK/metrics/artifacts.

## Fresh executable evidence

A03 may reuse the A02 harness/source-search structure, but must produce fresh attempt-specific observations sufficient to bind the A03 decision. At minimum:

- uv/Poetry/PDM or three current materially equivalent challengers under the same representative dependency graph;
- clean resolution/lock/locked install/import/warm-resync/repeat-lock stability;
- at least three repeats per candidate when timing variability is used;
- raw persisted measurements and candidate-specific failure/missingness accounting;
- clean locked install/build/test + Foundation Regression under a promoted candidate;
- release-path third-party Actions pinned to full commit SHA;
- least-privilege permissions; movable release Action refs = `0`; unnecessarily broad release token permissions = `0`;
- lock-derived cache keying with low-trust PR cache safety;
- deterministic releasable artifact;
- machine-verifiable SBOM;
- digest-bound local provenance + attestation verification;
- repository migration only if compliant DRG evidence justifies it.

## Boundary conditions

Do not convert toolchain evidence into a hidden production runtime/database/parser/frontend/deployment choice. Preserve T007 authority:

- production workflow/runtime: `PENDING_EVIDENCE`;
- production database/shared-state: `PENDING_EVIDENCE`;
- parser/OCR: `NO_PRODUCTION_PARSER_WINNER`;
- frontend/editor, identity/data/object vendors, observability backend, deployment class and unrelated technology choices remain open unless this exact attempt produces directly representative DRG-compliant evidence for that exact question.

No canonical coordination edits from the worker.

## Required lifecycle/output

1. Read current `main`, STATE 0055, this dispatch, W005-T009-A02, W006-T007-A01, and A01/A02 diagnostic RESULT/DR artifacts.
2. Preserve A03 provenance `STATE 0054 / 869e94a8694c96ee460b8f27d9678623838fa61e` and run a fresh `CONTINUITY_CHECK` against current canonical main/STATE.
3. Create/use `worker/W006-T008-A03`.
4. Emit exactly one protocol-valid `TASK_STARTED` before substantive work. Do not duplicate start signals.
5. Perform research + fresh experiment + implementation + clean-checkout validation.
6. Persist `SYSTEM/RESULTS/W006-T008-A03.md`, canonical DR record, raw machine-readable evidence and reproducibility/supply-chain artifacts.
7. Open worker PR, inspect required CI, then emit exactly one terminal `TASK_COMPLETE | TASK_BLOCKED | TASK_STALE`.

## Hard acceptance

- complete applicable DRG record for every material `LOCK`/default = `100%`;
- scalar/business utility without representative evidence = `0`;
- synthetic neutral scoring of missing required objectives = `0`;
- hard-gate compensation = `0`;
- valid point-Pareto computation on predeclared raw comparable objectives if a winner is promoted;
- clean locked install/build/test = `PASS` if a lock is promoted;
- movable third-party release Actions = `0`;
- unnecessarily broad release token permissions = `0`;
- releasable artifact has verifiable SBOM + provenance/attestation = `PASS`;
- repository migration without DRG evidence = `0`;
- unrelated unresolved production technology winners introduced = `0`;
- production-ready claim = `NOT_AUTHORIZED`.
