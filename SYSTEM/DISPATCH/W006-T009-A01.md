# DISPATCH W006-T009-A01

`STATUS: READY`
`TASK_ID: W006-T009`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0047`
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`
`ISSUE: #196`
`ROLE: Full-stack Production Integration`
`DEPENDENCIES: W006-T007,W006-T008 (accepted/integrated)`
`WORKER_BRANCH: worker/W006-T009-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Continuity authority

Original attempt provenance remains `STATE 0047 / 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`. Canonical coordination has advanced to STATE 0056 and accepted `W006-T008-A03`; the worker must perform a fresh `CONTINUITY_CHECK` against current main/STATE before substantive work and record the observed main SHA in `TASK_STARTED`.

Consume accepted T007 production-substrate fan-in plus the narrow T008 toolchain lock: Python `3.13.15`, `uv@0.12.18`, committed `pyproject.toml`/`uv.lock` for the current single-project Python graph. This toolchain lock does not select production runtime/database/parser/frontend/deployment vendors.

## Objective

Integrate the **same real product path**, not a disposable or fake parallel demo:

auth → workspace → secure upload → parse/provenance → durable exact 9-way workflow → eval/repair → aggregate → durable authoritative product/domain events → live cockpit.

The vertical slice must use current accepted contracts and fail closed where material technology choices remain unresolved. Do not manufacture a runtime/database/parser/frontend/deployment winner merely to make the slice pass. Any new material default requires task-specific DRG-compliant representative evidence.

## Hard acceptance

- current live run correlated by source/run/job/attempt/event identities;
- accepted provenance missing = `0`;
- exact branch coverage = `9/9`;
- accepted branch loss/duplication = `0`;
- silent stale same-run overwrite accepted = `0`;
- duplicate accepted branch output from retry/delivery = `0`;
- arbitrary untrusted server filesystem-path production input route = `0`;
- controlled upload/quarantine/provenance boundary remains fail closed;
- tenant adversarial suite remains PASS with cross-tenant unauthorized success = `0` in defined tests;
- authoritative snapshot/event replay + reconnect PASS, with cursor not acting as tenant/run authorization;
- telemetry outage does not corrupt or block authoritative product completion/result;
- static W004 cockpit is never used as production live truth/final-evidence fallback;
- hard-gate compensation = `0`;
- production-ready claim = `NOT_AUTHORIZED` from this task alone.

## Evidence expectations

Persist evidence for the actual integrated path, including exact 9-way run identities, retry/repair isolation, durable state/event consistency, source/provenance linkage, authenticated live projection/replay, tenant isolation, and failure behavior. If a local/reference adapter is used for an unresolved production substrate, label it explicitly as reference/non-production and do not convert it into a production lock.

## Output / lifecycle

1. Read current `main`, `SYSTEM/STATE.md`, this dispatch, accepted T007 RESULT/fan-in, accepted T008-A03 RESULT/DR-6009 and relevant accepted T002–T006/T010 contracts.
2. Preserve original provenance and perform `CONTINUITY_CHECK` against current canonical main/STATE.
3. Create/use `worker/W006-T009-A01`.
4. Emit exactly one protocol-valid `TASK_STARTED` before substantive work.
5. Implement and exercise the real integrated vertical slice with reproducible evidence.
6. Persist `SYSTEM/RESULTS/W006-T009-A01.md` plus implementation/evidence artifacts.
7. Open worker PR, inspect applicable CI, and emit exactly one terminal `TASK_COMPLETE | TASK_BLOCKED | TASK_STALE`.

No canonical coordination edits from the worker.
