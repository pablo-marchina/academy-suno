# DISPATCH W006-T013-A01

`STATUS: READY`
`TASK_ID: W006-T013`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0047`
`BASE_COMMIT_SHA: 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`
`ISSUE: #200`
`ROLE: Security Red Team + SRE + Project Auditor`
`DEPENDENCIES: W006-T009,W006-T011,W006-T012`
`WORKER_BRANCH: worker/W006-T013-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Objective

Execute threat-model, authorization/upload/tenant/secret/supply-chain adversarial qualification plus concurrency ladder, arrival staircase, burst, soak, saturation, restart/resume, state/event repair, backup/restore and deployment/migration rollback qualification on the accepted real/reference product path.

T013 is a qualification/red-team task, not a mandate to manufacture missing production substrate decisions. It must distinguish what is actually measured on the accepted reference path from what still requires a representative deployed production topology.

## Continuity / lifecycle

Preserve original attempt provenance `STATE 0047 / 0fa1fd46d02d8fb2ad3410823ba417d83b596eac`.

Before substantive work:

1. fetch current `main` and `SYSTEM/STATE.md` and run a fresh `CONTINUITY_CHECK` against observed STATE 0059/current main;
2. create/use `worker/W006-T013-A01`;
3. emit exactly one protocol-valid `TASK_STARTED` on Issue #200;
4. persist `SYSTEM/RESULTS/W006-T013-A01.md` plus raw/reproducible qualification evidence before exactly one terminal signal;
5. do not edit canonical STATE, TASK_LEDGER or W006 wave coordination files.

## Evidence boundaries carried from T011/T012

- T011 is accepted as current-fact / `NO_PREFERENCE` evidence. Fresh representative provider/model comparison was not run, `PARETO_NOT_COMPUTABLE` remains explicit, and no provider/model/routing winner is authorized.
- T012 accepts the W3C + OpenTelemetry-compatible + OTLP-compatible application boundary and telemetry privacy/outage invariants, but concrete observability backend/Collector topology, sampling, retention, SLO and capacity remain evidence-gated.
- runtime/database/parser/frontend/deployment/vendor winners remain unresolved unless this task produces a complete DRG-compliant decision package with representative evidence; otherwise preserve `NO_PREFERENCE` / `PENDING_EVIDENCE`.
- human primary streams remain `0`, adjudicated human gold remains absent, HELD_OUT remains `NOT_RUN`, and audience thresholds remain `DIAGNOSTIC_ONLY`.
- production-ready claim remains `NOT_AUTHORIZED`.

## Toolchain / product-path boundary

Use the accepted current single-project Python toolchain exactly: Python `3.13.15` + `uv@0.12.18` + committed `pyproject.toml`/`uv.lock`, with stale-lock/frozen-install checks where applicable.

Exercise the accepted same-product path and its current contracts; do not create a disposable parallel demo or substitute sampled telemetry for authoritative product/domain events.

## Hard acceptance

- all defined security zero-tolerance gates = `PASS`;
- cross-tenant unauthorized success in defined adversarial tests = `0`;
- secret/credential canary leakage in product events/telemetry = `0`;
- private quarantine bypass in defined tests = `0`;
- arbitrary untrusted server filesystem-path production input = `0`;
- accepted required provenance missing = `0`;
- exact branch coverage = `9/9`, accepted branch loss/duplication = `0`;
- silent stale same-run overwrite accepted = `0`;
- duplicate accepted branch output from retry/delivery = `0`;
- defined restart/resume scenarios = `100% PASS` before any production claim;
- defined backup/restore scenarios = `100% PASS` before any production claim;
- telemetry/export outage must not corrupt or block authoritative product state/events;
- publish measured saturation interval plus raw p50/p95/p99 latency, queue, throughput, error, resource and cost observations where the environment actually exposes them;
- supported-user count, production SLO, RTO or RPO invented from local/reference evidence = prohibited;
- failure/recovery scenarios not executable in the available environment must be reported explicitly as missing production evidence, not converted into PASS;
- hard-gate compensation = `0`;
- production-ready claim = `NOT_AUTHORIZED` unless a later canonical release gate explicitly authorizes it.

## Output / lifecycle

Persist `SYSTEM/RESULTS/W006-T013-A01.md` plus raw qualification evidence, environment/topology identity, executed scenario matrix, measurements, failure/recovery evidence, unresolved gaps and any remediation findings.

`CONTINUITY_CHECK → TASK_STARTED → work → RESULT → exactly one terminal signal`.
