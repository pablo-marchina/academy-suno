# DISPATCH W004-T009-A01

`STATUS: READY`
`TASK_ID: W004-T009`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0022`
`BASE_COMMIT_SHA: READ_FROM_ISSUE_95_AT_START`
`DEPENDENCIES: W004-T002`
`ISSUE: #95`
`WORKER_BRANCH: worker/W004-T009-A01`
`TASK_SIGNALS_REQUIRED: YES`
`OWNERSHIP_PATHS: app/annotation/**, src/suno_content/annotation/**, tests/annotation/**, docs/evals/annotation_operator/**`

## Objective

Build a minimal blind annotation operator/handoff package that makes it easy for two genuinely independent humans to annotate the 36 frozen W004 development items without seeing requested targets, evaluator predictions or held-out data.

## Must preserve

- no auto-labeling, pseudo-human labels or duplicated annotation streams;
- PRIMARY_A and PRIMARY_B sessions remain independent and separately attributable;
- requested target/evaluator/generator fields remain hidden from primary annotators;
- held-out is inaccessible;
- exports must validate against the W004 annotation schema/protocol from T002;
- adjudication inputs remain separate from primary sessions;
- this task may reduce friction but does not itself satisfy W004-T005 human-evidence gate.

## Acceptance

- runnable lightweight operator or equivalent local UI/CLI handoff;
- validated import/export for PRIMARY_A and PRIMARY_B;
- session/annotator provenance sufficient to establish independence without collecting unnecessary secrets;
- completion/progress checks for all 36 items;
- schema/blinding/held-out invariant tests;
- concise operator instructions for two independent humans;
- lifecycle signals required.

## Output

Persist `SYSTEM/RESULTS/W004-T009-A01.md` plus owned artifacts only. Do not modify canonical coordination files.