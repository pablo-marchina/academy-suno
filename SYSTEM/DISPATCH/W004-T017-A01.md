# DISPATCH W004-T017-A01

`STATUS: BLOCKED_UNTIL_POST_MERGE_BIND`
`TASK_ID: W004-T017`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0026`
`BASE_COMMIT_SHA: READ_FROM_ISSUE_115_AT_START_AFTER_RELEASE`
`DEPENDENCIES: W004-T012,W004-T014,W004-T015`
`ISSUE: #115`
`WORKER_BRANCH: worker/W004-T017-A01`
`TASK_SIGNALS_REQUIRED: YES`
`OWNERSHIP_PATHS: .github/workflows/demo-capture*.yml, scripts/demo_capture_ci/**, tests/demo_capture_ci/**, docs/demo/ci/**, artifacts/demo_ci/**`

## Objective
Remove, if technically possible, the manual-capture blocker from W004-T016 by producing a real browser-recorded demo in GitHub Actions against the integrated recipient-facing app.

## Acceptance
- checkout exact task SHA and run the real recipient app in GitHub-hosted CI;
- fetch a real public BCB financial PDF; validate PDF magic and SHA-256; synthetic fallback is forbidden;
- automate real browser interactions for text ingestion, PDF ingestion/upload/path, 3x3/evidence view, persisted FAIL→repair→PASS and evidence-state labels;
- record a real browser video artifact and upload it as an immutable/discoverable GitHub Actions artifact;
- measure actual duration with ffprobe/equivalent and hard fail above 300s;
- persist manifest with task SHA, run/job provenance when available, PDF hash, video hash, duration and tool/browser versions;
- assert required DOM/evidence elements and interactions were visited; metadata-only proof is insufficient;
- preserve MECHANICS_ONLY / DIAGNOSTIC_ONLY / PRODUCTION_UNKNOWN/BLOCKED; no human/provider evidence fabrication;
- if CI cannot create a trustworthy video, terminal TASK_BLOCKED with observed blocker; screenshots/storyboards are not success;
- lifecycle signals mandatory; no canonical coordination writes.

## Output
Persist `SYSTEM/RESULTS/W004-T017-A01.md` + owned artifacts only.
