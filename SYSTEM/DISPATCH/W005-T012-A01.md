# DISPATCH W005-T012-A01

`STATUS: READY`
`TASK_ID: W005-T012`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0040`
`BASE_COMMIT_SHA: 1cfeb9803036767f4b2cf14320e885751c266f10`
`ISSUE: #162`
`ROLE: Synthesizer + Project Auditor + Orchestrator Support`
`PRIORITY: P0 final W005 fan-in`
`DEPENDENCIES: W005-T010,W005-T011`
`WORKER_BRANCH: worker/W005-T012-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Readiness context
Canonical STATE 0047 accepts both dependencies. Preserve the original attempt provenance above and perform `CONTINUITY_CHECK` against observed current main/STATE 0047 before substantive work. Do not silently rewrite the base.

T011 completed with `CRITICAL: 0`, `HIGH: 3`, `MEDIUM: 4`, `LOW: 1`. Absence of a CRITICAL finding is not production readiness. T012 must consume the red-team artifact as a required correction input rather than merely restating T010.

## Success / traceability
- SUCCESS_TARGETS: Execution Robustness; Production Engineering; Decision Quality; Quality.
- PRODUCTION_REFS: PROD-001..PROD-017.
- RISK_REFS: RISK-0021,RISK-0031..RISK-0040.
- DRG_APPLICABILITY: NOT_REQUIRED for fan-in; any newly selected material alternative still requires valid DRG evidence. Unresolved material decisions remain `PENDING_EVIDENCE` / `NO_PREFERENCE`.

## Objective
Reconcile the accepted T010 architecture synthesis with accepted T011 independent findings, close or explicitly carry every material issue, verify DRG/traceability completeness, and produce the evidence-backed implementation DAG for Phase 9+.

## Mandatory T011 dispositions
T012 must explicitly resolve or carry, with implementation gate/owner/dependency/evidence path, all material findings. At minimum:

1. `H-01 repository strategy status inflation`: do not retain an unsupported single-repository topology `LOCK`; lock only `no repository migration without evidence` unless a valid DRG establishes topology.
2. `H-02 state↔durable-event consistency`: define atomic state+outbox/event semantics or deterministic reconciliation semantics, plus failure injection for partial commit, duplicate delivery, reordering, consumer restart and repair/replay before PROD-003/011/017 substrate promotion.
3. `H-03 static W004 cockpit fallback`: classify static cockpit as `DIAGNOSTIC_ONLY / COUNTERFACTUAL`, never production/final-evidence fallback; bind final evidence to live source hash, run/job/event IDs, provider/catalog/policy/evaluator versions and authoritative persistence.
4. `M-01`: define SSE auth/session integration test obligations for connect/resume/revocation/org-switch/cross-tenant cursor behavior.
5. `M-02`: provide a canonical path to globally unambiguous Decision Research references before implementation decisions rely on ambiguous IDs.
6. `M-03`: keep OTel lock narrow at portable instrumentation/export semantics; collector topology/processors/sampling/retention/backend remain evidence-gated.
7. `M-04`: encode explicit at-least-once execution + idempotent accepted-output semantics, immutable attempt identity, late-result handling and duplicate-cost accounting.
8. `L-01`: make event cursor authorization/scope explicit and adversarially testable.

Silence is not closure. If a finding cannot be closed from accepted evidence, persist it as a Phase 9 blocker/evidence task rather than inventing an answer.

## Scope in
- disposition every CRITICAL/HIGH and material MEDIUM finding;
- verify every architecture lock has adequate DRG evidence and reversal condition;
- preserve `NO_PREFERENCE`/`PENDING_EVIDENCE` where warranted;
- produce implementation increments, ownership paths and safe parallelism;
- specify migration from W004 baselines without unnecessary rewrite;
- bind quantitative acceptance/eval/security/reliability gates to each increment;
- distinguish autonomous implementation work from human/external blockers;
- normalize the implementation authority so T010 + T011 do not leave contradictory decision states;
- prepare the exact next-wave task graph for the Orchestrator, including dependencies and acceptance evidence.

## Scope out
- implementing Phase 9;
- suppressing unresolved decisions to make the plan look complete;
- inventing SLO/capacity/RTO/RPO/business thresholds;
- inventing deadline, Suno internal workflow, ROI or partner-owner facts;
- selecting a technology/vendor because multiple workers agree.

## Definition of done
- no unresolved material T011 finding without explicit disposition, blocker or evidence task;
- H-01/H-02/H-03 are explicitly corrected in the final authority plan;
- Production Contract coverage matrix is implementation-ready without relabeling design coverage as production PASS;
- next-wave DAG identifies independent vs dependent work, evidence owners and acceptance gates;
- all material decision states/evidence boundaries/reversal conditions are explicit;
- real product path and final evidence path are materially identical; static/synthetic counterfactuals cannot substitute;
- implementation can start without reopening foundational choices except where explicitly marked evidence-pending;
- RESULT persisted before exactly one terminal signal.

## Output / persistence
- `SYSTEM/RESULTS/W005-T012-A01.md`;
- final W005 implementation-wave planning artifact under owned docs path;
- no canonical coordination edits by worker.

## Lifecycle
Dependencies are accepted. Execute: `CONTINUITY_CHECK` → create/use worker branch → `TASK_STARTED` on #162 → final fan-in → persist RESULT/artifacts → exactly one `TASK_COMPLETE | TASK_BLOCKED | TASK_STALE` terminal signal.
