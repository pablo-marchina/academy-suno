# DISPATCH W007-T005-A01

`STATUS: READY`
`TASK_ID: W007-T005`
`ATTEMPT_ID: A01`
`BASE_STATE_VERSION: 0061`
`BASE_COMMIT_SHA: 3146b06323f8b15a6feef2ae2e6802d7fb7cafef`
`ISSUE: #240`
`ROLE: Eval Scientist + Human Calibration Auditor`
`DEPENDENCIES: none`
`WORKER_BRANCH: worker/W007-T005-A01`
`TASK_SIGNALS_REQUIRED: YES`

## Objective
Execute the accepted independent human calibration protocol: two blinded human primary streams/item, adjudication, source-grouped DEV/CALIBRATION/HELD_OUT isolation and HELD_OUT replication sufficient to determine whether audience/tone/factuality/domain thresholds can leave `DIAGNOSTIC_ONLY`.

## Hard acceptance
Primary human streams must be actual independent humans. LLM/model labels remain secondary sensors and may not be called human gold. Persist instructions, blinded records, disagreements/adjudication, agreement/error statistics, calibration evidence and HELD_OUT results. No threshold or production-eval lock without representative human evidence.

If independent humans are unavailable to the worker, do not simulate them: persist the precise missing external evidence and emit `TASK_BLOCKED`. That is a valid outcome and requires a fresh attempt after the blocker is resolved.

## Lifecycle/output
Fresh continuity check → exactly one TASK_STARTED → worker branch only → `SYSTEM/RESULTS/W007-T005-A01.md` + human calibration/adjudication/HELD_OUT artifacts (or precise blocker evidence) → exactly one terminal signal. No canonical coordination edits and no worker-side merge.
