# W004-T017 CI demo artifacts

Runtime video bytes are stored as immutable/discoverable GitHub Actions artifacts rather than committed to git.

After the real CI attempt, this directory may contain small provenance records that bind the accepted task commit to the Actions run/artifact metadata and the hashes/duration reported by the capture manifest. Such records must not claim successful visual evidence unless the corresponding browser video artifact actually exists and the run's DOM/content assertions passed.
