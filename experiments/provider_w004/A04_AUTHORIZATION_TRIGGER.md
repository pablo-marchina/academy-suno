# W004-T004-A04 authorized execution trigger

This marker records the final autonomous provider-family retry after A02/A03 failed closed without network access. A04 is authorized to route the supplied secret only if it is locally identified as a Groq `gsk_` family, an xAI `xai-` family, or is explicitly wrapped as `GROQ_API_KEY` / `XAI_API_KEY`. All other credential formats remain local and blocked.

The real call, if any, is one bounded Responses API mechanics smoke. Raw response content is discarded after SHA-256, byte-count, returned-model and usage extraction. Official current pricing snapshots are used only to derive cost from provider-reported usage. No content-quality or provider-preference claim is authorized by this trigger.
